※この文書は、AIエージェントの作業用の計画文書であり、正式成果物（ドキュメントやソースコード）から参照することは許さない。
ただし、作業の進捗がわかる様にこの文書を編集するのはOK.作業の結果、ドキュメントとして残した方がよいとおもうものはdocs配下の適切な場所に残しておこう

# エッジAI実装計画 — 音源識別（背景学習＋現場照合）となめらかな障害物回避

対象リポジトリ: 現在の `firmware/` 5層構成（`docs/firmware/ARCHITECTURE.md`）を正とする。
開発環境: **e² studio + VS Code**（`.vscode/scripts/Invoke-RaBuild.sh` が整備済み）。
**実機書込み・FSP生成・Arm向けビルドは macOS の e² studio 環境を正とする。**
ホスト側のオフターゲット回帰（`software/control-sim/`）は 2026-09-16 に Windows でも動くようにした（4-6-7）。
**作業機がどちらかで前提が変わるので、報告時は `build.py --toolchain` の出力を併記する。**

- [エッジAI実装計画 — 音源識別（背景学習＋現場照合）となめらかな障害物回避](#エッジai実装計画--音源識別背景学習現場照合となめらかな障害物回避)
  - [★エージェントへ: セッション開始時に必ず最初に読む](#エージェントへ-セッション開始時に必ず最初に読む)
    - [A-1. 今回のセッションでどこまでやるか決める](#a-1-今回のセッションでどこまでやるか決める)
    - [A-2. 自分のモデルで難しいと判断したらユーザーに申し出る](#a-2-自分のモデルで難しいと判断したらユーザーに申し出る)
    - [A-3. 絶対に作り直してはいけないもの（禁止事項）](#a-3-絶対に作り直してはいけないもの禁止事項)
    - [A-4. 「契約凍結」とは何か](#a-4-契約凍結とは何か)
    - [A-5. 実機作業は人間の担当](#a-5-実機作業は人間の担当)
  - [★現在の進捗サマリ（2026-09-18 更新）](#現在の進捗サマリ2026-09-18-更新)
    - [全体（付録B-5の実行順に沿う）](#全体付録b-5の実行順に沿う)
    - [Phase 2 の内訳](#phase-2-の内訳)
    - [★鎖の断点 — 現場学習と走行反映の接続状況](#鎖の断点--現場学習と走行反映の接続状況)
    - [2026-09-16 セッションで変更したもの](#2026-09-16-セッションで変更したもの)
  - [★発見した問題と改善（2026-09-16 調査）](#発見した問題と改善2026-09-16-調査)
    - [是正済み](#是正済み)
    - [未解決（対応が必要）](#未解決対応が必要)
    - [改善候補（未実施・優先度は低い）](#改善候補未実施優先度は低い)
    - [★音響実装の不変条件](#音響実装の不変条件)
  - [Context](#context)
  - [確定した方針（ユーザー判断済み）](#確定した方針ユーザー判断済み)
    - [Ethos-U を将来課題とする根拠（設計書に書く内容）](#ethos-u-を将来課題とする根拠設計書に書く内容)
    - [計画書からの差分（記録が必要な項目）](#計画書からの差分記録が必要な項目)
  - [車体・センサ諸元（要点）](#車体センサ諸元要点)
    - [エンコーダ搭載輪 = **中央輪**（ユーザー確認済み）](#エンコーダ搭載輪--中央輪ユーザー確認済み)
    - [これで初めて計算できた: 制動距離の見通し](#これで初めて計算できた-制動距離の見通し)
  - [Phase 0: 土台の是正と校正（AI着手の絶対前提）](#phase-0-土台の是正と校正ai着手の絶対前提)
    - [0-A. 系全体のタイミング是正 ★最優先・他のすべてに波及](#0-a-系全体のタイミング是正-最優先他のすべてに波及)
    - [0-B. エンコーダ校正 — 「4つの独立測定」に分割する](#0-b-エンコーダ校正--4つの独立測定に分割する)
    - [0-C. DoA極性の校正](#0-c-doa極性の校正)
    - [0-D. 車体・センサ幾何 — 自己計測を最大化し、手作業を最小化する](#0-d-車体センサ幾何--自己計測を最大化し手作業を最小化する)
      - [0-D-1. ToF 3台の実配置（ユーザー確認済み）](#0-d-1-tof-3台の実配置ユーザー確認済み)
      - [0-D-2. 平行ビーム配置の帰結 — 近距離に横方向の隙間ができる ★安全に直結](#0-d-2-平行ビーム配置の帰結--近距離に横方向の隙間ができる-安全に直結)
      - [0-D-3. 実測が必要な項目（手作業はここまで削減した）](#0-d-3-実測が必要な項目手作業はここまで削減した)
      - [0-D-3a. ToF計測ログ（2026-09-13）](#0-d-3a-tof計測ログ2026-09-13)
      - [0-D-4. 3Dデータから確定した寸法](#0-d-4-3dデータから確定した寸法)
      - [0-D-5. ROI マルチゾーン測距の評価と採用判断](#0-d-5-roi-マルチゾーン測距の評価と採用判断)
    - [0-E. デューティ・実速度・惰走距離の実測](#0-e-デューティ実速度惰走距離の実測)
    - [0-F. `age_ms` 生存性の是正 ★安全に直結](#0-f-age_ms-生存性の是正-安全に直結)
    - [0-G. 併せて片付ける技術的負債](#0-g-併せて片付ける技術的負債)
  - [Phase 1: 開発環境の整備（実機不要・Phase 0 と並行可）](#phase-1-開発環境の整備実機不要phase-0-と並行可)
    - [1-1. ビルド系](#1-1-ビルド系)
    - [1-2. ホスト側 Python 環境（`uv`）](#1-2-ホスト側-python-環境uv)
    - [1-3. RA8P1 への TFLM 導入](#1-3-ra8p1-への-tflm-導入)
  - [Phase 2: 音響識別AIと現場学習](#phase-2-音響識別aiと現場学習)
    - [2-1. 設計の鍵 — 「PCMではなく特徴量をリングバッファする」](#2-1-設計の鍵--pcmではなく特徴量をリングバッファする)
    - [2-2. 特徴量の数値仕様（ホスト参照実装とビット一致を取る対象）](#2-2-特徴量の数値仕様ホスト参照実装とビット一致を取る対象)
    - [2-3. 推論と現場学習 — 背景AEのトリガ ＋ 能動フレーム選別の識別](#2-3-推論と現場学習--背景aeのトリガ--能動フレーム選別の識別)
    - [2-4. バースト送受信で守るべき規律（実読で判明した落とし穴）](#2-4-バースト送受信で守るべき規律実読で判明した落とし穴)
    - [2-5. 新規プロトコルメッセージ](#2-5-新規プロトコルメッセージ)
    - [2-6. 学習結果の永続化 — RA8P1 Code MRAM の予約領域へ保存](#2-6-学習結果の永続化--ra8p1-code-mram-の予約領域へ保存)
    - [2-7. 学習モードの起動と操作](#2-7-学習モードの起動と操作)
    - [2-8. ホスト側検証と実録音のしきい値決定 `software/acoustic-trainer/`](#2-8-ホスト側検証と実録音のしきい値決定-softwareacoustic-trainer)
  - [Phase 3: 環境認識と可視化（rover-monitor の発展）](#phase-3-環境認識と可視化rover-monitor-の発展)
    - [3-1. 目標を「精度」から「正しく分かること」へ置き換える](#3-1-目標を精度から正しく分かることへ置き換える)
    - [3-2. rover-monitor に3層の表示を作る](#3-2-rover-monitor-に3層の表示を作る)
    - [3-3. 状態送信レートを直す（語を増やす前に）](#3-3-状態送信レートを直す語を増やす前に)
    - [3-4. 生カウントは「累積値」＋「時刻語」を送る](#3-4-生カウントは累積値時刻語を送る)
    - [3-5. オドメトリの実装場所](#3-5-オドメトリの実装場所)
    - [3-6. 学習制御に最適なテレメトリ周期（考察）](#3-6-学習制御に最適なテレメトリ周期考察)
  - [Phase 4: なめらかな障害物回避と μT-Kernel タスク分離の実証](#phase-4-なめらかな障害物回避と-μt-kernel-タスク分離の実証)
    - [4-0. ★センサ回避のデッドロック是正（2026-09-16・実施済み）](#4-0-センサ回避のデッドロック是正2026-09-16実施済み)
      - [4-0-1. 症状と再現（control-simで機械再現済み）](#4-0-1-症状と再現control-simで機械再現済み)
      - [4-0-2. 是正 — 「巡航できる広さ」と「走り出せる広さ」を分ける](#4-0-2-是正--巡航できる広さと走り出せる広さを分ける)
      - [4-0-3. 検証（`software/control-sim/tests/test_deadlock_recovery.py`・7件）](#4-0-3-検証softwarecontrol-simteststest_deadlock_recoverypy7件)
      - [4-0-4. 付随して確認した点（是正不要と判断）](#4-0-4-付随して確認した点是正不要と判断)
      - [4-0-5. ★実機で確認すること（人間の担当・B-4）](#4-0-5-実機で確認すること人間の担当b-4)
    - [4-1. 方針 — 古典プランナを先に作り、それを教師にして学習モデルへ](#4-1-方針--古典プランナを先に作りそれを教師にして学習モデルへ)
    - [4-2. 入力ベクトルと `valid_flags` の扱い](#4-2-入力ベクトルと-valid_flags-の扱い)
    - [4-3. 駆動系の実像に合わせる](#4-3-駆動系の実像に合わせる)
    - [4-4. 安全エンベロープ — AIの出力を信用しない構造](#4-4-安全エンベロープ--aiの出力を信用しない構造)
    - [4-5. ★μT-Kernel のタスク分離を「ながら動作」として実証する](#4-5-μt-kernel-のタスク分離をながら動作として実証する)
    - [4-6. `software/control-sim/` — オフターゲット試験基盤](#4-6-softwarecontrol-sim--オフターゲット試験基盤)
      - [4-6-1. 成立する根拠（実読で確認済み）](#4-6-1-成立する根拠実読で確認済み)
      - [4-6-2. 構成](#4-6-2-構成)
      - [4-6-3. バインディングとドリフト対策](#4-6-3-バインディングとドリフト対策)
      - [4-6-4. 4種類のテスト](#4-6-4-4種類のテスト)
      - [4-6-5. 入れてはいけないもの（純粋性の規律）](#4-6-5-入れてはいけないもの純粋性の規律)
      - [4-6-6. 立ち上げ順（最初のセッションでここまで）](#4-6-6-立ち上げ順最初のセッションでここまで)
    - [4-7. ★CPU1 を μT-Kernel で動かす意味を強くする](#4-7-cpu1-を-μt-kernel-で動かす意味を強くする)
      - [4-7-a. 1 ms 周期を「周期ハンドラ＋イベントフラグ」へ移す ★最優先](#4-7-a-1-ms-周期を周期ハンドライベントフラグへ移す-最優先)
      - [4-7-b. 安全ウォッチドッグを独立した最高優先度タスクへ分離 ★安全性の実利あり](#4-7-b-安全ウォッチドッグを独立した最高優先度タスクへ分離-安全性の実利あり)
      - [4-7-c. IPC受信を「ISR → イベントフラグ → タスク起床」へ（任意）](#4-7-c-ipc受信をisr--イベントフラグ--タスク起床へ任意)
      - [4-7-d. タスク単位のフォールト封じ込めと再起動（任意）](#4-7-d-タスク単位のフォールト封じ込めと再起動任意)
      - [4-7-e. 「2つの独立カーネル」という非対称マルチコア構成を前面に出す](#4-7-e-2つの独立カーネルという非対称マルチコア構成を前面に出す)
      - [4-7-f. 測れる主張に落とす（これが成果物）](#4-7-f-測れる主張に落とすこれが成果物)
  - [Phase 5: LAN UDP でのクラウド試験](#phase-5-lan-udp-でのクラウド試験)
  - [実装順序まとめ](#実装順序まとめ)
  - [検証方法](#検証方法)
  - [付録: 確認済み諸元（すべて現在の実ファイル由来、推測値なし）](#付録-確認済み諸元すべて現在の実ファイル由来推測値なし)
    - [RA8P1 (R7KA8P1KFLCAC / EK-RA8P1)](#ra8p1-r7ka8p1kflcac--ek-ra8p1)
    - [μT-Kernel の時間基準（最重要）](#μt-kernel-の時間基準最重要)
    - [音響経路](#音響経路)
    - [プロトコル `firmware/common/acoustic_protocol.h/.c`（両端で同一ソースをコンパイル）](#プロトコル-firmwarecommonacoustic_protocolhc両端で同一ソースをコンパイル)
    - [センサ](#センサ)
    - [駆動・オドメトリ](#駆動オドメトリ)
    - [安全に関する既存の不変条件（AIが破ってはならない）](#安全に関する既存の不変条件aiが破ってはならない)
    - [閾値と速度の現状](#閾値と速度の現状)
    - [ビルド・ホスト環境](#ビルドホスト環境)
  - [付録B: Codex で実装する場合の進め方とモデル配分](#付録b-codex-で実装する場合の進め方とモデル配分)
    - [B-1. フェーズ別の要求能力とモデル配分](#b-1-フェーズ別の要求能力とモデル配分)
    - [B-2. コストを下げる梃子（この計画に固有のもの）](#b-2-コストを下げる梃子この計画に固有のもの)
    - [B-3. 禁止事項](#b-3-禁止事項)
    - [B-4. エージェントに委譲してはいけないもの](#b-4-エージェントに委譲してはいけないもの)
    - [B-5. 推奨する実行順（コスト効率順）](#b-5-推奨する実行順コスト効率順)
  - [付録C: TRONコンテスト2026 評価観点への対応とDEMO設計](#付録c-tronコンテスト2026-評価観点への対応とdemo設計)
    - [C-1. 計画書が約束した機能と現在地](#c-1-計画書が約束した機能と現在地)
    - [C-2. ★最も費用対効果が高い順](#c-2-最も費用対効果が高い順)
    - [C-3. ★DEMO設計 — 成功率を設計で作る](#c-3-demo設計--成功率を設計で作る)
      - [C-3-1. デモを壊す既知の様態と対策](#c-3-1-デモを壊す既知の様態と対策)
      - [C-3-2. デモ会場で測る数値（そのまま発表資料になる）](#c-3-2-デモ会場で測る数値そのまま発表資料になる)
      - [C-3-3. デモの構成（成功率順に段を分ける）](#c-3-3-デモの構成成功率順に段を分ける)
    - [C-4. 計画書との差分（発表時に明示すべきもの）](#c-4-計画書との差分発表時に明示すべきもの)

---

## ★エージェントへ: セッション開始時に必ず最初に読む

このプランは複数セッションに分けて実装する前提で書かれている。
**まずこの節だけを読み、今回のセッションの範囲を決めてから本文へ進むこと。**

### A-1. 今回のセッションでどこまでやるか決める

1. **「実行順」（付録B-5）を見て、まだ終わっていない最初の項目を特定する。**
   飛ばして先の項目に着手しない。特に「契約凍結」と `control-sim` は後続全部の前提になる
2. **その項目が下の「1セッションの単位」のどれかを判定する:**

| 単位                                    | 目安                              | 例                                                               |
| --------------------------------------- | --------------------------------- | ---------------------------------------------------------------- |
| **小**（1セッションで完結させる） | 純関数1本＋そのオフターゲット試験 | `odometry.c`、古典プランナ、`acoustic_identifier` の距離計算 |
| **中**（1セッションで完結させる） | 1ファイル群の新規追加＋ビルド通過 | `task_infer` の骨組み、プロトコル `0x04` の両端実装          |
| **大**（**必ず分割する**）  | 複数ファイルにまたがる挙動変更    | Phase 0-A のタイミング是正、Phase 4 の S1〜S5                    |

3. **「大」に当たる場合は、1セッションで1段だけ進める。**
   Phase 4 の S1〜S5 は**1セッション＝1段**。S1 を終えたら止まり、
   人間が実機ゲートを通すまで S2 に触らない
4. **セッションの終わりに必ず報告すること:** ①何を変更したか ②ビルドが通ったか
   ③**人間が実機で確認すべきこと**（該当フェーズの検証項目から引く） ④次にやるべき項目

### A-2. 自分のモデルで難しいと判断したらユーザーに申し出る

**遠慮せず申し出ること。黙って進めて壊す方がコストが高い。**
次のいずれかに当たったら、作業を止めてユーザーへ報告し、上位モデルへの切替を提案する:

- 付録B-1 で **「最上位」** とされた項目（**0-A / 0-F / Phase 4 の S1〜S5**）に着手しようとしている
- 変更が**安全経路**（停止条件・タイムアウト・veto・`emergency_stop`）に触れる
- 同時に3ファイル以上の**挙動**（整形やリネームではなく）を変える必要が出た
- プラン本文とソースの実態が食い違っていて、どちらが正か判断がつかない
- 2回試して同じビルドエラー／同じ設計上の壁に当たった

**申し出るときの書き方**（そのまま使ってよい）:

> この作業は本プラン付録B-1で「最上位モデル推奨」に分類されています。
> 理由: 〈安全経路に触れる／系全体に波及する／等〉。
> 私のモデルでも着手できますが、〈具体的なリスク〉を見落とす可能性があります。
> 上位モデルへ切り替えるか、このまま進めるかを指示してください。
> 判断材料として、現時点で分かっていることは〈…〉です。

逆に、**「安価」「中位」に分類された項目は自信を持って進めてよい**。
特に純関数＋オフターゲット試験は実機に触れないので、失敗しても安全である。

### A-3. 絶対に作り直してはいけないもの（禁止事項）

この土台には既知の不具合があり、**知らないエージェントは善意で作り直してしまう**。

1. **周期処理に `tk_dly_tsk` を使うな。** `tk_dly_tsk(N)` は N+1 ms 待つ。
   周期は周期ハンドラ＋`tk_set_flg`、経過時間は `tk_get_otm()` で取る（0-A / 4-7-a）
2. **`sensor_snapshot_t.valid_flags` にビットを追加するな。**
   `control/obstacle_avoidance_controller.c:33` が**完全一致比較**なので恒久停止する（3-5）
3. **`age_ms` を「生存性」の判定に使うな。**（2026-09-16に実装を再確認し記述を精密化）
   `services/sensor_hub.c:335` は**成功毎に0**を代入する。`tasks/task_sensor.c:117-120,161-164` が
   取得失敗時に周期分を**加算**するので、`age_ms` は「最後に成功した取得からの経過」として
   **I2C障害の鮮度判定には有効**である（回避側 `:52` の使用は妥当）。
   しかし **`task_sensor` 自体が止まると誰も加算しないので値が凍る**。
   **タスク停止の検出は `update_count`（`control/sensor_liveness.h` → `task_think`）でしか出来ない**。
   0-Fはこの生存性監視の追加で完了しており、**`age_ms` の検査を消してはならない**（両方必要）
4. **`emergency_stop` を通常の障害物vetoに使うな。** CPU1側のラッチで解除に指令往復が要る（4-4-3）
5. **ToF無効時に 0 mm をモデル入力へ入れるな。** 1.0（遠方）＋有効フラグ0にする（4-2）

加えて:

- **新規タスクを `task_registry` に登録するな**（生成失敗がCPU0全停止になる。2-3）
- **音響mutexを保持したまま推論するな**（`TA_INHERIT` で優先度昇格し `task_acoustic_link` を阻害。2-4-3）
- **上りCDC送信を複数タスクから行うな**（`s_sequence++` が非アトミック。2-4-1）
- **ソース追加後は `Fast Build` ではなく `Generate + Clean Build`**（1-1）
- **TFLMベンダツリーを整形・リネーム・lintするな**（差分が爆発する。1-3）
- **`app_config.h` の平文Wi-Fi資格情報を出力・ログ・コミットに載せるな**

### A-4. 「契約凍結」とは何か

**契約＝あとから変えると全フェーズが作り直しになる界面の定義**のこと。
本プランでは次の3つを指す。**実装に入る前にこの3つを確定し、以降は変更しない。**

| # | 契約                                                                                                                                 | 場所 | 変わると何が壊れるか                                                                                                |
| - | ------------------------------------------------------------------------------------------------------------------------------------ | ---- | ------------------------------------------------------------------------------------------------------------------- |
| 1 | **特徴量仕様** — サンプリング16 kHz / 窓400点 / FFT 512点 / ホップ160点 / mel 32bin(125 Hz〜) / int8 / フレーム毎レベル正規化 | 2-2  | ESP32の抽出・CPU0の再組立・背景モデル・ホスト参照実装が**全部**やり直しになる |
| 2 | **プロトコルのメッセージ表** — `0x04` / `0x05` / `0x10` / `0x11` / `0x22` の型番とペイロード構造                    | 2-5  | 両端が同一ソースを共有しているので、片側だけ直すと**無言で壊れる**（CRCは通るがデコードがずれる）             |
| 3 | **IPC語の割当て** — 状態語 `0x88`/`0x89`/`0x8A` と `ACTUATOR_IPC_STATUS_WORD_COUNT` 7→10                             | 3-4  | CPU0/CPU1を同時に更新しないと commit が成立しない。片側だけ焼くと状態が永久に確定しない                             |

**凍結状況: 2026-09-11 に人間が確定。** 以降のセッションは**この表を唯一の正とする**。
実装中に「変えた方が良い」と気づいたら、
**勝手に変えずユーザーへ申し出る**（他フェーズへの波及を人間が判断する）。

凍結しておくと、以降の作業が界面で分割できるので**並列化でき、安価なモデルに出せる**。
これがコストを下げる最大の理由である。

### A-5. 実機作業は人間の担当

物理作業（実機を動かす・測る・止まるのを見る）は**エージェントの担当ではない**。
エージェントは「人間が何を測ればよいか」を明示して渡すところまでを担当する。
ただし本プランでは**計測の大半をローバー自身の自己計測へ寄せてある**（0-B / 0-E 参照）ので、
エージェントが書くべきなのは**計測用のコードとログ出力**である。

---

## ★現在の進捗サマリ（2026-09-18 更新）

> この節が実装状況の**唯一の要約**である。セッション開始時は A-1 の判断より先にここを読む。
> 「完了」はソース・テスト実行ログ・ユーザーの実測報告のいずれかで裏が取れるものに限る。

### 全体（付録B-5の実行順に沿う）

| B-5 | 項目 | 状態 | 根拠 |
| --- | ---- | ---- | ---- |
| 1 | 契約凍結 2-2 / 2-5 / 3-4 | **完了**（2026-09-11） | `docs/firmware/edgeai/README.md` |
| 2 | control-sim 構築（4-6） | **完了**。2026-09-16にWindows対応 | `uv run pytest -q` → **130 passed**（2026-09-19実行） |
| 3 | Phase 1 TFLMベンダリング | **完了** | ベンダ485ファイルのSHA-256検証、CPU0 ELFに96 KiBアリーナ |
| 4 | Phase 0-A / 0-F | 0-A は**第1・2段のみ**（全7定数実測は未達）／0-F は**完了**（実機ゲート済） | 0-A節、`validation/README.md` |
| 5 | Phase 0-B〜0-E 実機計測 | **完了扱い**（ユーザー判断） | `validation/README.md` |
| 6 | Phase 2 音響AI | **完了・非対象音リジェクト性向上済み** | 送受信・UI・タスク・背景AE・識別・MRAM保存・走行反映・しきい値クランプ[0.08, 0.22]・採取ガード・即座リセット実装済、全ビルド・テスト合格 |
| 7 | **Phase 3 IPC拡張・オドメトリ・可視化** | **完了・テレメトリ精査＆音響認識UI刷新済み**（2026-09-19） | IPC状態語10語拡張、オドメトリ、`0x22` POSEテレメトリ。96B制約厳守でテレメトリ重複削除＆音響照合指標（コサイン距離、類似度、ピーク周波数、サンプル数、ステータス）追加、rover-monitor音響照合メーター刷新完了 |
| 8 | **Phase 4 S1〜S5 ＆ CPU1強化** | **S1〜S3実機完了・S4/S5/4-7実装完了 ← 現在地** | S1〜S3は実機確認完了（ユーザー報告）。S4連続追従（バースト撤去）、S5 50ms同期、4-7-a周期ハンドラ化、4-7-b task_safety実装完了。CPU0/CPU1全ビルド・control-sim 130件合格。実機ゲート（S4/S5/4-7、識別追従）へ |
| 9 | **Phase 5 UDP JSON拡張** | **完了扱い**（ユーザー判断） | LAN内PC確認で要件充足。`0x22` POSE含む全テレメトリ送出、rover-monitor受信表示完了 |

### Phase 2 の内訳

「実装済み」は**その部品単体**の話である。**系として動くかは別**なので、直後の「鎖の断点」を必ず読む。

| 節 | 項目 | 状態 | 根拠（実ファイル） |
| -- | ---- | ---- | ------------------ |
| 2-1 | ESP32のlog-mel常時計算・800 msリング・自己診断 | **実装済み** | `esp32s3/src/log_mel_extractor.c`（285行）、`audio_capture.c:139-187` |
| 2-2 | 特徴量仕様とホスト参照実装のビット一致 | **実装済み** | `acoustic-trainer/features.py`、`tests/test_log_mel.py` |
| 2-5 | `0x04` の符号化・復号（両端で同一ソース） | **実装済み** | `firmware/common/acoustic_protocol.c` |
| 2-1 | CPU0側の80フレーム再組立・欠落破棄 | **実装済み** | `services/acoustic_feature_assembler.c`、`task_acoustic_link.c:667` |
| 2-6 | Code MRAM永続化（A/Bスロット・CRC32・世代） | **実装済み・実機未確認** | `services/prototype_storage.c`。32 KiBを16 KiB×2スロットとして、背景デコーダ・5見本・しきい値を保存 |
| 2-7 | ボタン長押し2秒で学習モード、LED表示、5サンプル計数 | **実装済み・実機未確認** | `task_think.c`。96次元要約を5回取得し、leave-one-outしきい値を計算して保存 |
| 2-1 | ESP32のイベントトリガと `0x04` バースト送信 | **実装済み・実機未確認**（2026-09-16） | `audio_capture.c` が300 ms+500 msを退避・収集し、`acoustic_frontend.c` の単一送信タスクが20 msごとに2フレームを送る。PlatformIO build成功 |
| 2-3 | `task_infer`（優先度11の独立タスク） | **実装済み・実機未確認**（2026-09-16） | `tasks/task_infer.c`。`task_registry`外で非致命起動し、完成パッチはmutex外で推論する |
| 2-3 | 背景AE＋能動フレーム識別 | **実装・接続済み、実機未確認**（2026-09-16） | `services/background_model.c`、`acoustic_identifier.c`、`tasks/task_infer.c`。80フレームをmutex外でRLS背景更新・能動要約・cosine照合する |
| 2-3 | **現場学習音響識別の走行反映経路（Issue #9）** | **実装・接続完了**（2026-09-18） | `task_think.c`、`sound_follow_controller.c/.h`、`control_config.h`。`CPU0_SOUND_REQUIRE_IDENTIFIER_MATCH` (既定0=無効) で安全切替可能。control-sim 3件追加通過 |
| 2-5 | `0x22` POSE_TELEMETRY のペイロード | **実装済み**（2026-09-17） | `firmware/common/acoustic_protocol.h/.c`、CPU0 `task_acoustic_link.c`、ESP32 `wifi_telemetry.c` |
| 2-5 | `0x05` / `0x10` / `0x11` のペイロード | **未実装**（型番enumのみ） | `acoustic_protocol.h:62-68` |
| 2-8 | 設計判断を再現するホスト検証 | **完了**（2026-09-16） | `acoustic-trainer/acoustic_learning.py` と9件の回帰でRLS、ゲート、要約、cosine、leave-one-out、不定を固定 |
| 2-8 | 実録音の収集としきい値決定 | **未実施（人間の実機作業）** | 確定した実機録音だけを最終閾値の根拠にする |

### ★鎖の断点 — 現場学習と走行反映の接続状況

```
ESP32 log-mel生成 ✅ ──▶ 0x04送信 ✅ ──▶ CPU0再組立 ✅ ──▶ task_infer ✅ ──▶ 背景AE・能動フレーム識別 ✅ ──▶ 走行反映 ✅ (安全フラグ切替可能)
```

ESP32側は、VADかつレベルしきい値の立ち上がりで直近300 msを退避し、I2Sタスクが
後続500 msを連続収集する。完成イベントは`acoustic_frontend`だけが20 msごとに2フレームずつ
送信するため、複数タスク送信による`sequence`競合も、800 msリングの上書きによる先頭フレーム
喪失も起こさない。送信失敗時は残りを送らず、次イベントの`frame_index=0`でCPU0の再組立を再同期する。
CPU0側では`task_infer`の照合結果を`task_think`が監視し、`CPU0_SOUND_REQUIRE_IDENTIFIER_MATCH`が有効な場合は対象音一致（TARGET）時のみ走行追従を許可・維持する。既定値は0（無効）としてあり、実機しきい値決定前の安全性を確保している。

> **次にやるべき最小単位**: 実機でUSBの80フレーム・バースト受信、5回学習、電源再投入後の
> MRAM復元を確認し、実録音からしきい値を決める。CNN学習・C配列化は行わない。
> `task_infer` の非致命起動と、音響mutex外での計算という安全上の構造は維持する。

### 2026-09-16 セッションで変更したもの

| 変更 | 目的 | 検証 |
| ---- | ---- | ---- |
| `CPU0_SENSOR_CLEAR_DISTANCE_MM (1000)` → `CPU0_SENSOR_RECOVER_CLEAR_MM (650)` と `obstacle_avoidance_controller.c` の2つの判定 | **回避デッドロックの是正**（4-0） | control-sim 105 passed。是正前の閾値へ戻すと新規回帰2件が落ちることを確認済み |
| `software/control-sim/controlsim/toolchain.py` 新規、`build.py`・試験4本の書き換え | ホスト試験基盤の**Windows対応**（4-6-7） | 同上（macOSのclang＋ASan/UBSan経路は変更なし） |
| `tests/test_deadlock_recovery.py` 新規（7件） | デッドロックの恒久回帰 | 同上 |
| ESP32 `audio_capture.c` / `acoustic_frontend.c` | level/VADでの80フレームイベント収集と`0x04`の2フレーム毎送信 | PlatformIO `seeed_xiao_esp32s3` build成功 |
| CPU0 `task_infer.c/.h`、`task_think.c`、`task_acoustic_link.c` | 優先度11の独立タスク、イベント通知、背景学習、見本照合、MRAM保存 | CPU0 Generate + Clean Build 0 errors、control-sim 102 passed |
| `software/acoustic-trainer/acoustic_learning.py`、`tests/test_acoustic_learning.py` | 背景AE・能動フレーム照合のホスト数値契約 | `uv run python tests/test_acoustic_learning.py` → 9 passed |
| CPU0 `services/background_model.c/.h`、`acoustic_identifier.c/.h`、`tasks/task_infer.c`、`services/prototype_storage.c`、`tasks/task_think.c` | RLS背景AE、能動選別、96次元要約、cosine、leave-one-out、不定結果、32 KiB MRAM A/B保存、5回学習UI | control-sim全体102件 passed、acoustic-trainer 17件 passed、CPU0 Generate + Clean Build 0 errors・164 warnings。実機確認としきい値決定が次工程 |


---

## ★発見した問題と改善（2026-09-16 調査）

実コードを読んで確認した事項のみを載せる。**推測は載せない。**

### 是正済み

| # | 問題 | 影響 | 是正 |
| - | ---- | ---- | ---- |
| 1 | **回避が恒久停止する**。復帰判定と再試行返却が巡航幅1000 mmに紐づいていた | ユーザー報告の「障害物が多いとデッドロック」そのもの。**デモを最も確実に壊す** | 4-0。`CPU0_SENSOR_RECOVER_CLEAR_MM (650)` を新設。回帰7件追加 |
| 2 | **Windowsでホスト回帰が1件も走らない**。`clang`・`.so`・ASanを直書き | B-2の最大の梃子（テストに対して反復する）が失われていた | 4-6-7。`controlsim/toolchain.py` を新設。105 passed |
| 3 | **なめらか回避の調整値が直値**。850 / 950 / 900 / 250 / 500 / 130 / 25 / 15 が `.c` に散在 | デモ調整で触るべき値が1箇所に無い。1000 mmを2つの意味で使ったことが#1の原因 | `control_config.h` へ移設。**値は不変**（下記の同値性検証） |
| 4 | **`CPU0_SENSOR_CAUTION_DISTANCE_MM (700U)` が未使用** | 実挙動（850 mm）と乖離した定数が残り、読者を誤らせる | 削除 |
| 5 | **A-3 #3 の `age_ms` 記述が実装と食い違っていた**（「恒真」は0-F是正前の姿） | 将来のエージェントが `age_ms` 検査を「無意味だから」消す危険 | A-3 #3 を精密化。**`age_ms`（I2C鮮度）と `update_count`（タスク生存）は両方必要** |
| 6 | **`CPU0_AUTONOMY_MODE` のコンパイル時排他** | 音源追従と回避が同時に動かない | S2で解決。`safety_arbiter` 経由での実行時合成へ移行 |
| 7 | **走行経路への識別結果の反映が無い（旧#9）** | 一致を測定できても音源追従条件に接続されていなかった | `CPU0_SOUND_REQUIRE_IDENTIFIER_MATCH` (既定0) で安全切替可能にし、`task_think` と `sound_follow_controller` へ接続。回帰3件追加・全129件合格 |
| 8 | **Phase 5 UDP JSONクラウド試験** | 未整理だった外部連携要件 | ユーザー判断によりLAN内PC確認で完了扱い。`0x22` POSE含む全テレメトリ送出完備 |
| 9 | **現場音響識別が学習音以外の声や物音に誤反応する** | 5サンプルのばらつきで閾値が0.40〜0.80へ過大化し非対象音も受容。また暗騒音の誤学習・NOT_TARGET時のラッチ未破棄・トリガー未解除が重なる | `acoustic_identifier.c`にしきい値クランプ[0.08, 0.22]と1.5σ適用、`task_think.c`に学習採取音量(>=-45dBFS)&VADガードおよびNOT_TARGET即時リセット、`sound_follow_controller.c`に非対象音時の即時トリガー解除を追加。control-sim 130件・全ターゲットビルド合格 |

> **#3の同値性検証**: `uv run python -m controlsim.fingerprint` が
> ToF 19値の3重直積 × gyro 7値 × 時計2始点 = **96,026ステップ**の出力をSHA-256で畳む。
> 移設前後で `1b98b330b6c257d6e52f0a104bd11b97dc80a120b171da3ea49cd637ab6bfe25` と**完全一致**。

### 未解決（対応が必要）

| # | 問題 | 影響 | 誰が | 対応 |
| - | ---- | ---- | ---- | ---- |
| 1 | **Phase 2の実機ゲート** | `0x04`送信とCPU0再組立・背景AE・識別の実機確認が未完。学習成功を主張できない | ユーザー | USBバースト、drop数、推論時間、5見本、MRAM復元、実録音しきい値を確認 |
| 2 | **背景AE・能動フレーム識別の実機根拠が無い** | ホスト・ターゲットの実装はできたが、実録音で受理率・誤受理率を主張できない | ユーザー | 5回学習、電源再投入、対象／非対象・距離・音量を変えた録音で確認（2-3/2-6/2-8） |
| 3 | **優先度分離の実機ジッタ証拠が無い** | `task_infer`は実装済みだが、「ながら動作」の主張は未実証 | ユーザー | 背景AE・識別の負荷ありでCPU1 1 ms周期と`task_command`50 msのジッタを測定 |
| 4 | **Phase 4 実機ゲート（S4/S5/4-7）** | S4連続追従、S5 50ms同期、4-7-b独立安全タスクの実機実証 | ユーザー | 実機書き込みの上、連続走行・周期・安全停止を確認（S1〜S3は実機完了済み） |

### 改善候補（未実施・優先度は低い）

| 項目 | 内容 |
| ---- | ---- |
| `wall_world.py` の世界が**無限平面1枚**だけ | 行き止まり・箱・散在障害物の閉ループ試験が作れない。デッドロックの**閉ループ**再現（4-0は開ループで再現済み）や、C-3-2の「訪問範囲」指標の事前評価に使える。線分レイキャストへ一般化すれば済む |
| 復帰後の初動が無制限 | 4-0は閾値のみ是正した。復帰直後の速度を数百msだけ `CPU0_SENSOR_MIN_FORWARD_RPM` へ抑えると実機の安心感が増す。**安全経路なので実機ゲート後に検討** |
| **復帰の頻度が無制限** | 4-0-5 #5 の循環対策。`attempts` の返却に回数・間隔の制限を足す（例: 同一遭遇内の復帰は1回まで、または返却の継続要求を1000 msより延ばす）。**恒久停止と無計測後退の循環はトレードオフなので、実機の記録を見てから決める**。既存の `test_closed_loop_wall_approach_clears_the_wall_without_repeating_backup` は30秒・36通りで `backup_entries <= 3` を守っており、**単一壁については循環していない**ことの根拠になる。罠（行き止まり・囲い）を作れないのは下記 `wall_world.py` の限界 |
| `CPU0_SENSOR_REPULSE_SIDE_MM` と `CPU0_SENSOR_CRUISE_FULL_MM` が同値(850) | 意味は別。デモ調整では**別々に**動かせる（意図的に2定数にした） |

### ★音響実装の不変条件

送受信、タスク分離、学習UI、永続化を含む音響経路は、背景学習と個別見本照合を前提に構成する。

| 区分 | 内容 | 方針 |
| ---- | ---- | ---- |
| 方式非依存 | ESP32S3のlevel/VADトリガと`0x04`バースト送信 | 維持。実機検証を残す |
| 音響タスク | `task_infer`（優先度11・イベントフラグ起床） | 音響mutex外で背景更新・要約・cosine照合を実行する |
| 学習モードUI | 長押し・LED・サンプル計数 | 96次元の個別見本を5件集める |
| MRAM | A/B・CRC-32・世代・読戻し | 32 KiBへ背景デコーダ・見本・しきい値を保存する |
| 識別 | `acoustic_identifier.c`と`prototype_storage_data_t` | 能動選別、96次元要約、個別見本のcosine照合を行う |

旧予約領域は`0x0207F000`、新予約領域は`0x02078000`である。旧レイアウトを誤って再利用しないこと、
また新レイアウトのCRC不一致・世代選択・電源再投入を実機で確認することを必須とする。`0x05`は引き続き不使用である。


---

## Context

`docs/program-plan-2026_ja.pdf`（TRONプログラミングコンテスト2026 応募計画書）の中核機能のうち、
**エッジAIに関わる部分が現行コードに一切存在しない**。

- **AI/DSPコードはゼロ**（`fft` / `mfcc` / `mel` / `arm_math` / `tflite` の該当が `firmware/` 全体で0件）
- ESP32S3 は I2S PCM から RMS/peak スカラーだけを算出し **PCMを捨てている**
- BMI270 は読まれているが、既定ビルドで**消費する制御コードが1箇所も無い**
- `control/obstacle_avoidance_controller.c` は `CPU0_AUTONOMY_MODE` により**既定ビルドで未コンパイル**。
  しかも状態を持たない if/else 梯子で「滑らか」ではない
- 自己位置・環境認識・クラウド送信は未実装（テレメトリは固定IPへの LAN UDP JSON 4 Hz のみ）

さらに調査で、**AI以前に土台側の不具合4件**が判明した（Phase 0 で先に返済する）。

| #           | 不具合                                                                    | 影響                                                                                             | 位置                                                                                                                                                             |
| ----------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A** | `tk_dly_tsk(N)` が N+1 ms 待ち、`*_MS` 定数が反復回数で積算されている | **1 msループが実約2 ms**。報告RPMが約2倍高い                                               | `drivers/encoder.c:17,61-68,133-142` / `tasks/task_actuator.c:69-77` / `services/actuator_service.c:188-196` / `services/drive_service.c:90-100,182-187` |
| **B** | `age_ms` が常に0に戻され、期限判定が**恒真**                      | `task_sensor` ハング時に snapshot が永遠に「新鮮かつ有効」に見え、250 mm veto が凍結距離を読む | `services/sensor_hub.c:335` ＋ `tasks/task_sensor.c:114-115,137-138`                                                                                         |
| **C** | 回避ゲートが`valid_flags` の**完全一致**比較（IMU必須）           | ToF 3台健全でもIMU無効で停止。かつ`valid_flags` にビットを1本足すと恒久停止                    | `control/obstacle_avoidance_controller.c:33` ＋ `services/sensor_hub.h:35-36`                                                                                |
| **D** | 異常ラッチを実行中に**誰もクリアしない**                            | 一度 fault が立つとリセットまで`CPU0_THINK_STATE_FAULT` から復帰不能                           | `tasks/task_think.c:363,380,445,484` ＋ `control/sound_follow_controller.c:278-279`                                                                          |

---

## 確定した方針（ユーザー判断済み）

| 論点                 | 決定                                                                     | 理由                                                                                                            |
| -------------------- | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- |
| 音源識別AIの実行場所 | **ESP32S3が特徴量算出 → RA8P1 CPU0が推論**                        | AIをμT-Kernelマイコン側に置く。特徴量なら96 Bペイロードに収まる                                                |
| GCC-PHAT / TDoA      | **実装しない。XVF3800のDoAを使う**                                 | XVF3800のI2Sはビームフォーミング済みPCMで、生マイク4chが物理的に取得できない                                    |
| 制御MLPの推論エンジン | **TFLM を採用**（Cortex-M85 + Helium、CPU実行）                    | Phase 4の障害物回避MLPを実行する。M85 1 GHz なら数msで収まる                                                    |
| Ethos-U NPU          | **本スコープ外。将来課題として明記**                               | シリコンは実在するがソフト層が丸ごと未整備、かつ**能力を増やさない**（約2 ms→約0.2 ms のみ）。詳細は下記 |
| 現場学習             | **背景AEで環境の候補を検出し、能動フレームの個別見本を照合する** | オフラインCNNを要さず、背景の異常検出と対象音の識別を混同しない |
| 自己位置精度         | **センチメートル精度グリッドは不要**（ユーザー判断）               | 代わりにローバー中心の相対環境認識＋可視化へ振り替える                                                          |
| 学習データ周期       | **通常4 Hz据え置き、収集時10 Hz、上限20 Hz**                       | 情報量の天井がToFの20 Hz、制御判断が`task_think` の10 Hz                                                      |
| クラウド             | **LAN UDP 送信まで**で可                                           | MQTT/TLSはESP32のROMと負荷を食う。範囲外                                                                        |
| 開発環境             | **macOS**（計画書どおり）                                          | `Invoke-RaBuild.sh` が `E2studio.app` を探して headless build する経路が既にある                            |

### Ethos-U を将来課題とする根拠（設計書に書く内容）

**シリコン側は確認済みで実在する**:

- `R_NPU_Type` = 4096 B (0x1000) のレジスタブロック @ `R_NPU_BASE 0x40140000`
- `R_NPU_BASEP0..P3`（ベースポインタ群）＝ **Arm Ethos-U のレジスタ構成**
- `ELC_EVENT_NPU_IRQ = (0x067)`（`bsp_elc.h:103`）でIRQ配線可能
- `BSP_PERIPHERAL_NPU_PRESENT (1)`、`BSP_FEATURE_CGC_HAS_NPUCLK (1)`、`BSP_CFG_NPUCLK_DIV` = /2（**500 MHz**）で既に設定済み

**ソフト層が丸ごと存在しない**:

- FSP 6.4.0 の生成ツリーに `r_npu` は無い（`ra/fsp/src/` は bsp / r_iic_master / r_ioport / r_ipc / r_usb_basic / r_usb_hcdc のみ）
- `ra/arm/CMSIS_6/CMSIS/` は **Core のみ。DSP も NN も無い**
- Ethos-U ドライバ、Vela コンパイラ成果物、TFLM delegate はいずれも未導入
- 必要になるのは ドライバ＋Vela＋delegate＋NPU可視領域へのメモリ配置＋D-cache の clean/invalidate＋IRQ

**判断**: NPUは推論を約2 ms→約0.2 ms にするだけで、**解くべき性能問題が存在しない**。
成功すれば目立つが失敗すれば見せ物ゼロ。よって設計書には
「NPUを同定し余力を定量化した上で、CPU実行で要件を満たすと判断した」と明記し、
将来拡張として残す。これ自体が設計判断の説明になる。

### 計画書からの差分（記録が必要な項目）

1. **GCC-PHAT/TDoA を実装しない** — XVF3800 のハードウェアDoAで代替
2. **「センチメートル精度のグリッドでマッピング」を目標から外す** — ローバー中心の相対環境認識
   （障害物・走行経路の可視化）に置換。根拠は Phase 3
3. **Ethos-U NPU は将来課題** — 上記
4. **クラウドは LAN UDP 送信まで** — MQTT/TLS/クラウドSDKは範囲外
5. 開発PCは計画書どおり **macOS**（差分なし）

---

## 車体・センサ諸元（要点）

> 寸法・ToF配置・計測方法の詳細は **Phase 0-D** に集約した。ここでは設計判断に効く要点だけ示す。

- ★**車輪外径 108.00 mm / トレッド面幅 40 mm**（`tire.stl` 実測）。**円周 339.29 mm**
- ★**ToF 3台は全て前方向き**（横方向にオフセットした平行ビーム）。扇状配置ではない（0-D-1）
- ★**ToF光学中心は左(-90, +94)、中央(0, 0)、右(+90, +94) mm**。隣接間隔90 mmで、250 mm付近ではビーム間が連続する（0-D-1/2）
- 構成（README より）: 6× GA25 ギヤドモーター、4× ステアリングサーボ、2× BTS7960

### エンコーダ搭載輪 = **中央輪**（ユーザー確認済み）

**オドメトリにとって最良の配置**である。4輪操舵は4隅の輪であり、
中央輪は非操舵なので、**中央輪の走行距離はそのまま車体中心線の走行距離になる**。
操舵角による距離補正が不要で、旋回時のスリップ影響も4隅輪より小さい。

### これで初めて計算できた: 制動距離の見通し

**速度の推定**（開ループなので指令RPMではなく実デューティから逆算する）:

- `120 RPM` 指令 → raw duty `120*1000/300 = 400‰` → 左右倍率 ×0.8 → **320‰**
- JGA25-370 は 12 V / 300 RPM 仕様。320‰ で無負荷なら約 96 RPM、負荷込みで **60〜96 RPM**
- 車輪速 = RPM × 339.29 mm/min ⇒ **0.34 〜 0.54 m/s**

**検知→適用の最悪遅延**（`tk_dly_tsk` の +1 ms を織り込む）:

| 区間                  | 名目   | 実際                                          |
| --------------------- | ------ | --------------------------------------------- |
| `task_sensor` 周期  | 50 ms  | 51 ms                                         |
| `task_think` 周期   | 100 ms | 101 ms（S5後は 51 ms）                        |
| `task_command` 周期 | 50 ms  | 51 ms                                         |
| CPU1`task_actuator` | 1 ms   | 2 ms                                          |
| **合計**        |        | **約205 ms**（S5後 **約155 ms**） |

**遅延中の走行距離**:

| 速度     | 205 ms | 155 ms (S5後) |
| -------- | ------ | ------------- |
| 0.54 m/s | 111 mm | 84 mm         |
| 0.34 m/s | 70 mm  | 53 mm         |

`CPU0_SENSOR_HARD_STOP_DISTANCE_MM (250)` に対し遅延分は **70〜111 mm**。
残り **139〜180 mm** が惰走に使える。しかも速度0指令は
`drive_service_stop()` → `bts7960_stop()` で **ランプせず即座にPWM停止＋EN Low** なので、
惰走はギヤヘッドの逆駆動抵抗だけ（ギヤドモーターでは小さい）。

⇒ **250 mm は妥当な見通し（余裕 約1.3〜2.5倍）**。0-Eの実測では、320‰出力停止後の
惰走は左右約11.1／10.6 mmだった。旧計画の「机上で判定不能」は解消した。

---

## Phase 0: 土台の是正と校正（AI着手の絶対前提）

### 0-A. 系全体のタイミング是正 ★最優先・他のすべてに波及

**優先順位変更（2026-09-12、ユーザー指示）:** コストを抑え、停止に直結する問題を優先する。
詳細なPWM更新間隔・ランプ・帰還待ち・旧版対照波形は未測定のまま保留し、0-Fへ進む。
下記の全7定数実測という元の完了条件は未達として残すが、0-F着手を妨げない。

**進捗（2026-09-12）: 第1・2段の実装・ホスト検証済み。第1段のソフト周期とtimeout発火・復帰、第2段の状態周期・送信数をユーザー確認済み。0-A全体は未完了。**

- [X] 第1段: CPU1駆動を周期ハンドラ＋イベントフラグへ移行し、
  `tk_get_otm()`からの実Δtをencoder・drive・actuator serviceへ渡す。
  RPM窓、PWMランプ／5 ms更新、速度帰還150 ms待ち、指令1500 ms期限を実時間化。
- [X] 実周期と速度窓のLive Watch変数、時刻／待機異常時の停止、周期資源解放を追加。
- [X] CPU1 Generate + Clean Buildと生成差分整理後のClean Build成功。
  ホストは既存5＋CPU1構成別2＋状態タスク1の計8テスト成功（ASan/UBSanを含む）。
- [X] ユーザー実測: 手計測10.02 s / 20.19 sで更新10067 / 20211回、OS経過10068 / 20212 ms。
  差分10144回 / 10144 msで平均1.000 ms、min=1、max=2、late=1のまま、error=0。
  ソフト周期確認は通過。実出力の停止時間や残り6定数の検証を代用しない。
- [X] 第1段のCPU0送信断確認: CPU1実行継続、約1.5秒でtimeout bit発火・左右duty=0、CPU0再開で復帰をユーザー確認。
  駆動系電源OFFの診断確認として記録。通電回転中の実停止時刻は未測定。
- [X] 第2段: `task_status`を周期通知10 ms・実Δtへ移行。送信中も100 ms採取周期を計時し、
  1起床1語、再送中のsnapshot維持、診断変数と失敗時資源解放を追加。
- [X] 第2段の状態周期・送信数: ユーザー実測1001→1984回、10011→19841 msで平均10.000 ms。
  min=10、max=11、late=1を維持、error=0、採取100→198、全語送信99→197、採取間隔100 ms、retry=0。
- [X] 第2段の駆動回帰: 駆動周期last=1 ms・error=0、CPU0送信断中のCPU1実行継続、
  約1.5秒でtimeout bit発火・左右duty=0、CPU0再開後の指令・状態送信復帰をユーザー確認。
- [X] RPM計算窓: sample_elapsed_ms=100、約10秒でsample_countが約100増加、静止時左右RPM=0をユーザー確認。
  前進で正・後進で負との報告も記録。counts/revは0-Bで実測し702へ校正済み。
- [X] 通電停止の目視確認: CPU0 Suspendから約1.8秒で全車輪停止をユーザー確認。
  移動時間を一時10000 msにして試験。dutyが0になる時刻は未測定で、1.5秒の実出力停止時刻とは区別する。
- [ ] 保留: 全7定数の前後実測とEN/PWM/サーボ実出力の停止時刻。
  ソフト周期以外の実出力・停止時間と、是正前の対照値は未測定。

実装内容・監視変数・手順は [CPU1_TIMING_VALIDATION.md](docs/firmware/CPU1_TIMING_VALIDATION.md)。
0-Fのセンサー更新停止試験、0-CのDoA極性校正、0-DのToF配置・名目FoV計算、0-Eの
デューティ・実速度・惰走距離計測は完了扱いとする。次工程へ進む。
4-7-bのCPU1駆動タスク独立watchdogは未実装。

**根拠（呼び出し鎖）**:

- `timer.c:107 / :119` が `event->time = lltoul(knl_current_time) + tmout + TIMER_PERIOD`
  （コメントも "add TIMER_PERIOD on wait time"）
- `TIMER_PERIOD = CNF_TIMER_PERIOD`（`knldef.h:66`）
- プロジェクト側 `common/mtkernel/config.h:13` が **1**、BSP側 `mtk3_bsp2/config/config.h:29` が **10**。
  `.project` が `mtkernel_config` を `common/mtkernel` へリンクし、`.cproject` の `-I` 順が
  `mtkernel_config` → `mtk3_bsp2/config` なので**プロジェクト側の 1 が勝つ**
  （この上書きは `firmware/ra8p1/README.md:52-59` に設計意図として明記済み＝**仕様であり負債ではない**）
- ⇒ **`tk_dly_tsk(N)` は N+1 ms 待つ**

> 誤差は**一律 +1 ms**。`tk_dly_tsk(100)` は1%で無害。**問題は 1 ms ループだけ**
> （`tk_dly_tsk(1)` = 2 ms ＝ 2倍）。そこにRPM算出とPWMランプが載っている。

**反復回数を「ms」として積算している箇所**:

| 定数                                          | 名目     | 実際                                        | 位置                                                                    |
| --------------------------------------------- | -------- | ------------------------------------------- | ----------------------------------------------------------------------- |
| `ACTUATOR_LOOP_PERIOD_MS (1)`               | 1 ms     | **≥2 ms**                            | `config/task_config.h:8`、`tasks/task_actuator.c:69-77`             |
| `WHEEL_SPEED_SAMPLE_PERIOD_MS (100)`        | 100 ms窓 | **約200 ms窓を100で除算** → RPM約2倍 | `config/drive_config.h:10`、`drivers/encoder.c:61-68,133-142`       |
| `ACTUATOR_COMMAND_TIMEOUT_MS (1500)`        | 1.5 s    | **約3 s**                             | `config/actuator_config.h:8`、`services/actuator_service.c:188-196` |
| `DRIVE_UPDATE_PERIOD_MS (5)`                | 5 ms     | **約10 ms**                           | `config/drive_config.h:23`、`services/drive_service.c:182-187`      |
| `DRIVE_RAMP_PER_MS (2)`                     | 2‰/ms   | **約1‰/ms**                          | `config/drive_config.h:22`、`services/drive_service.c:90-100`       |
| `DRIVE_SPEED_FEEDBACK_START_DELAY_MS (150)` | 150 ms   | 約300 ms                                    | `config/drive_config.h:28`（FB無効なので現在未使用）                  |
| `CPU1_STATUS_TASK_PERIOD_MS (10)`           | 10 ms    | **11 ms**                             | `config/task_config.h:13`、`tasks/task_status.c:94-107`             |

**作業**:

1. 経過時間を**反復回数ではなく実時間基準**で取る。
   現状 `tk_get_otm` / `tk_get_tim` / HWタイマを読む箇所は **CPU1全体で0件**。
   ★**実装方式は 4-7-a の「周期ハンドラ＋イベントフラグ」を採用する**
   （`tk_dly_tsk` の相対待ちをやめれば +1 ms もドリフト累積も同時に消える。
   `CNF_MAX_CYCID (4)` は既に確保済み）
2. `drivers/encoder.c` の RPM 窓を実時間基準へ
3. 是正後に全 `*_MS` 定数を再評価

**完了条件**:

1. `actuator_service_update_1ms()` の実周期をフリーランタイマ/スコープで実測し、名目との倍率を記録
2. **上表7定数すべての是正前／後の実効値を対で実測記録**（1点の実測では 0-B・0-E・S4 が未証明の前提を継ぐ）
3. ★**CPU1指令タイムアウトが是正後も発火し safe stop することを実機確認**。
   `ACTUATOR_COMMAND_TIMEOUT_MS` は約3 s → 1.5 s に変わる＝**停止タイミングを変える安全変更**。
   タイミング是正が 1500 ms ウォッチドッグを黙って壊すのが**今日より悪くなる唯一の失敗様態**。
   確認方法: CPU0 の指令送信を意図的に止め、停止までの時間を測る

> 0-A と 0-F はどちらも**安全挙動を変える変更**。Phase 4 の S1〜S5 と同じ規律で、
> 各々「停止することの実機証明」を伴って初めて完了とする。

### 0-B. エンコーダ校正 — 「4つの独立測定」に分割する

進捗（2026-09-12）: ユーザーが左右RPMの静止時0・前進正・後進負を確認（0-B-0の符号観測）。
0-B-2に相当する駆動周期約1 msと速度計算窓100 msも確認済み。
さらに2000 mm手押しを10回実施し、0-B-1のcounts/revを取得した。
左右平均は約702.77 / 700.92、共通推定は約701.85 counts/revで、区間ばらつきは約1%。
走行中の`g_encoder_*_rpm_x10`も確認したうえで、ユーザー判断により共通校正値702を採用した。

変更前の`WHEEL_ENCODER_COUNTS_PER_REV (900U)`（`config/drive_config.h:9`）は
**実測か仮定かのコメントが無い裸の定数**で、真値1800の疑いがあった（RPM 2倍表示）。
**しかし 0-A の窓バグも独立に約2倍を生む**。両者は同じ症状を出すため、
**「報告RPMが2倍だから counts/rev は1800」と結論すると誤る**（片方だけ直して2倍が残る）。
0-Aの実時間窓是正後、手押し測定の共通推定を丸めた702へ変更した。

互いに独立な量を別々に測る:

| #     | 測定                                                                                                                                                   | 使う量         | 使わない量         |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | ------------------ |
| 0-B-0 | `WHEEL_ENCODER_LEFT_FORWARD_SIGN (-1)` / `WHEEL_ENCODER_RIGHT_FORWARD_SIGN (+1)`（`config/drive_config.h:11-12`）が実機と合っているか            | 符号のみ       | 大きさ             |
| 0-B-1 | **直線走行**。床にテープで 2000 mm を測り、走らせてカウント差を読む。`counts_per_rev = counts × 339.29 / 2000`（車輪円周が既知なので成立）    | カウントと距離 | **時間**     |
| 0-B-2 | `actuator_service_update_1ms()` の実周期を**ソフトのみで**実測（ループ回数と `tk_get_otm()` の経過を10秒間比較。スコープ不要。0-A の成果物） | 時間のみ       | **カウント** |
| 0-B-3 | 0-B-0〜2 確定後に、報告RPMとストップウォッチ実測RPMを比較                                                                                              | 両方           | —                 |

**完了条件**: counts/rev が時間に依らず確定し、0-B-3 の残差が数%以内。
2000 mm手押し10区間の再現性と左右差0.26%からcounts/revは702へ確定し、
ユーザー判断により0-B-3の厳密な同時刻ストップウォッチ照合は追加検証扱いとして、0-Bを完了とする。
変更後の基準値は`WHEEL_ENCODER_COUNTS_PER_REV=702U`である。
**車輪円周 339.29 mm が既知なので、確定した counts/rev で mm/count が即座に出る。**

> **0-B の結果は速度定数へ波及する**。`CPU0_SOUND_MOVE_LEFT/RIGHT_RPM (120)`、
> `CPU0_SOUND_TURN_INNER_RPM (90)`、`CPU0_SENSOR_*_RPM` は「2倍かもしれない読み」で
> チューニングされている。**0-B 完了後の速度定数再評価を Phase 0 の完了条件に含める**。

### 0-C. DoA極性の校正

進捗（2026-09-12）: ユーザーが右音源を正、左音源を負として確認したため、
`CPU0_SOUND_DOA_CLOCKWISE_POSITIVE=0U`は変更しない。極性校正は完了。
採用DoAの詳細診断値追加は、必要なテレメトリ拡張として後続の診断整備へ回す。

進捗（2026-09-21）: マイク音孔面を表（上向き）にするため基板を表裏反転。
マイク配置の左右鏡像反転に伴い、`config/control_config.h` の `CPU0_SOUND_DOA_CLOCKWISE_POSITIVE=1U` に更新。
合わせてWebUI（rover-monitor, acoustic-ai-lab）およびcontrol-simテストの左右極性を更新。

| 作業                           | 変更対象                                                                                   | 完了条件                                                                                                                                           |
| ------------------------------ | ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 駆動停止で車体4方向のDoAを校正 | `config/control_config.h` の `CPU0_SOUND_DOA_CLOCKWISE_POSITIVE`（1U 設定済み）           | 右方向音源が論理`+θ`（右旋回）へ写る。**サーボ極性は独立に確認し、音源座標とサーボ極性を同時に反転させない**                              |
| 採用DoAの診断値追加            | `tasks/task_acoustic_link.c` / `control/sound_follow_controller.c` / テレメトリJSON    | 採用raw/relative DoA、採用観測sequence、検出開始からの経過時間、サンプル数、取得経路が記録される。右→左切替で旧方位が採用されないことをログで証明 |

**次の実機手順（ユーザー作業）**:

1. CPU0/CPU1を702設定でGenerate/Clean/Buildし、両ELFを書き込む。DoA確認中はモーター電源を切る（音響フロントエンドとデバッグ電源は維持）。
2. e2studioのCPU0 Live Watchへ次を追加する。`g_task_acoustic_link_observation.doa_deg`、`g_task_acoustic_link_observation_age_ms`、`g_task_acoustic_link_frame_count`、`g_task_think_new_observation`、`g_task_think_steering_deg`、`g_task_think_state`、`g_task_think_actuator_enable`。
3. 同じ音源を車体から約1 mの正面・右90°・左90°・後方へ置く。各位置で3秒以上待ち、DoAが安定した5サンプルを読む。停止後の値ではなく、`g_task_think_new_observation`が更新されている走行判断前後の値を記録する。
4. 各位置について、`raw doa_deg`の平均と`steering_deg`の符号を次の形式で返す。

   ```text
   位置, raw doa_deg, steering_deg, state, new_observation
   前, ..., ..., ..., ...
   右, ..., ..., ..., ...
   左, ..., ..., ..., ...
   後, ..., ..., ..., ...
   ```
5. 正面の操舵角が0付近で、右音源が正、左音源が負なら現在の`CPU0_SOUND_DOA_CLOCKWISE_POSITIVE=0`を維持する。符号が逆なら同定数だけを1へ変更し、`CPU0_STEERING_SERVO_OUTPUT_SIGN`は同時に変更しない。

### 0-D. 車体・センサ幾何 — 自己計測を最大化し、手作業を最小化する

#### 0-D-1. ToF 3台の実配置（ユーザー確認済み）

進捗（2026-09-13）: ユーザーが光学中心の座標を測定した。車体座標は前方を`+y`、
右方向を`+x`、中央ToFを原点とする。

| 位置    | 光学中心 (x, y) [mm] | 向き             |
| ------- | -------------------: | ---------------- |
| 左ToF   |           (-90, +94) | 前方、中央と平行 |
| 中央ToF |               (0, 0) | 前方             |
| 右ToF   |           (+90, +94) | 前方、中央と平行 |

`ToF参考.png` の赤枠3箇所に、**距離センサICが前方を向くように**取り付けられている。

| 位置         | 取付面                                   | 向き           | 回転                                     |
| ------------ | ---------------------------------------- | -------------- | ---------------------------------------- |
| A（中央）    | シャーシ本体の外側垂直壁                 | **前方** | しない                                   |
| B・C（左右） | サーボを載せる固定ハウジングブロックの面 | **前方** | しない（サーボはハウジング下の脚を回す） |

★**3台とも前方向き。ドキュメントの「LEFT −45° / CENTER 0° / RIGHT +45°」という扇状配置ではない。**
実態は**横方向にオフセットした3本の平行ビーム（カーテン状）**である。
B・C はロッカーアーム先端のハウジングに付くため、**車体中心から大きく外側**にある。

#### 0-D-2. 平行ビーム配置の帰結 — 近距離に横方向の隙間ができる ★安全に直結

ToF の視野は `drivers/vl53l1x.c:105-135` が ST ULD 既定ブロックをそのまま書くだけで
ROI/timing budget を設定していないため **16×16 SPAD 全面 ≈ 27°**（半角 13.5°）。

距離 d でのビーム幅 = 2·d·tan(13.5°) ≈ **0.48·d**

| 距離 d                   | 1本のビーム幅    |
| ------------------------ | ---------------- |
| 250 mm（ハード停止距離） | **120 mm** |
| 500 mm                   | 240 mm           |
| 1000 mm                  | 480 mm           |
| 1500 mm                  | 720 mm           |

隣接センサの横間隔は実測で`S=90 mm`。左右ToFは中央ToFより94 mm前方にある。
中央センサから前方`Y` mmの障害物平面を考えると、既定FoV 27°（半角13.5°）の
中央・側方ビーム半幅はそれぞれ`Y·tan(13.5°)`、`(Y-94)·tan(13.5°)`となる。
隣接ビームが接する距離は次式から**約234.44 mm**となる。

```text
Y_touch = (90 / tan(13.5°) + 94) / 2 = 234.44 mm
```

`Y=250 mm`では、中央の半幅約60.02 mm、側方の半幅約37.45 mm、合計約97.47 mmとなり、
隣接間隔90 mmに対して**約7.47 mm重なる**。したがって、現測定値では
`CPU0_SENSOR_HARD_STOP_DISTANCE_MM=250`付近の中央〜左右間に連続した検出帯ができる。
一方、234 mmより近づくと理論上は隙間が開く。実効FoVの実測は省略し、現段階では
この名目27°を設計値として扱う。測距無効時は従来どおり有効フラグで除外する。

現時点で外側2台の角度変更やハード停止距離の変更は行わない。**ROIマルチゾーン（0-D-5）は
隙間を埋めない**ため、今回の計算結果と250 mm停止ゲートを現行設計の前提にする。

#### 0-D-3. 実測が必要な項目（手作業はここまで削減した）

3Dデータから取れたのは **車輪外径 108.00 mm** まで（`tire.stl`）。
STEP の組立は部品ローカル座標と組立座標が混在しており、**タイヤ自体が組立に含まれていない**ため、
トレッド幅・ホイールベースは CAD からは確実に取れなかった。

そこで**大半をローバー自身の自己計測へ置き換える**。ToFの光学中心座標は測定済み。
実効FoVは細かなターゲット移動を要求せず、ST ULD既定値の名目27°と実測座標から
接触距離・重なりを計算して設計値を確定する。実効FoVの実機確認は任意の検証へ下げる。

| # | 項目                           | 取得方法                                                                                                                | 手作業                                                       |
| - | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| 1 | ループ実周期（0-A）            | **ソフトのみ**。1 msループでカウンタを回し、`tk_get_otm()` の経過と10秒間比較してテレメトリへ出す               | **不要**（スコープ不要）                               |
| 2 | `*_MS` 定数7本の実効値       | 同上の手法で各々を計測してログ                                                                                          | **不要**                                               |
| 3 | counts/rev                     | **直線走行**。床にテープで 2000 mm を測り、走らせてカウント差を読む。`counts_per_rev = counts × 339.29 / 2000` | ★メジャー1回                                                |
| 4 | 最小走行デューティ（静止摩擦） | **ソフトのみ**。デューティを1‰ずつ上げ、エンコーダが動き出した値を左右別に記録                                   | **不要**                                               |
| 5 | 惰走距離                       | **ソフトのみ**。停止指令後もエンコーダを数え続け、止まるまでのカウント×mm/count                                  | **不要**（定規不要）                                   |
| 6 | 実効トレッド幅／旋回半径       | **円弧走行**。固定舵角で走り、ジャイロ積分ヨー Δθ と走行距離 L から R = L/Δθ。2舵角で実施                     | **不要**。しかも実スリップ込みで**定規より正確** |
| 7 | ToF 縦オフセット               | **完了**。左・右は中央より+94 mm                                                                                  | 済                                                           |
| 8 | ToF 横間隔 S                   | **完了**。中央−左、右−中央とも90 mm                                                                             | 済                                                           |
| 9 | ToF 実効視野角                 | **計算で確定（名目値27°）**。実測座標から接触距離と重なりを算出。実機確認は任意                                  | 済（実測任意）                                               |

#### 0-D-3a. ToF計測ログ（2026-09-13）

`/Users/hino/Desktop/sen_dis.csv` の計測ログを確認した。CSVは3台の距離、
`valid_flags`、3台の生Range Statusを各10000サンプル収録している。
`task_sensor` の50 ms周期を前提にすると、実データは先頭約12.8秒分である。

| 項目                                   | LEFT | CENTER | RIGHT |
| -------------------------------------- | ---: | -----: | ----: |
| 開始直後10サンプルの中央値 [mm]        | 1514 |   1562 |  1447 |
| 最短値 [mm]                            |  258 |    354 |   240 |
| 最短値のサンプル番号                   |  165 |    165 |   165 |
| 250 mm未満の回数（完全有効サンプル内） |    0 |      0 |     1 |

サンプル0〜254では3台の`range_status`がすべて`9`（VL53L1Xの有効値）で、
`valid_flags & 0x07 == 0x07`も成立した。サンプル255はRIGHTのStatusだけ0となり、
サンプル256以降は`valid_flags=0`・Status=0へ遷移している。後半の0 mmは実測距離0 mmではなく、
取得終了または無効状態として扱う。列ごとのLive Watch記録は同一時刻保証がないため、末尾の1サンプルは
スナップショットの完全な同期判定には使わない。

このログから、3台のI2C読出し・有効判定・距離変化は確認できる。一方、ターゲットの横位置、
ターゲット面までの既知距離、サンプル時刻が記録されていないため、これだけで**実効FoVの実測値**を
逆算することはできない。そこで0-D-9は、既定FoV約27°を名目設計値として採用し、0-D-2の
接触距離234.44 mmと250 mm時の重なり7.47 mmを根拠に計算確定とする。実機FoVの確認は後日の
任意検証とし、現段階で細かなターゲット移動は行わない。

> **6 が定規より良い理由**: プランナが必要としているのは物理的なトレッド幅ではなく
> **「この舵角で走ると実際どれくらいの半径で曲がるか」**である。円弧走行なら
> タイヤのスリップ・並列3輪の引きずり・重量配分が全部入った実効値が得られる。
> 物理採寸値を使って理論半径を計算するより実機に合う。

得られた値は新規 `rover_geometry.h` へ単位付きで定義する。

> `software/rover-monitor/index.html:424` の `WHEEL_RADIUS_MM = 35` は**誤り**。
> 正解は **54.0 mm**（直径108.00）。`TRACK_WIDTH_MM = 150` も項目6の実効値へ差し替える。

#### 0-D-4. 3Dデータから確定した寸法

| 部品                     | 寸法 (mm)                           | 用途                                                                    |
| ------------------------ | ----------------------------------- | ----------------------------------------------------------------------- |
| **`tire.stl`**   | **108.00 × 108.00 × 40.00** | ★**車輪外径 108.00 / トレッド面幅 40**。**円周 339.29 mm** |
| `rim.stl`              | 51.99 × 51.99 × 40.00             | リム径 52                                                               |
| `body.stl`             | 140.02 × 150.00 × 73.00           | シャーシ本体                                                            |
| `arm_left/right.stl`   | 67.50 × 305.19 × 90.10            | ロッカーアーム（B・C の ToF はこの先端に付く）                          |
| `pivot_left/right.stl` | 54.53 × 187.00 × 125.01           | ボギー側ピボット                                                        |
| `differential_bar.stl` | 183.08 × 17.99 × 3.00             | 差動バー                                                                |
| `motor_mount.stl`      | 54.60 × 27.02 × 88.35             | モーターマウント                                                        |

counts/rev が確定すれば mm/count が出る: 校正前900なら **0.3770**、校正後702なら **0.4833**。

#### 0-D-5. ROI マルチゾーン測距の評価と採用判断

VL53L1X は 16×16 SPAD アレイの一部を **ROI（4×4 以上）**として切り出し、
**ROI中心をアレイ上で移動できる**。これを時分割すれば1個のセンサから複数ゾーンの距離が取れる。
現ドライバ（`drivers/vl53l1x.c`、237行）は**ROI も timing budget も一切プログラムしていない**ので全て新規実装になる。

**まず正直に: ROI は 0-D-2 の「センサ間の隙間」を埋めない。**
ROI は視野を**狭める**方向にしか働かず、27° の外側は見えるようにならない。
隙間対策は 0-D-2 の候補1〜3で別途行う必要がある。

**それでも有用である。理由は「どこにあるか」が分かるようになるから。**

|               | 現状（16×16 全面）                                     | ROI 3ゾーン/台                         |
| ------------- | ------------------------------------------------------- | -------------------------------------- |
| 1台が返す情報 | **27° の錐体全体で距離1個**（最も強い/近い反射） | 錐体内を3分割した距離3個               |
| 分かること    | 「27°のどこかに障害物がある」                          | 「錐体の左寄り／中央／右寄りのどこか」 |
| 取れる行動    | **止まるしかない**                                | **避ける方向が決まる**           |
| 有効ビン数    | 3                                                       | **9**                            |

★これは「なめらかな障害物回避」に直結する。
現状は錐体内の位置が分からないので**減速・停止しか選べない**が、
ゾーン化すれば VFH-lite のヒストグラムが 3ビン→9ビンになり、**曲がる方向を根拠を持って選べる**。
Phase 3 の「周囲の環境が正しく分かる」にもそのまま効く。

**コストと制約**:

- ゾーンごとに個別の測距が必要。timing budget は実用上 **20〜33 ms/回**が下限
- 3台 × 3ゾーン = 9測距。逐次実行すると1巡 **180〜300 ms**（現在は3台で50 ms周期）
- **ROI を小さくすると SPAD 数が減り、最大測距距離が縮み S/N が悪化する**
- ROI 変更のたびにレジスタ再設定が要る（TCA9548A 経由の I2C トラフィックが増える）

**採用する設計: 2層構成（安全性を落とさない）**

```
安全層（レート維持）: 3台とも 16×16 全面で 50 ms 周期（20 Hz）
                      → 250 mm ハード停止 veto はこれまでどおりの応答性
地図層（低レート）  : 余った時間でオフセンターROIをラウンドロビンに1ゾーンずつ
                      → 9ゾーンの横方向プロファイルが約 300 ms で1巡
```

- **安全 veto は全面ROIのまま**なので、ゾーン化によって停止応答が遅くなることはない
- プランナと地図は「速い3ビン＋遅い9ビン」を併用する
- ゾーン値は必ず**取得時刻とROI番号を添えて**公開する（鮮度が違うものを混ぜない）

**判断: 採用する。ただし Phase 3 の後半**に置き、
安全層（現状の3台全面）が S1〜S3 で実証できてから着手する。
Phase 4 の古典プランナは**まず3ビンで成立させ**、9ビンは精度向上として後乗せする。
（プランナが9ビン前提だと、ROIが動かなかったときに回避そのものが作れなくなるため。）

### 0-E. デューティ・実速度・惰走距離の実測

`services/drive_service.c` / `config/drive_config.h` の現状:

- `drive_service_rpm_to_duty_permille`（`:26-41`）が `duty = rpm*1000/DRIVE_TARGET_RPM_MAX(300)` を計算し、
  **先に** `[DRIVE_DUTY_MIN_PERMILLE(0), DRIVE_DUTY_MAX_PERMILLE(700)]` へクランプ。
  その**後**で `drive_service_feedback_duty_permille`（`:52-63`）が左右倍率を掛けて再クランプ
- 左右倍率は **800/800（対称）**（`config/drive_config.h:24-25`）。左右非対称の問題は無い
- `120 RPM` → raw 400‰ → **左右とも 320‰**
- **`DRIVE_DUTY_MIN_PERMILLE (0)` ＝ 静止摩擦デッドバンド処理が無い** → 速度スケール下端は未計測
- クランプ順序のため **`DRIVE_DUTY_MAX_PERMILLE (700)` は到達不能**。raw は 210 RPM で 700 に張り付き、
  そこへ ×0.8 が掛かるので**実効最大は 560‰**
- `DRIVE_SPEED_FEEDBACK_ENABLE (0U)` ＝ 速度FBは無効（`KP`/`MAX_CORRECTION` 定数は未使用）

**進捗（2026-09-13）:** 既存の120 RPM・10秒前進データ（4区間）を、採用済みの
702 counts/revで再計算した。120 RPM指令は左右とも実出力デューティ320‰になる。

| 側    | 10秒カウント増分の平均 |  計算実RPM | 計算rpm_x10 | 10秒走行距離 |
| ----- | ---------------------: | ---------: | ----------: | -----------: |
| LEFT  |                11811.0 | 100.95 RPM |        1009 |    5708.5 mm |
| RIGHT |               12239.25 | 104.61 RPM |        1046 |    5915.5 mm |

同一デューティでRIGHTがLEFTより約3.6%速い。完全に同じ負荷・直進条件での補正候補は
DRIVE_RIGHT_DUTY_SCALE_PERMILLE ≈ 772（現状800）だが、4区間のカウントだけで設定変更はせず、
まず最小始動デューティと惰走距離を確定する。速度FBを有効化する判断もこの後に行う。

**追加実測報告（2026-09-13、更新）:** ユーザーが通常前進指令で走行した距離は
24 mではなく**約400 mm**だった。始動Dutyは約150‰からごく僅かに動き始め、200‰で
前進できた。したがって現時点では「始動境界 ≈150‰」「再現性を期待する実用始動値
≈200‰」として記録し、`DRIVE_DUTY_MIN_PERMILLE`への反映は複数回の再現確認後に行う。

前回の`/Users/hino/Desktop/sen_stop.csv`は時間／距離列を含まなかったため、今回の
`/Users/hino/Desktop/sen_stop2.csv`で`g_task_actuator_elapsed_total_ms`を追加確認した。
この時間値は18,169〜82,164 msの範囲で、約1秒間隔の65サンプル（観測窓は63,995 ms）
だった。ただし、当時240 mmと記録していた走行の開始・停止を示すマーカーはなく、累積カウントも
LEFT 179/521/781/783/656、RIGHT 192/550/823/824/695のように同一走行の開始値・終了値
として単調に対応していない。`rpm_x10`も前進側863/854、逆方向側-675/-743など散発値で、
定常速度の代表値には使わない。

よって`sen_stop2.csv`は時間列が追加されたが、**約400 mm走行そのものの経過時間Tはまだ
確定できない**。現在の1秒間隔では、80〜105 RPM程度で400 mmを走る想定時間0.7〜0.9秒
も十分には分解できない。702 counts/revと車輪外径108 mmから、400 mmに対応する期待カウントは
約827.6 count。実走行時間をT秒で開始・終了同時に記録できれば、平均速度は`0.400/T m/s`、
車輪回転数は`70.7355/T RPM`で算出する。距離は約400 mmとして扱う。

**最新実測（`/Users/hino/Desktop/Untitled.csv`、2026-09-13）:** e² studioの設定は10 msに
変更されたが、`g_task_actuator_elapsed_total_ms`の実サンプル間隔は24〜75 ms程度で、
中央値34 ms（主な範囲32〜45 ms）だった。デバッガ取得の実効間隔は設定値より長い。
今回の履歴では複数回のデューティ印加があり、最後にエンコーダカウントが実際に変化した区間は
約17.307〜18.807 sだった。出力中は約17.307〜18.303 s、出力0後の惰走は約0.504 sである。

この区間の累積カウント差はLEFT +833、RIGHT +877で、702 counts/rev換算では
約402.6 mm／423.9 mmとなる。ユーザーの実測約400 mmと同程度であり、前回の240 mmとの差は
計測ミスによるものだった。左右差は約5.3%あるため、単一の代表距離としてはまだ校正へ反映せず、
同一条件の複数回測定を続ける。約400 mmを物理距離として使う場合の平均値は、開始から惰走停止までの
1.500 sを採れば`0.267 m/s`、車輪回転数換算`47.16 RPM`となる。出力中の0.996 sだけを
走行時間と定義するなら`0.402 m/s`、`70.82 RPM`となるため、停止後の惰走を含めるかを
計測定義として固定する必要がある。なお、約21.071〜22.043 sの
最後のデューティ印加ではカウントもrpm_x10も変化しておらず、実走行区間とは判定しない。

**作業（すべてローバー自身で自己計測できる。定規もスコープも不要 — 0-D-3 参照）**:

1. **最小走行デューティ（静止摩擦）**: デューティを1‰ずつ上げ、
   **エンコーダが動き出した値**を左右別に記録する自動スイープを書く → 速度スケール下端
2. 速度スケール 0..1 の実効レンジを**実効最大 560‰ を上限として**決める
3. ★**惰走距離**: 320‰ 走行中に速度0指令を出し、**停止指令後もエンコーダを数え続けて**
   止まるまでのカウント × mm/count を距離とする。**定規不要**。
   これが制動距離見通しの最後の未知数（「制動距離の見通し」参照）
4. 左右差が残るなら、同一デューティでの左右カウント差から補正係数を算出して打ち消す

> いずれも**計測用コードとテレメトリ出力を書けば自動で取れる**。
> エージェントが担当すべきはこのコードであり、人間は走らせるだけでよい。

**一時計測ビルド（2026-09-13）:** ユーザー了承を受け、CPU1のLive Watchから0〜560‰の前進デューティを段階設定できる
`DRIVE_MEASUREMENT_TEST_ENABLE (1U)` を追加した。通常のIPC有効指令を受けるまで出力を許可せず、既存のtimeout・safe stopを
そのまま通す。mode 2への遷移時には停止指令直前の左右エンコーダカウントを一度だけ保存するため、惰走距離の開始点を
Live Watchで読み取れる。詳細なe² studio操作と計算式は
[CPU1_DRIVE_MEASUREMENT_20260913.md](docs/firmware/CPU1_DRIVE_MEASUREMENT_20260913.md)へ記録した。
これは通常版ではない。0-E終了後にマクロを`0U`へ戻してGenerate + Clean Buildし、通常版を書き戻すこと。
オフターゲット回帰はASan/UBSan付きで12件成功。CPU1の生成済みmakefileによるClean相当ビルドも成功し、
一時変数がELFへ入っていることを`nm`で確認した。macOS上のe² studioヘッドレスGenerate + Clean Buildは
本体がAbort trap 6で終了したため、実機前のGUI操作でGenerate Project Content → Clean/Buildを実施する。

### 0-F. `age_ms` 生存性の是正 ★安全に直結

**進捗（2026-09-12）: 実装・ホスト回帰10件・CPU0 Generate + Clean Buildと最終再ビルド成功。
実機ゲートも完了。** `task_sensor` 更新停止中に車輪が停止し、解除後に
`sensor_fresh` と `update_count` が復帰することをユーザーが確認した。
思考側でupdate_count進行と実時間を監視し、200 ms更新停止・時刻異常・取得失敗で通常停止。
音源追従／センサー走行の両モードへ適用。mutex取得は無待機化して監視の永久待機を避ける。
試験用`g_task_sensor_test_pause`で取得だけを止められる。移動時間の一時10000 ms設定は1000 msへ復帰。
[変更と実機試験](docs/firmware/SENSOR_LIVENESS_VALIDATION.md)。

`services/sensor_hub.c:335` が**成功ポーリング毎に `.age_ms = 0U` を代入**しており、
増加するのは `tasks/task_sensor.c:114-115,137-138`（センサI/O失敗時のみ到達する経路）だけ。
よって `tasks/task_think.c:70-85` と `control/obstacle_avoidance_controller.c:33` の
`age_ms <= CPU0_SENSOR_STALE_TIMEOUT_MS (200)` は**恒真**で、
**`task_sensor` がハングすると snapshot は永遠に「新鮮かつ有効」に見え、250 mm veto が凍結距離を読み続ける**。

**作業**: 生存性を `sensor_snapshot_t.update_count`（`services/sensor_hub.h:91-102` に既に存在）の
進行と、思考タスク側の実時間基準の比較で判定する。
**完了条件**: `task_sensor` を意図的に停止させ、車両が停止することを実機で確認。**達成（2026-09-12）。**

### 0-G. 併せて片付ける技術的負債

- fault 通知経路を `tasks/task_common.h` 側の共通イベント通知へ寄せ、**循環依存3件を解消**
- **異常ラッチの解除経路を作る**（不具合D）。`tasks/task_think.c:363/380/445/484` は
  `g_task_think_fault_flags` へ OR するだけで実行中は誰もクリアしない（`:107` の生成時初期化のみ）
- **`services/sensor_hub.h` から FSP 依存を剥がす**。`hal_data.h` を `fsp_err_t` のためだけに引いており、
  純粋な制御層まで FSP が波及している。`sensor_snapshot_t` と valid フラグを型専用ヘッダへ分離し、
  `fsp_err_t` を返すドライバAPIだけ `sensor_hub.h` へ残す（4-6-1）
- `firmware/ra8p1/common/mtk3_bsp2/mtkernel/` が入れ子submoduleで未展開。
  `git submodule update --init --recursive` で取得する。
  また submodule が `mtk3_bsp2/mtk3_bsp2-main/` と一段深く入っていると
  `.cproject` の `-I`（`mtk3_bsp2/config` 等）が解決しないので、配置を合わせる

---

## Phase 1: 開発環境の整備（実機不要・Phase 0 と並行可）

**完了（2026-09-11）**

- [X] `software/acoustic-trainer/` をuvプロジェクト化。Python 3.12、TensorFlow 2.20.0、
  NumPyを `uv.lock` で固定し、制御MLPの学習と完全int8変換を検証。
- [X] 公式TFLM `e4e81fbdc3dd165b5345fb9580ce064fe650a766` を
  `firmware/ra8p1/common/tflm/` へベンダリング。485ファイルのSHA-256とライセンスを保存。
  CMSIS-NNは追加せず、公式エクスポートのソースを整形・改名せず使用。
- [X] CPU0にC++17設定と `src/ai/tflm_runtime.h/.cc` のC APIを追加。
  96 KiB静的アリーナ、モデル／入出力検証、初期化・推論・情報取得・resetを実装。
- [X] CPU0 **Generate + Clean Build成功（0 errors）**。
  自動生成の無関係な差分を元に戻した後のクリーン再ビルドも成功。
  GNU size: text **135,112 B** / data **320 B** / bss **123,848 B**。
  ELF内の推論APIと96 KiBアリーナを確認。外部ヘッダとnewlib nosys由来の警告は残る。
- [X] ホストで実際のCラッパーとTFLMをビルドし、合成制御MLPの11入力で
  TFLite参照実装とのint8出力差 **0 LSB**。破損／切詰め／不整列／floatモデル、
  サイズ不一致、アリーナ不足、再初期化失敗と復帰の試験も成功。
  検証モデルは2,304 B、ホストのアリーナ実使用量は1,120 B（実機値ではない）。
- [X] ビルドスクリプトのClean-only完了誤認による途中終了を修正し、回帰3テスト成功。
  前提のcontrol-sim既存5テストも成功。

手順と再現方法: [acoustic-trainer/README.md](software/acoustic-trainer/README.md)、
[TFLMベンダ記録](firmware/ra8p1/common/tflm/README.md)。
**人間の実機確認: Phase 1では不要。** Phase 4の制御MLPの実機処理時間／アリーナ使用量は、
その実装時に検証する。
**次の項目: Phase 0-CのDoA極性校正（実機レビュー）。**

### 1-1. ビルド系

macOS 側は `.vscode/tasks.json` の `osx` オーバーライドと
`.vscode/scripts/Invoke-RaBuild.sh`（`E2studio.app` → `arm-none-eabi-gcc` → GNU make を探索して
headless で Generate + Clean Build を回す）で**既に整備済み**。本計画では確認作業を設けない。

> 1点だけ実装上の制約として記憶すること: **ソース追加時は `RA8P1: Generate + Clean Build` が必須**。
> FSP Solution と `.cproject` がファイル一覧を所有するため `Fast Build` では拾われない。
> Phase 2〜4 は新規ファイルを多数追加するので、毎回こちらを使う。

### 1-2. ホスト側 Python 環境（`uv`）

新規 `software/acoustic-trainer/`（`uv` プロジェクト）。**TFLM採用により TensorFlow が必要になった**。

| 依存           | 用途                                                              |
| -------------- | ----------------------------------------------------------------- |
| `numpy`      | 特徴量参照実装（FFTは`numpy.fft`）。RA8P1実装とのビット一致検証 |
| `tensorflow` | Phase 4の障害物回避MLPの学習・量子化（音響識別では不要） |

`uv` 0.10.4 は利用可能で、`software/rover-monitor` が既に `uv` 管理下にある。
Vela は不要（Ethos-U を範囲外としたため）。

### 1-3. RA8P1 への TFLM 導入

- `.cproject` に既に C++（g++）、hard-float FPU、`-O2`、function/data sections がある → **C++有効化は可能**
- TFLM は `firmware/ra8p1/common/tflm/` 配下の**ベンダツリー**として扱い、
  `docs/firmware/CODING_RULES.md` の型・リンケージ統一対象から**除外**する。
  アプリ側へは `LOCAL`/`EXPORT` に従った**薄いCラッパー1枚**だけを公開
- **CMSIS-NN は現在未同梱**（`ra/arm/CMSIS_6/CMSIS/` は Core のみ）。
  まず CMSIS-NN 無しで動かし、実測して足りなければ導入する（Helium があるので不要な見込み）
- テンソルアリーナは静的配列。SRAM 約1.83 MB に対し数十KBなので余裕

---

## Phase 2: 音響識別AIと現場学習

> **2026-09-16に現場学習の方式を変更した。** 2-3 と 2-6 が対象。
> 経緯・数値仕様・実装順は
> [`docs/firmware/edgeai/acoustic_learning_method.md`](docs/firmware/edgeai/acoustic_learning_method.md) を正とする。
> 特徴量仕様（2-2）とプロトコル（2-5）の固定契約は変更していない。

### 2-1. 設計の鍵 — 「PCMではなく特徴量をリングバッファする」

計画書の「突発音を検知するたび推論タスクが起床」は 96 B 制約と一致する。生PCMを常時流す必要はない。
さらに**log-melを常時計算して特徴量をリングバッファする**方がPCMを溜めるより1桁軽い
（32 B/フレーム × 80 = **2.6 KB**、PCM 800 ms なら 25.6 KB）。

```
ESP32S3                                        RA8P1 CPU0
  I2S 16ms読み出し（256サンプル）
      ↓  400サンプル窓 / 160サンプルホップ（読み境界をまたぐ繰越しバッファ）
  log-mel を常時計算（100 fps）
      ↓
  特徴量リングバッファ（800ms = 80フレーム = 約2.6 KB）
      ↓  level/VADトリガ発火
  トリガ前 300ms は既にバッファ内 → 後 500ms を追加取得
      ↓
  0x04 ACOUSTIC_FEATURE を「2フレーム計算する毎に1パケット」で送出
      ↓  USB CDC
                                    task_acoustic_link がバースト再組立（32×80 パッチ）
                                                ↓ tk_set_flg
                                    task_infer 起床 → 背景学習・能動要約・見本照合 → task_think
```

### 2-2. 特徴量の数値仕様（ホスト参照実装とビット一致を取る対象）

> **契約凍結済み（2026-09-11）**: この表の値は以降変更しない。

| 項目                             | 値                                                                         | 根拠                                                         |
| -------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------ |
| サンプリング                     | 16 kHz / mono（XVF3800処理済み2chのうち1ch）                               | 既存I2S設定                                                  |
| 窓長                             | 25 ms =**400 サンプル**                                              | 標準的なlog-mel設定                                          |
| FFT長                            | **512 点**（400を零詰め）→ 片側 257 bin、bin間隔 **31.25 Hz** | 400超の最小の2の冪                                           |
| ホップ                           | 10 ms = 160 サンプル →**100 fps**                                   | I2S読み(256)と割り切れない →**繰越しバッファ必須**    |
| melフィルタバンク                | **32 bin、下端 125 Hz から**                                         | 40binだと最低域6本前後が 31.25 Hz 間隔に対して縮退・重複する |
| 量子化                           | int8 →**32 B/フレーム**                                             |                                                              |
| 窓関数                           | Hann（ホストと同一式で生成）                                               | パリティ試験の対象                                           |
| イベント長                       | プリ 300 ms + ポスト 500 ms =**800 ms = 80 フレーム**                |                                                              |
| **フレーム毎レベル正規化** | **必須**（各フレームから対数エネルギー平均を減算＝CMVN相当）         | 下記                                                         |

> **レベル正規化を必須とする理由**: `docs/firmware/RESPEAKER_INTEGRATION.md` はレベルが
> マイク感度・XVF3800のAGC・設置・反響で変動すると明記しており、
> **さらにローバーは音源へ向かって走るのでイベント中にレベルが動く**。

**バースト量**: 1パケット = メタ 8 B + 32 bin × 2 フレーム = **72 B**（96以内）→ wire 88 B。
80 ÷ 2 = **40 パケット** = 3,520 B / イベント。**まとめてダンプせず 2フレーム毎に1パケット**（約20 ms間隔）。

> **まとめてダンプ禁止の理由**: `CONFIG_TINYUSB_CDC_TX_BUFSIZE=512` に対し
> `usb_link_send()` は `APP_USB_TX_TIMEOUT_MS`=20 ms で諦め（`app_config.h:29`、`usb_link.c:103-117`）、
> `frontend_send_frame()` は `s_sequence` を進めてからエラーを捨てる（`acoustic_frontend.c:59-67`）ため、
> **タイムアウトすると切り詰められたフレームが線上に残る**。

### 2-3. 推論と現場学習 — 背景AEのトリガ ＋ 能動フレーム選別の識別

**トリガと識別は別物として実装する。** 再構成誤差は「この環境で異常な音か」の候補生成だけに使う。
対象音の同定は、能動フレームの
96次元要約と、現場で登録した個別見本へのコサイン距離だけで行う。

| 役割 | 方式 | 禁止事項 |
| ---- | ---- | -------- |
| トリガ | 固定乱数エンコーダ（32→16）＋RLSデコーダ（16→32）の背景AE。MSEが閾値を超えたら候補 | MSEを対象音の識別スコアにしない |
| 識別 | 標準偏差が12 LSB超の能動フレームを選別し、`{mean,std,max}×32` の96次元int8を作る。5件の見本への最小cosine距離 | 見本を重心化しない。不十分な能動フレームを「非対象」と断定しない |

背景モデルは、入力int8を±64でクリップして[0,1]へ写し、hard sigmoid
`clip(0.2x + 0.5, 0, 1)`、忘却係数 `λ=0.95`、初期 `P=I/0.01` を用いる。
能動フレームが一つでもあるイベントでは、背景モデルを更新しない。固定乱数エンコーダは種から再生し、
デコーダはRLSで更新する。数式・固定小数点化方針は方式文書を正とする。

| ファイル | 責務 | 現在地 |
| -------- | ---- | ------ |
| `services/background_model.c/.h` | 背景AE、RLS更新、候補MSE、能動フレーム時の更新ゲート | 実装済み・control-sim回帰済み |
| `services/acoustic_identifier.c/.h` | 能動選別、96次元要約、個別見本へのcosine最小距離、leave-one-outしきい値 | 実装済み・control-sim回帰済み |
| `tasks/task_infer.c/.h` | 完成バーストのローカルコピー、上記サービス実行、`task_think`へ結果通知 | 実装済み・実機未確認 |

`task_infer` は優先度11・`task_registry`外・非致命起動のまま維持する。音響mutex下はコピーだけにし、
背景更新・要約・cosine計算は必ずロック外で行う。TFLMランタイム自体は削除せず、Phase 4-1bの
障害物回避MLPで使用する。

> 設計検討時の合成データの肯定的な数値は、実録音の精度として引用しない。実験コードが残っていないため、
> まず方式文書 §6-3 のホスト検証を再作成する。実機録音で確定するまで、しきい値の数値主張もしない。

### 2-4. バースト送受信で守るべき規律（実読で判明した落とし穴）

| # | 制約                                                                                                                                                                                                                                                                                                                                          | 根拠                                                                    |
| - | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 1 | **上り送信はすべて単一タスクから行う**。`s_sequence++`（`acoustic_frontend.c:65`）は非アトミックで、複数タスクから送ると順序が乱れる。`task_acoustic_link_sequence_accept`（`:556-570`）は非増加シーケンスを**黙って捨てる**                                                                                              | 取りこぼしが`g_task_acoustic_link_sequence_drop_count` にしか現れない |
| 2 | **イベント内の欠落検出は 0x04 の `frame_index` で行う**。シーケンス番号の飛びは無害（等値・逆行のみ拒否）なので送信失敗で番号を焼いても構わない                                                                                                                                                                                       | 同上                                                                    |
| 3 | **音響mutexを保持したまま推論しない**。`task_acoustic_link` は1 msポーリングのBulk IN完了毎に取得し（`:35-36,683`）`TA_INHERIT` なので、保持中は `task_infer` が優先度8へ昇格して**`task_acoustic_link` を阻害する**。mutex下でコピーし、計算は外で                                                                           |                                                                         |
| 4 | **下りに新メッセージ用の位相が無い**。`task_acoustic_link_telemetry_start`（`:421-434`）は単一 `audio_tx_buffer`（`:87`）と真偽値1個による2相交替で、`if (audio_write_pending) return` により**設定書込みが無期限に飢餓させられる**。**明示的な位相enum＋テレメトリより優先する config-pending フラグ**を追加する | 無いとNVS保存が無保証。テレメトリ20 Hz化でさらに厳しくなる              |
| 5 | パーサ自体はバーストに耐える（単一フレーム状態、FRAME_READY で`used`/`expected` リセット、`acoustic_protocol.c:398-399`）。上り吸い出しも 512 B/poll で足りる                                                                                                                                                                           | 問題なし                                                                |

### 2-5. 新規プロトコルメッセージ

> **契約凍結済み（2026-09-11）**: この表の型番・方向・ペイロード構造は以降変更しない。

`firmware/common/acoustic_protocol.h/.c` へ**対称に**追加（両端が同一ソースをコンパイル）。
ヘッダ14 B + ペイロード ≤ **96 B** + CRC-16/CCITT-FALSE 2 B。**分割・継続フラグは存在しない**
（encoder `:102-103` / parser `:365-368` が対称に96超を拒否）。

| type     | 名称                               | 方向          | ペイロード                                                                                                                |
| -------- | ---------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `0x04` | `ACOUSTIC_FEATURE`               | ESP32 → CPU0 | 8 B メタ（event_id u16 / frame_index u16 / frame_count u16 / n_bins u8 / flags u8）＋ mel int8 × 32 × 2 =**72 B** |
| `0x05` | `PROTOTYPE_DATA`                 | 双方向        | **予約・不使用**。背景モデルと見本はCPU0 Code MRAM内で完結する                                                       |
| `0x10` | `SET_CONFIG`（**空き枠**） | CPU0 → ESP32 | モード指令／プロトタイプ保存要求                                                                                          |
| `0x11` | `ACK`（**空き枠**）        | ESP32 → CPU0 | 保存結果／設定反映結果                                                                                                    |
| `0x22` | `POSE_TELEMETRY`                 | CPU0 → ESP32 | 姿勢・環境認識用（Phase 3）。`0x20` は **96 B満杯・予備1 B のみ**なので新設                                       |

`n_bins`/`frames` をメタに入れるので、後から粒度を変えても wire 互換を壊さない。
見本と背景モデルはCPU0内で完結するため、音響学習用の追加ペイロードを送らない。

### 2-6. 学習結果の永続化 — RA8P1 Code MRAM の予約領域へ保存

RA8P1に搭載されている1 MiBの不揮発性Code MRAMのうち、CPU0割当末尾**32 KiB**（`0x02078000`〜`0x0207FFFF`）をリンカで予約し、CPU0単体で学習結果を永続化する。

> 背景デコーダと5件の個別見本を保存するため4 KiBから拡張する。既存のA/Bスロット・CRC-32・世代・読戻し検証は維持し、
> 旧4 KiB領域と旧ペイロードは新レイアウトとして読まない。

| 保存対象 | 容量 |
| -------- | ---- |
| 背景AEのデコーダ `W`（32×16 float32） | 2 KiB |
| 見本5件（96次元int8） | 480 B |
| しきい値・世代・CRC-32・乱数種 | 数十B |
| A/Bスロット、将来の見本数・隠れ層の余白 | 残り |

相関逆行列 `P` は推論に不要なので保存しない。固定乱数エンコーダも種から再生成するため保存しない。

| 方式                               | 判定                                                                                                                                                                                                                                                |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **RA8P1 Code MRAM 自己書換** | **採用**。データFlash領域は存在しないため、リンカスクリプトでCPU0領域末尾をデータ領域として切り出し、FSP `r_mram` APIで保存する。32 byte単位、A/Bスロット、CRC-32、世代番号、読戻し検証を使用し、書込み中はCPU0割込みとCPU1実行を停止する。 |
| ESP32 NVS                          | MRAMへの自己書換によるFSP設定工数回避の目的で一時候補となったが、システム的理想形を追求するため不採用。                                                                                                                                             |
| OSPI Flash                         | P801/P803/P808をサーボへ転用中でOSPI使用不可。不採用                                                                                                                                                                                                |
| RAMのみ                            | 電源断で消える。「現場で記憶させる」要件を満たさない。不採用                                                                                                                                                                                        |

### 2-7. 学習モードの起動と操作

ローバー本体にUIが無い。**EK-RA8P1のユーザーボタン＋LED**で完結（`r_ioport` は同梱済み）。

1. ボタン長押しで学習モードへ（LED点滅パターンで表示）
2. 対象音を N 回（既定5回）鳴らす。1回取り込む毎にLEDで確認応答
3. N回から能動フレームの96次元要約を個別見本として保存し、leave-one-out距離の平均+3σでしきい値を決定
4. MRAMへ保存し、完了表示（2-4 の位相修正が前提）
5. 走行モードへ復帰

> 遠隔起動も後段で追加可能だが、`ARCHITECTURE.md` の「UDPは外部診断専用」
> 「CPU0を権限・安全・timeoutの境界として維持」に従い、
> **UIからは「モード要求」を送るだけで可否判断はCPU0が持つ**構成にする。

### 2-8. ホスト側検証と実録音のしきい値決定 `software/acoustic-trainer/`

1. 方式文書 §6-3 を再現する**決定論的ホスト参照実装・回帰試験**を作る。固定乱数、RLS、更新ゲート、能動選別、96次元要約、cosine、leave-one-outしきい値をCPU0実装の仕様にする。
2. 実機録音を、背景・対象音・混同しやすい非対象音・距離/音量を変えた対象音として収集する。合成データは数式検証に限定する。
3. 実録音で「能動フレーム不足は不定」「対象/非対象を非同一」として扱い、しきい値と誤判定を記録する。最終しきい値はこの測定でのみ確定する。
4. 取得手順・録音条件・しきい値・結果を `docs/firmware/validation/` に日付付きで残す。CNN学習・tflite変換・C配列化は行わない。

---

## Phase 3: 環境認識と可視化（rover-monitor の発展）

### 3-1. 目標を「精度」から「正しく分かること」へ置き換える

センチメートル精度グリッドは**要件から外した**（ユーザー判断）。理由は技術的にも整合する:

1. **6輪に対しエンコーダ2個**（中央輪）。4輪操舵の旋回でスリップが乗る
2. 状態フレームは**約187 ms周期（約5.3 Hz）**で、エンコーダ語は commit 時点で既に約66 ms古い（3-3）
3. ジャイロは20 Hzポーリング、バイアス校正なし、しかも `gyro_dps_x10` は **0.1 dps LSB** で
   センサ本来の 0.0153 dps より **6.5倍粗い**（`services/sensor_hub.h`）
4. 絶対基準が無い（GPS非採用は計画書の方針、ランドマークも無し）

**代わりに主張すること**: 「ローバーが自分の周囲を正しく把握できている」。
これは**累積誤差に依存しない表現**で成立する。

### 3-2. rover-monitor に3層の表示を作る

`software/rover-monitor/`（`main.py` 148行 / `index.html` 732行）を発展させる。

| 層                                      | 内容                                                                                                                                                                                                 | 累積誤差の影響                 |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| **① ローバー中心ビュー（主役）** | 車体を中心に固定し、**ToF 3台の実オフセットから前方ビーム**を描き、測距値を点/弧で表示。**近距離のビーム間隙間も明示**（0-D-2）。ROI導入後は9ゾーンへ拡張。`valid_flags` と age も出す | **受けない。常に正しい** |
| **② 累積相対マップ（副）**       | ヨー（ジャイロ積分）＋距離（中央輪エンコーダ、0.4833 mm/count）で ToF ヒットを 100 mm セルの占有グリッドへ置く。走行経路も描く。**「相対マップ・累積誤差あり」と明示ラベル**                   | 受ける。だから副役             |
| **③ 音響イベント重畳**           | `task_infer` がターゲット音と判定したら、その時点の姿勢からDoA方位のレイを、クラス名と確信度付きで描く                                                                                             | ①に重ねれば誤差の影響なし     |

★**①を主役にするのが設計の核**。「障害物がどこにあるか」はローバー中心座標で十分に答えられ、
cm精度の自己位置を必要としない。②は「走行経路が分かる」要件のために置き、正直にラベルする。

> `index.html:424` の `WHEEL_RADIUS_MM = 35` は**誤り**。正解は **54.0 mm**（直径108.00）。
> `TRACK_WIDTH_MM = 150` も 0-D の実測値へ差し替える。

### 3-3. 状態送信レートを直す（語を増やす前に）

`tasks/task_status.c:94-107` は**1反復あたり最大1語**しか送らず、`tk_dly_tsk(10)` は 11 ms かかる:

- `telemetry_elapsed_ms` の加算は `telemetry_word_index >= WORD_COUNT` で**ガードされている**（`:85-87`）ため、
  送信中（`word_index < 7`）は経過カウンタが**凍結**する。つまり2相は厳密に直列
- arm: 10反復 ≈ 110 ms → send: 7反復 ≈ 77 ms ⇒ **約187 ms/フレーム ≈ 5.3 Hz**
- snapshot はフレーム先頭で1回取る（`:94-95`）ので**エンコーダ語は commit 時点で約66 ms古い**
- `docs/firmware/diagnostics/2026-09-07-sound-test-analysis.md` の「状態age 60–242 ms」と独立に整合

**作業（語追加より先）**: `FSP_ERR_OVERFLOW` をバックプレッシャとして使い
**1反復でN語まで送る**ループへ変更する。**4段FIFOは律速ではなく、1語/11msのループが律速**。

### 3-4. 生カウントは「累積値」＋「時刻語」を送る

> **契約凍結済み（2026-09-11）**: 状態語 `0x88` / `0x89` / `0x8A` と状態語数 7 → 10 は以降変更しない。

デルタ送信は CPU1 に新規状態が必要で、かつ**損失時に不整合**になる
（`:99-101` は失敗語を次反復で再送するが**ステージ済みフレームを再スナップしない**ため、
フレーム落ち／再送でデルタは二重計上または恒久的な変位喪失を起こす）。

**採用**: `drivers/encoder.c` が既に持つ `W` 累積カウンタ（`g_encoder_left_count` /
`g_encoder_right_count`）を `count & 0xFFFFFF` で送り、**CPU0側で符号拡張つき剰余差分**を取る。
約1800 counts/s なら 220 ms あたり約396 counts ≪ 2²³ で一意。**フレーム落ちにも遅延にも強い**。

**時刻語が必須**: カウント差分は**時間差分なしには使えない**（187–220 ms でジッタあり）。
**CPU1の単調増加ミリ秒（24 bit、4.66 hでラップ）を第3の語として送り commitマーカとする**。
これが無いと想定周期で除算することになり、0-A と同じ種類のバグを作り込む。

**IPC変更点**（`firmware/ra8p1/common/ipc_message.h`）:

- 状態IDは `0x81`〜`0x87`（`:55-69`）。**`0x88`/`0x89`/`0x8A` は未使用**
- 語数は `ACTUATOR_IPC_STATUS_WORD_COUNT (7U)`（`:49`）＋ `ipc/actuator_ipc_server.c:83-107` の
  `switch` ＋ `tasks/task_status.c:74,83,92,99,102` — **機械的に追える**。**7 → 10語**へ
- CPU0クライアントの既定は無害な `break`（`ipc/actuator_ipc_client.c:174-175`）なので**前方互換は安全**。
  CPU1 server の `default:` は `g_rx_fault_pending = TRUE`（`ipc/actuator_ipc_server.c:172-174`）で
  `ACTUATOR_FAULT_IPC_RX` のソフトfaultのみ（安全停止は起こさない）

### 3-5. オドメトリの実装場所

**新規** `CPU0/src/services/odometry.c/.h`（L2 services）— 純関数 + コンテキスト構造体。

- **新規タスクは作らない**。`task_sensor`（prio 9 / 50 ms）がセンサ状態を所有するのでその周期で更新
- **中央輪がエンコーダ輪**なので、**距離はそのまま車体中心線の走行距離**として使える
  （非操舵輪なので操舵角補正が不要。4隅輪に付いている場合より条件が良い）
- **ジャイロは生値を `odometry.c` 内部で積分する**。`gyro_dps_x10` 経由にすると
  6.5倍の分解能を界面で捨て、残留バイアス1 LSB で量子化だけから100秒あたり約10°ドリフトする
- ヨーは**ジャイロ積分と車輪差動の相補フィルタ**、距離は車輪のみ
- 静止時ジャイロバイアス校正（起動時＋停止毎に自動）
- IMUのみ BMI270 FIFO 経由で100 Hz取得（現状はODR 100 Hz設定に対しポーリング20 Hz）

> ★**pose の有効性ビットを `sensor_snapshot_t.valid_flags` に追加してはならない**（不具合C）。
> `control/obstacle_avoidance_controller.c:33` は `CPU0_SENSOR_VALID_ALL == valid_flags` の
> **完全一致**比較なので、ビットを1本足した瞬間にこのゲートが恒久 false になる。
> pose の有効性は**独立した構造体**で公開する。あるいは Phase 4 の S1 でマスク化した後に触る。

### 3-6. 学習制御に最適なテレメトリ周期（考察）

**結論: 収集時は 10 Hz から始め、20 Hz を上限とする。通常走行は 4 Hz 据え置き。**

根拠:

| 制約           | 値                                                          | 意味                                                                               |
| -------------- | ----------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| ToF 更新       | `task_sensor` 50 ms ＝ **20 Hz**                    | **情報量の天井**。これより速く記録しても新しい観測は無い                     |
| 制御判断の生成 | `task_think` 100 ms ＝ **10 Hz**（S5後 20 Hz）      | 学習対象は「観測→判断」の写像。**判断より速く記録すると同じ判断が重複する** |
| 現在のCDC側    | `CPU0_AUDIO_TELEMETRY_PERIOD_MS (250U)` ＝ **4 Hz** | ★**律速はここ**                                                             |
| 現在のWi-Fi側  | `APP_UDP_TELEMETRY_PERIOD_MS (250U)` ＝ 4 Hz              | CDCと一致                                                                          |

★**重要**: 上り（CPU0→ESP32）のCDCテレメトリが 4 Hz なので、
**ESP32側の `APP_UDP_TELEMETRY_PERIOD_MS` だけ下げても同じデータの再送になる。**
`CPU0_AUDIO_TELEMETRY_PERIOD_MS` と**両方**を同時に下げる必要がある。

帯域の確認（いずれも余裕）:

- CDC: 20 Hz なら (96 + 24) B × 20 = **2.4 kB/s**（RA8P1側RX構造上限は約 512 kB/s）
- Wi-Fi: JSON 最大 1400 B × 20 Hz = **28 kB/s**

**採用する設定**:

| モード                        | CDC / UDP 周期            | 用途                                               |
| ----------------------------- | ------------------------- | -------------------------------------------------- |
| 通常走行                      | 250 ms（4 Hz）            | USB帯域を音響バースト（40パケット/イベント）に譲る |
| 学習データ収集（第1段）       | **100 ms（10 Hz）** | 現行`task_think` 100 ms と1:1。重複が出ない      |
| 学習データ収集（第2段・S5後） | **50 ms（20 Hz）**  | ToF天井と一致。これ以上は上げない                  |

⚠ **干渉に注意**: 音響バースト（40パケット連続、約40 ms占有）と高レート テレメトリは
**同じCDCリンクと 2-4 の2相・単一バッファ構造を共有する**。
20 Hz 化は 2-4 の位相enum修正を**前提**とする。

- 20 Hz を超えない理由: ToF が 20 Hz なので新規情報が増えず、CDCの競合リスクだけが上がる
- 収集時は `sensor_snapshot_t.update_count` を必ずログに含め、**重複サンプルを後処理で除去できる**ようにする

---

## Phase 4: なめらかな障害物回避と μT-Kernel タスク分離の実証

### 4-0. ★センサ回避のデッドロック是正（2026-09-16・実施済み）

> **A-2 の申し出**: これは**安全経路（停止条件）に触れる変更**である。ユーザーからの明示の依頼
> （「あまり障害物が多いとデッドロックしちゃう」）に基づき実施した。変更は**閾値1つ**に限定し、
> 新しい運動（ブラインド後退やその場旋回）は**追加していない**。
> **実機ゲートは未実施**。下の「実機で確認すること」を人間が通すまで本番デモに使わない。

#### 4-0-1. 症状と再現（control-simで機械再現済み）

`AVOIDANCE_ESCAPE_BLOCKED` は `left_rpm = right_rpm = 0` / `actuator_enable = FALSE` を出す
**完全停止**状態である。ここから抜ける条件は、是正前は

```
CENTER ≥ CPU0_SENSOR_CLEAR_DISTANCE_MM(1000) かつ 左右 ≥ 500 が 1000 ms 継続
```

だった。**停止している車両は自分でこの距離を作れない。** 2つの経路で恒久停止する。

| # | 経路 | 再現（実測） |
| - | ---- | ------------ |
| 1 | **1回のかすり**。3眼のどれかが150 mm以下を1回でも読むと `obstacle_avoidance_block()` が即時発火し、再試行回数に関係なくBLOCKEDへ落ちる | `L150/C900/R600` を1回入れた直後、`L700/C950/R700`（**明らかに走行可能**）へ12秒置いても復帰しない。CENTERが**50 mm足りない**だけで永久停止 |
| 2 | **再試行回数が返ってこない**。`attempts`（上限3）の返却も同じ1000 mm判定に紐づく | 通路の最大値が980 mmの部屋では、脱出が**毎回成功していても**4回目の接近でBLOCKEDへ落ち、以後復帰しない |

前向き3眼は±90 mmの平行ビームなので、斜め進入では**車体に余裕があっても**片眼だけが
150 mmを踏む。障害物が増えるほど経路1の発火確率が上がる。これが
「障害物が多いとデッドロックする」の機序である。

#### 4-0-2. 是正 — 「巡航できる広さ」と「走り出せる広さ」を分ける

根本原因は**1つの閾値が2つの意味を兼ねていた**ことにある。復帰判定に巡航幅を要求していた。

```c
/* control_config.h */
#define CPU0_SENSOR_RECOVER_CLEAR_MM       (650U)   /* 旧 CPU0_SENSOR_CLEAR_DISTANCE_MM (1000U) */
```

- `650` の根拠: `CPU0_SENSOR_ESCAPE_FRONT_MM (550)` より **100 mm 高い**ので、
  復帰直後に脱出が再発火しない。`CPU0_SENSOR_PIVOT_DISTANCE_MM (500)` より上なので後退にも落ちない。
  復帰時の指令は約108 RPM（≈565 mm/s）で、50 msの測距周期＝約28 mmの前進、惰走約11 mm（0-E）。
  `CPU0_SENSOR_CRITICAL_STOP_DISTANCE_MM (150)` まで**500 mm の余裕**が残る
- 左右の `CPU0_SENSOR_SIDE_DISTANCE_MM (500)` は**変更していない**（元から拘束していなかった）
- **`emergency_stop` は一切使っていない**（4-4-3 の規律を維持）
- **150 mm極近接の即時停止は1 stepも変えていない**。変わったのは**ラッチが続く長さだけ**

#### 4-0-3. 検証（`software/control-sim/tests/test_deadlock_recovery.py`・7件）

| 試験 | 内容 |
| ---- | ---- |
| `..._does_not_latch_the_vehicle_forever` | かすり直後の停止は**即時・無動作**であること、その後 `700/950/700` で復帰すること、**1秒未満では復帰しないこと** |
| `..._budget_is_refunded_in_a_room_that_never_opens...` | 980 mm止まりの部屋で6回連続の接近・脱出が全て走行継続で終わること |
| `..._still_requires_measured_clearance`（3件） | CENTER 400 / 500 / 600 mm を10秒与えても停止を保つこと |
| `..._gate_is_exact`（3件） | `650/500` で復帰、`649/500` と `650/499` では復帰しないこと |
| `..._does_not_shorten_the_next_escape_settling_time` | 復帰直後に再接近しても**400 msの整定**が短縮されないこと（後述の時計再初期化の確認） |

**実行結果: `uv run pytest -q` → 105 passed / 1 skipped**（skipは下記の欠落fixture）。
閾値を1000へ戻すと上の2件が落ちることを確認済み＝**空回りしない回帰**である。

> ★**CPU0のArm向けビルドは未実行**（macOSのe² studio環境が必要）。
> control-simがコンパイルするのは `obstacle_avoidance_controller.c` / `control_config.h` /
> `task_think.c` / `task_command.c` / `acoustic_identifier.c` / `acoustic_feature_assembler.c` だけである。
> 削除・改名したマクロ（`CPU0_SENSOR_CLEAR_DISTANCE_MM` / `CPU0_SENSOR_CAUTION_DISTANCE_MM`）の
> 残参照が `firmware/` `software/` `docs/` に**1件も無い**ことはgrepで確認済み（`.cc`/`.cpp`/`.hpp` を含む）。
> それでも **`bash .vscode/scripts/Invoke-RaBuild.sh --target CPU0` をユーザーが通すまで
> ビルド成功を主張しない。**

既存試験で唯一更新したのは `test_repeated_reapproach_is_bounded_and_rearms_only_after_stable_clearance` の
証拠通路 `(600, 800, 600)` → `(600, 500, 600)`。**主張（「安定した実測クリアランスなしにBLOCKEDを抜けない」）は
そのままで、閾値の証拠だけを新しい基準へ移した**。契約の緩和ではない。

#### 4-0-4. 付随して確認した点（是正不要と判断）

`obstacle_avoidance_controller_step()` の BLOCKED 分岐にある `obstacle_avoidance_controller_init()` は、
同じstepで設定済みの `clock_valid` / `last_ms` を消す。復帰直後の1 stepは `elapsed_ms = 0` になる。
判定を**遅らせる方向**（保守側）にしか働かず、復帰1回につき1回しか起きない。
上の整定試験で**400 msが維持される**ことを確認したため是正しない。

#### 4-0-5. ★実機で確認すること（人間の担当・B-4）

1. 片眼だけが150 mmを踏む**斜め進入**を意図的に作り、停止後に車体を動かさずに
   前方を空ける（人が退く）と**約1秒で走り出す**こと。走り出しが暴走でないこと
2. 復帰直後の前進が `CPU0_SENSOR_CRITICAL_STOP_DISTANCE_MM (150)` を侵さないこと
3. 障害物を5〜6個置いた実環境で**60秒間走らせ、一度も恒久停止しない**こと（付録C-3のDEMO計測と同じ指標で記録）
4. 650 mmが実環境で**低すぎない**こと。接触が出るなら700〜750へ上げる（`ESCAPE_FRONT_MM` 550より上を維持）
5. ★**`BACKUP` → `BLOCKED` → 復帰 → `BACKUP` の循環が起きないこと。**
   `CPU0_SENSOR_ESCAPE_MAX_ATTEMPTS (3)` は「無限ループ防止」だけでなく、
   **後方センサーが無いまま後退する回数の上限**でもあった。
   `BACKUP_RPM (-100)`（約565 mm/s）× `BACKUP_MAX_MS (2500)` で
   **1回あたり最大約1.4 mの無計測後退**になる。
   是正前は真の行き止まりで恒久停止して終わったが、是正後は
   「3回後退 → BLOCKED → 後退したので前方に650 mmある → 1秒で復帰 → 再接近」
   という**周期15〜25秒の循環**が原理的に起こり得る。
   **60秒あたりの後退回数と後退総時間を記録し、恒久停止しないことと同じ重みで見る。**
   循環が出た場合の対策は 650 mm を 1000 mm へ戻すことではなく、
   **返却の頻度を縛ること**（継続時間を延ばす／復帰回数を数える）である

---

### 4-1. 方針 — 古典プランナを先に作り、それを教師にして学習モデルへ

学習モデルには**教師データの出所**が必要で、そこが実務上の律速。二段構えが最も確実。

**4-1-a. 曲率制限付き滑らか操舵（古典・約300 STEP）**

- 3つのToFから斥力、目標方位から引力を作る VFH-lite / ポテンシャル法。
  **3台は前方向きの平行ビームで原点が異なるので、各レイを 0-D-3 の実オフセットで車体座標へ写してから使う**
- ★**まず3ビンで成立させる。** 0-D-5 の ROI 9ゾーン化は精度向上として後乗せする
  （9ビン前提で作ると、ROIが動かなかったとき回避そのものが作れなくなる）
- **近距離のビーム間隙間（0-D-2）を設計前提に入れる**。
  「3本のどれにも映らない＝安全」ではない
- 出力は「操舵角」「速度スケール」の連続値。**レート制限**で滑らかさを構造的に保証
  （現行 if/else 梯子が滑らかでない根本原因は、離散的な切替とレート制限の不在）
- これが**永続的な安全フォールバック**を兼ねる
- **カーネル半幅は 0-D-3 項目9で採用したToF名目視野角（27°）に基づいて決める**。
  実効FoVの実測値が得られた場合は後で差し替える

**4-1-b. 小規模MLPへの方策蒸留（学習・約500 STEP）**

- 4-1-a の出力と実走ログを教師に模倣学習。**TFLMが既に載っているので実行基盤は共用できる**
- 構成: 入力 10 → 隠れ16 → 出力2。出力の範囲変換は推論外のC関数で行う。int8量子化で**重み約200 B**。M85 では数μs
- 教師データは既存のUDP JSONログで足りる（`tof_mm[3]`, `accel_mg[3]`, `gyro_dps_x10[3]`,
  `*_encoder_rpm_x10`, `think.steering_deg` が流れている）。
  `software/rover-monitor/main.py` の `FORWARD_DESTINATIONS`（コメントアウト済み）を有効化すれば分岐できる
- 収集周期は 3-6 のとおり **10 Hz →（S5後）20 Hz**

### 4-2. 入力ベクトルと `valid_flags` の扱い

ToF無効時は **0 mm** が published される（`services/sensor_hub.c:334-338,361-380` は無効時に
距離を書かず、snapshot 先頭の複合リテラル初期化で 0 のまま）。
そのまま入力するとモデルは「接触寸前」と誤読して急停止を学習する。

```
入力[0..2] : ToF L/C/R を 0..1 正規化（4000 mm でクリップ）
             無効時は 1.0（=遠方）を代入し、対応する有効フラグを 0 にする。**0 mm を入れない**
入力[3..5] : 各ToFの有効フラグ（0/1）
入力[6..7] : 目標方位の sin, cos（音源追従からの入力）
入力[8]    : 現在の速度スケール（0..1）
入力[9]    : ヨーレート（正規化）
出力[0]    : 操舵角（-1..1 → ±CPU0_SOUND_STEERING_MAX_DEG(45)）
出力[1]    : 速度スケール（0..1）
```

### 4-3. 駆動系の実像に合わせる

`services/drive_service.c` / `drivers/bts7960.c` から、**連続値指令が想定通り効かない3点**:

1. **ランプは自分で制御できない直列レート制限**。`drive_service_ramp_value`（`:90-100`）は
   `drive_service_update_1ms()` の呼び出し毎＝実約2 msに1回なので `DRIVE_RAMP_PER_MS (2)` は
   実効**約1‰/ms**。さらにPWM反映は `DRIVE_UPDATE_PERIOD_MS (5)` で間引かれ（`:182-187`）
   **実約10 ms粒度**。プランナのΔv制限とこのランプは**縦列2段のスルーリミッタ**で、
   S5（50 ms周期）では車輪応答はランプ側が支配する
   → **「車輪で測った滑らかさ」はプランナを測っていない**
2. **速度ゼロはランプではなく即断**。`drive_service_set_target_rpm`（`:140-147`）は左右とも0で
   `drive_service_stop()` へ早期復帰し、`bts7960_stop()`（`drivers/bts7960.c:36-58`）が
   enableピンをLOWにし4PWMを0にしてタイマ停止。
   **連続速度則がちょうど0を通ると衝撃的な不連続＋再ランプ＋再イネーブルになる**
   → **停止を意図する時以外、速度スケール出力を0から離してクランプする**
   （※この即断特性は**制動距離には有利**に働く。0-E で惰走を実測する）
3. **速度スケールの基準値が未実走**（0-E）。下端は静止摩擦で死んでおり（`DRIVE_DUTY_MIN_PERMILLE (0)`）、
   上端はクランプ順序のため実効 560‰ で頭打ち

### 4-4. 安全エンベロープ — AIの出力を信用しない構造

多層防御。**AIは提案し、決定は既存の安全機構が持つ**。

1. **CPU0内**: モデル出力を `steering ≤ ±45°`、`speed ≤ 上限` でクランプし、レート制限を掛ける
2. **CPU0内**: `CPU0_SENSOR_HARD_STOP_DISTANCE_MM (250)` のハード停止veto は
   **モデルの外側**に置き、モデル出力に関わらず優先する
3. **veto に `emergency_stop` を使わない**。`emergency_stop` は CPU1 側の**ラッチ**で、
   解除に `emergency_stop==0` かつ `actuator_enable==0` の指令往復が必要
   （`services/actuator_service.c:86-101`、`tasks/task_command.c:250,279-283`）。
   veto 毎に**2指令周期（約120 ms以上）の解除往復**が発生する。
   通常の障害物 veto は**速度0指令**で表現し、`emergency_stop` は真の異常時のみ
4. **既存2コントローラの `emergency_stop` 方針が食い違っている**ことに注意:
   回避は全経路（停止時も）で `FALSE`（`control/obstacle_avoidance_controller.c:68-76`）、
   音源追従は WAIT_LINK/FAULT で `TRUE`（`control/sound_follow_controller.c:253-255`）。
   **S2/S3 は両方を走らせる**
5. **IMU依存の是正**（不具合C）:
   - 既定の音源追従経路 `task_think_sound_motion_allowed()`（`tasks/task_think.c:70-85`）は
     3つのToFのみを**マスクで**要求し、IMUを要求していない。**これが正しい設計**
   - 回避側 `obstacle_avoidance_snapshot_usable()`（`control/obstacle_avoidance_controller.c:31-34`）は
     `CPU0_SENSOR_VALID_ALL == valid_flags` の**完全一致**で、`VALID_ALL` にはIMUが含まれる
     （`services/sensor_hub.h:35-36`）→ `:118-121` で `CPU0_SENSOR_RULE_SAFE_STOP`
   - **是正**: 回避側を既定経路と同じ**マスク意味論・ToFのみ要求**へ揃える。
     IMUは転倒・衝撃検知専用の**独立veto**へ分離
6. **異常ラッチの解除経路が必要**（不具合D／0-G）。`tasks/task_command.c:220-242` は
   `CPU0_COMMAND_TARGET_TIMEOUT_MS (500)` 内に目標更新が無いと fault を立て、
   `tasks/task_think.c:375-382` は publish 失敗で fault を立てる。
   **publish せずに早期復帰する arbiter 経路はリセットまで車両を殺す**
7. **CPU1**: ローカル safe stop は名目1500 msではなく**実測約3 s**（0-A）。S4 でこれが支配的になる
8. **CPU0はCPU1の状態フレームが来るまで何も送らない**（`tasks/task_command.c:245-247`）。
   S2 のモード再構成はこの順序関係を壊してはならない

### 4-5. ★μT-Kernel のタスク分離を「ながら動作」として実証する

計画書の看板機能「音を聴きながら、走りながら、避けながら」は、
**μT-Kernel の優先度付きマルチタスクの有用性そのものの実証になる**。
これは本コンテストにおいて NPU より確実な加点要素だと考える
（NPUは「使えた」と言えて初めて点になるが、タスク分離は**測れば必ず点になる**）。

**現状の障害**: `CPU0_AUTONOMY_MODE` による**コンパイル時排他**（`config/control_config.h:8-10`、
`tasks/task_think.c:366`。既定は `SOUND_FOLLOW`）で、かつ音源追従は
1秒の `MOVE_STEP` バーストしか動かない。これでは「走りながら避け続ける」にならない。

**最終構成（タスク分担が主張の中身）**:

| コア     | タスク                                   | 優先度       | 周期                     | 責務                                                              |
| -------- | ---------------------------------------- | ------------ | ------------------------ | ----------------------------------------------------------------- |
| CPU0 M85 | `task_command`                         | 6            | 50 ms                    | IPC keep-alive・期限監視                                          |
| CPU0 M85 | `task_acoustic_link`                   | 8            | 1 ms poll                | USB・frame検証・**特徴量バースト再組立**                    |
| CPU0 M85 | `task_sensor`                          | 9            | 50 ms                    | ToF×3 ＋ IMU                                                     |
| CPU0 M85 | `task_think`                           | 10           | 100→50 ms               | 目標方位・回避方策・**safety_arbiter**                      |
| CPU0 M85 | **`task_infer`**                | **11** | イベント起床             | **背景AE・能動フレーム識別**                                |
| CPU1 M33 | **`task_safety`（新規・4-7-b）** | **3**  | 周期                     | **指令タイムアウト監視・safe stop（制御タスクより高優先）** |
| CPU1 M33 | `task_actuator`                        | 4            | 1 ms（周期ハンドラ起床） | 指令適用・制限                                                    |
| CPU1 M33 | `task_status`                          | 12           | 10 ms                    | 状態送信・LED                                                     |

**実証として測る内容（これが成果物）**:

1. **`task_infer` が走っている間も CPU1 の 1 ms 制御周期が一切乱れない**（別コア＋IPC境界）
2. **CPU0 内でも優先度により `task_command` の 50 ms keep-alive が推論に遅延させられない**
   （`task_infer` は優先度11で最下位）
3. `task_infer` の実行時間と、`task_think` の周期内完了
4. 推論負荷あり／なしでの `task_command` ジッタ比較（Live Watch ＋ GPIOトグルをスコープで）
5. 期限超過カウンタが0であること

> この「**別コアで安全を担保し、同一コア内は優先度で実時間性を担保する**」二段構えは
> `docs/firmware/ARCHITECTURE.md` が既に持つ設計思想であり、
> 数値で裏付ければそのまま設計上の主張になる。

**段階適用（S1〜S5）** — ⚠ ここは安全上クリティカル。
現在ローバーが壁へ突っ込まない理由は `task_think_sound_motion_allowed()` の ToF veto と、
音源追従が1秒バーストしか動かないこと、**この2点だけ**。4-5 はその両方を外す。
**一括で適用してはならない。**

| 段           | 変更                                                                                                                                                                                          | この段の後で必ず証明すること                                                                                                    | 状態 |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ---- |
| **S1** | **新規** `CPU0/src/control/safety_arbiter.c/.h`（L3）へ ToF veto 判定を集約。**意味論は `tasks/task_think.c:70-85` のマスク方式を採用と明記**（回避側の完全一致比較は捨てる） | 既存の4方向試験が同じ結果。**かつ 0-F の生存性判定が入っていること**（下記★）                                            | **実機確認完了** |
| **S2** | `CPU0_AUTONOMY_MODE` のコンパイル時排他を廃止し実行時合成へ。**この段では回避出力を使わない**                                                                                         | 既定挙動が S1 と同一。250 mm 接近で停止。`task_command.c:245-247` の起動順序が保たれている                                    | **実機確認完了** |
| **S3** | 4-1-a 古典プランナの出力を`safety_arbiter` 経由で有効化。**音源追従はまだ1秒バースト**                                                                                                | 障害物へ向けて走らせ、出力に関わらず 250 mm で停止。舵角・速度がレート制限内。veto が`emergency_stop` ラッチを使っていない    | **実機確認完了** |
| **S4** | 音源追従を「目標方位の生成」だけに絞りバースト走行を撤去（＝連続走行＝「ながら動作」成立）                                                                                                    | **制動距離の実測**。0-B の counts/rev・0-D の採寸・0-E の惰走距離から、遅延込みで 250 mm 以内に停止できることを実測で示す | **実装完了・実機待ち** |
| **S5** | `task_think` 100 ms → **50 ms**（`task_sensor` と同期）。テレメトリも 20 Hz へ                                                                                                     | 実行時間が周期内（Live Watch）。`task_infer` 起床と競合しない                                                                 | **実装完了・実機待ち** |

> ★**S1 の受入条件に「センサタスクを止めた状態での試験」を含める**。
> 不具合B のとおり `age_ms` は恒真なので、「障害物に近づけたら止まった」だけでは
> **凍結スナップショットで走り続ける欠陥を検出できない**。
> `task_sensor` を意図的に停止させて停止することを確認して初めて veto が証明される。

**最終の直列合成**:

```
task_think 50ms:
  音響識別結果（task_infer） + 音源方位（sound_follow） → 目標方位
                                                          ↓
  ToF/IMU snapshot → avoidance policy（4-1a or 4-1b） → 操舵・速度
                                                          ↓
                    safety_arbiter（生存性・クランプ・veto・レート制限）
                                                          ↓
                    task_command → IPC → CPU1 task_actuator
```

### 4-6. `software/control-sim/` — オフターゲット試験基盤

**進捗（2026-09-12）: 4-6-6の立ち上げ順を完了。**
μT-Kernel/FSP shim、既存の音源追従・障害物回避Cコードのホストビルド、
ctypesレイアウトガード、不変条件I2/I8、合成シナリオS1、ASan/UBSan付き
スタンドアロン検査を実装した。さらにrover-monitorのJSON Linesを実際のC回避
コントローラへ流すreplay層を追加し、`software/control-sim` の回帰は12件成功。
0-Fの実機ゲート（`task_sensor` 更新停止時の停止と解除後の復帰）と0-CのDoA極性校正は完了。
0-Eのデューティ・実速度・惰走距離計測は完了扱いとし、S1〜S5や安全経路の変更へ進める。

> ★**この節は Phase 4 に置いてあるが、付録B-5 のとおり「最初に」作る。**
> 回帰試験が先にあれば Phase 4 が「完璧に推論する」から「テストに対して反復する」に変わり、
> 安価なモデルでも到達できる。本計画で最もコストを下げる項目である。

#### 4-6-1. 成立する根拠（実読で確認済み）

既存コントローラはホスト可搬で、FSP・I/O・外部グローバル参照を持たない。
ただし `sound_follow_controller.c` は状態機械であり、ファイル内の `controller` コンテキストを
`sound_follow_controller_init()` で初期化する。したがって初回試験では**初期化後の同一入力列が
同一出力列になること**を検証し、以後に追加するコントローラは真の純関数として設計する。

| ファイル                                    | include                                                                                      | FSP依存        | グローバル参照                                               |
| ------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------ |
| `control/sound_follow_controller.c`       | 自ヘッダ ＋`config/control_config.h` のみ                                                  | **なし** | ファイル内`controller` コンテキスト（`init` でリセット） |
| `control/sound_follow_controller.h`       | `common/acoustic_protocol.h`（固定幅型・ESP32と共有＝既にホスト可搬）＋ `<tk/tkernel.h>` | **なし** | —                                                           |
| `control/obstacle_avoidance_controller.c` | 自ヘッダ ＋`sound_follow_controller.h` ＋ config 2枚                                       | **なし** | **なし**                                               |
| `config/control_config.h`                 | include なし（`#define` のみ）                                                             | **なし** | —                                                           |

⇒ **ホスト側で足りないのは実質2つだけ**:

1. `<tk/tkernel.h>` — μT-Kernel のスカラ型（`B/UB/H/UH/W/UW/D/UD/BOOL/ER/ID/INT`）と
   リンケージマクロ（`LOCAL`/`EXPORT`/`IMPORT`/`Inline`）。**30行程度のシムで代替できる**
2. `hal_data.h` — `services/sensor_hub.h:8` が `fsp_err_t` のためだけに引いている。
   **10行程度のスタブで代替できる**

> **`sensor_hub.h` の FSP 依存は設計上も剥がすべき**（純粋な制御層が FSP を引く理由がない）。
> `sensor_snapshot_t` と valid フラグ定義を**型専用ヘッダへ分離**し、
> `fsp_err_t` を返すドライバAPIだけ `sensor_hub.h` に残すのが正しい層分け。
> ただし**まずはスタブで動かし、ヘッダ分割は Phase 0-G の負債整理で行う**
> （control-sim の立ち上げを型分割の完了に依存させない）。

#### 4-6-2. 構成

```
software/control-sim/
├─ pyproject.toml            uv プロジェクト（numpy / pytest）
├─ shim/
│  ├─ tk/tkernel.h           μT-Kernel 型・リンケージマクロのホスト代替
│  └─ hal_data.h             fsp_err_t / FSP_SUCCESS のスタブ
├─ build.py                  対象 .c をホストコンパイル → 共有ライブラリ
├─ controlsim/
│  ├─ bindings.py            C 構造体のバインディング
│  ├─ layout_guard.py        ★構造体レイアウトのドリフト検出（下記）
│  ├─ scenarios.py           合成シナリオ生成
│  ├─ replay.py              UDP JSON ログ → 入力列
│  └─ invariants.py          不変条件アサーション
├─ tests/
│  ├─ test_invariants.py     不変条件（最重要）
│  ├─ test_scenarios.py      合成シナリオ
│  ├─ test_golden.py         記録トレースの回帰
│  └─ test_mel_parity.py     mel 係数のビット一致（Phase 2 検証①）
├─ traces/                   収録した JSON Lines
└─ golden/                   期待出力
```

**ビルド**: ホストの clang で
`-std=c99 -Wall -Wextra -Werror -fsanitize=address,undefined -O0 -g`。
macOSでは framework Python が sanitizer 付き共有ライブラリを読み込めないため、ctypes用の共有ライブラリは
非計装で作り、**同じCソースをリンクしたスタンドアロン検査を ASan/UBSan 付きで必ず実行する**。

> ★**サニタイザが効く理由**: この制御コードは `H`（int16）演算を多用する。
> `rpm * 1000` のような式は**実機では静かに桁あふれする**が、
> UBSan は符号付きオーバーフローを検出して落としてくれる。
> 実機に載せる前にこの種のバグを潰せるのは大きい。

#### 4-6-3. バインディングとドリフト対策

実装は**実際の C をそのまま呼ぶ**（Python への再実装はしない。二重実装は必ずずれる）。

構造体を Python 側でミラーする方式は、**C 側の構造体が変わると無言でずれる**のが唯一の弱点。
これを**自動検出**する:

- C 側に小さな自己記述プログラムを置き、各構造体の `sizeof` と主要メンバの `offsetof` を出力する
- `layout_guard.py` がそれを読み、Python 側バインディングのレイアウトと突き合わせる
- `tests/` の最初に必ず走らせ、**ずれたらテスト全体を落とす**

これがあれば「構造体を変えたのにテストが通ってしまう」事故が起きない。

#### 4-6-4. 4種類のテスト

**① 不変条件テスト（最重要・安全エンベロープをコードにする）**

4-4 の安全設計をそのまま実行可能な形にする。**ランダム入力（無効フラグ・0 mm・古い age を含む）を
大量に流して、以下が常に成り立つことを検査する**。

| #  | 不変条件                                                                                       | 由来                                             |
| -- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| I1 | 有効な ToF のいずれかが 250 mm 未満 → 出力速度スケール = 0                                    | 4-4-2                                            |
| I2 | `\|steering_deg\| ≤ CPU0_SOUND_STEERING_MAX_DEG (45)`                                         | 4-4-1                                            |
| I3 | 連続呼出し間の舵角変化がレート制限以内                                                         | 4-1-a                                            |
| I4 | 連続呼出し間の速度変化がレート制限以内                                                         | 4-1-a                                            |
| I5 | `valid_flag = 0` のチャンネルの距離値が出力に影響しない（**0 mm を近接と誤読しない**） | 4-2                                              |
| I6 | 通常の障害物 veto では`emergency_stop` が立たない                                            | 4-4-3                                            |
| I7 | `update_count` が進まない入力列では一定時間後に停止側へ倒れる                                | 0-F                                              |
| I8 | `init` 後に同じ入力列 → 同じ出力列（既存の状態機械の決定性）                                | 初回基盤。新規純関数は初期化不要で同条件を満たす |
| I9 | 停止意図でない限り速度スケールが 0 を通らない                                                  | 4-3-2                                            |

★**I5 と I7 は今回発見した不具合そのもの**。テストに落としておけば二度と再発しない。

**② 合成シナリオテスト（実機データが無くても書ける＝最初に書ける）**

| #  | シナリオ                                  | 期待                                                                                                         |
| -- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| S1 | 正面の壁へ直進接近                        | 250 mm までに停止                                                                                            |
| S2 | 廊下（左右に壁）                          | 中央を維持して直進                                                                                           |
| S3 | 片側だけ障害物                            | 反対側へ**滑らかに**曲がる（離散的に飛ばない）                                                         |
| S4 | ★**ビーム間隙間に細い脚**（0-D-2） | 234 mmより近い領域では隙間が理論上生じる。250 mm停止ゲートが先に働くか、検出無効時の扱いをテストで明文化する |
| S5 | ToF 1本が無効                             | 残り2本で減速走行を継続（停止しない）                                                                        |
| S6 | 全 ToF 無効                               | 停止                                                                                                         |
| S7 | センサ更新停止（`update_count` 凍結）   | 停止                                                                                                         |
| S8 | 目標方位が後方                            | その場旋回せず前進しながら曲がる                                                                             |

★**S4 の価値**: 近距離のビーム間隙間という設計上の穴を、
**床で発見するのではなくテストで先に直視させる**。
「隙間に入った脚は見えない」という事実をテストに書くことで、
対策（0-D-2 の候補1〜3）を選ぶ判断が実装前に強制される。

**③ ゴールデン回帰テスト（実走ログが溜まってから）**

- `software/rover-monitor/main.py` が受ける UDP JSON Lines をそのまま `traces/` へ保存
- 各行から観測（`tof_mm[3]` / valid / `gyro_dps_x10` / 目標方位 / 速度スケール）を組み立てて C へ流す
- 出力を `golden/` と突き合わせる。**意図しない挙動変化を検出する**
- 意図した変更のときは golden を更新し、**その差分を人間がレビューする**

**④ mel パリティテスト**

Phase 2 検証① をここで実施する。ホストの numpy 参照実装と、
RA8P1 用 C 実装（同じくホストコンパイル）の mel 係数・int8 量子化が**ビット一致**することを検査。
完全に機械判定できるので、安価なモデルの反復に向く。

#### 4-6-5. 入れてはいけないもの（純粋性の規律）

FSP・μT-Kernel API・I2C・PWM・USB に触る関数は **control-sim の対象外**。
もし対象にしたくなったら、それは**その関数が純粋でない**という設計上の警告である。

この制約が「コントローラを純関数に保つ」圧力として働く。
`safety_arbiter` / `avoidance_policy` / `odometry` / `acoustic_identifier` は
**最初からこの制約下で設計する**（＝時刻もセンサ値も引数で受け取り、自分で取りに行かない）。

#### 4-6-6. 立ち上げ順（最初のセッションでここまで）

1. `shim/tk/tkernel.h` と `shim/hal_data.h` を書く
2. **既存の `sound_follow_controller.c` をホストコンパイルして1回呼ぶ**
   — ここまでで「本当に純粋か」が実証される。**失敗したらこの節の前提が崩れるので即報告**
3. `layout_guard` を入れる
4. 不変条件 I2 / I8 を書く（既存コントローラに対して今すぐ書けるもの）
5. 合成シナリオ S1 を書く

> 2 が通れば、以降の新規純関数（`safety_arbiter` / `avoidance_policy` / `odometry` /
> `acoustic_identifier`）は**すべて同じ型で試験できる**。
> ここが通らないうちに Phase 4 へ進まないこと。

### 4-7. ★CPU1 を μT-Kernel で動かす意味を強くする

**現状の正直な評価**: CPU1 が使っている μT-Kernel API は
**タスク生成（`tk_cre_tsk`/`tk_sta_tsk`/`tk_ter_tsk`/`tk_del_tsk`）と `tk_dly_tsk` / `tk_slp_tsk` だけ**である。
mutex・イベントフラグ・セマフォ・メールボックス・周期ハンドラは**一切使っていない**
（CPU0 は mutex 10箇所とイベントフラグ1組を使っている）。

⇒ 「`tk_dly_tsk` で寝る2タスク」は**ベアメタルのタイマISR＋スーパーループと機能的に等価**で、
現状では「なぜCPU1もμT-Kernelなのか」に説得力が無い。**これは補強すべき弱点である。**

一方、共有設定 `common/mtkernel/config.h` は
`CNF_MAX_CYCID (4)` / `CNF_MAX_ALMID (8)` / `CNF_MAX_FLGID (16)` / `CNF_MAX_SEMID (16)` /
`CNF_MAX_MTXID (4)` を**既に確保しているのに CPU1 では未使用**。資源は揃っている。

#### 4-7-a. 1 ms 周期を「周期ハンドラ＋イベントフラグ」へ移す ★最優先

**これは 0-A のバグ修正そのものであり、同時に RTOS の正しい使い方の実証になる。**

現状 `tasks/task_actuator.c:69-77` は `while(1) { update(); tk_dly_tsk(1); }`。
`tk_dly_tsk` は**相対待ち**なので、①`+TIMER_PERIOD` で 2 ms になり
②処理時間が周期に上積みされて**ドリフトが累積する**。
`WHEEL_SPEED_SAMPLE_PERIOD_MS` を反復回数で数えている限り RPM は狂い続ける。

**変更**: μT-Kernel の**周期ハンドラ**（`tk_cre_cyc` / `tk_sta_cyc`）を 1 ms で起動し、
ハンドラは `tk_set_flg` だけを行う。`task_actuator` は最高優先度で `tk_wai_flg` で待つ。

```
周期ハンドラ (1 ms, カーネルtick絶対基準)
    └─ tk_set_flg のみ（ハンドラ部なので待ち系APIは呼ばない）
           ↓
task_actuator (優先度 4, tk_wai_flg で待機)
    └─ actuator_service_update_1ms()
```

| 観点           | `tk_dly_tsk` ループ（現状）          | 周期ハンドラ＋フラグ（変更後）                       |
| -------------- | -------------------------------------- | ---------------------------------------------------- |
| 周期基準       | **相対**（前回起床から）         | **絶対**（カーネルtick境界）                   |
| 処理時間の影響 | **周期に上積みされ累積ドリフト** | 吸収される。ドリフトしない                           |
| 実周期         | 2 ms（+TIMER_PERIOD）                  | **1 ms**                                       |
| 実行文脈       | タスク部                               | ハンドラ部は通知のみ、処理は**タスク部のまま** |

> 周期ハンドラは「タスク独立部（ハンドラ部）」で走るため待ち系APIを呼べず、システムスタックを使う。
> よって**ハンドラでFSPのPWM APIを直接叩かず、`tk_set_flg` だけに留める**のが定石。
> これにより「ドリフトしない時間基準」と「タスク文脈での駆動処理」を同時に得る。
> 周期ハンドラを使わない場合の代替は `tk_get_otm()` で絶対デッドラインを持つ方式だが、
> 実装量はほぼ同じでイディオムとしては弱い。

#### 4-7-b. 安全ウォッチドッグを独立した最高優先度タスクへ分離 ★安全性の実利あり

**現状の構造的欠陥**: 指令タイムアウト判定が
`services/actuator_service.c:188-196` ＝ **1 msループの内側**にある。
つまり**制御タスクが止まると、ウォッチドッグも一緒に止まる**。
「CPU0が死んでも車両は止まる」は成立しているが、
**「CPU1の制御タスク自身が死んだら止まらない」**。

**変更**: 指令タイムアウトと safe-stop を **`task_actuator` より高い優先度の独立タスク**
（または専用の周期ハンドラ）へ移す。制御タスクがハングしても、
ウォッチドッグが先に走って `bts7960_stop()` を叩ける。

- これは**ベアメタルのスーパーループでは構造的に書けない**（同じループの中にいるため）
- μT-Kernel の優先度付きプリエンプションが**安全性に直結して効く**唯一の例になる
- 実証方法: `task_actuator` を意図的に無限ループへ落とし、**車両が止まることを実機で示す**

#### 4-7-c. IPC受信を「ISR → イベントフラグ → タスク起床」へ（任意）

現状は IPC callback が IRQ 文脈で staging/commit だけ行い、タスクは 1 ms ポーリングで取りに行く
（`docs/firmware/ARCHITECTURE.md` の明示的な設計判断）。
`tk_set_flg` は μT-Kernel が ISR からの通知に用意している正規の手段なので、
これを使えば指令適用の遅延が最大2 ms → ほぼ0になる。

**ただし優先度は低い**。1 ms ポーリングでも十分速く、現行方針は安全側に倒した妥当な設計である。
4-7-a / 4-7-b を先に済ませ、余力があれば着手する。着手する場合は
「IRQからはPWM APIを呼ばない」という既存の不変条件を崩さないこと。

#### 4-7-d. タスク単位のフォールト封じ込めと再起動（任意）

`tk_ter_tsk` / `tk_del_tsk` / `tk_cre_tsk` は既に初期化で使われている。
**実行時に**使えば「ドライバタスクが異常になったら safe-stop を維持したまま当該タスクだけ再生成する」
が書ける。ベアメタルには無い能力の実演になる。Phase 4 の余力次第。

#### 4-7-e. 「2つの独立カーネル」という非対称マルチコア構成を前面に出す

これは**既に実現できていて、かつ十分に珍しい**。主張として整理する:

| 項目         | CPU0                            | CPU1                                           | 出典                                                        |
| ------------ | ------------------------------- | ---------------------------------------------- | ----------------------------------------------------------- |
| コア         | Cortex-M85                      | **Cortex-M33（M85前提のBSPを上書き）**   | `CPU1/src/mtkernel_config/include/.../ek_ra8p1/machine.h` |
| クロック     | 1 GHz                           | **250 MHz（PLL1P÷4）**                  | `.../ek_ra8p1/sysdef.h`                                   |
| SRAM領域     | `0x22000000` + 936 KiB        | **`0x220EA000` + 936 KiB（重ねない）** | `.../cpu/ra8p1/sysdef.h`                                  |
| T-Monitor    | 有効                            | **無効（SCI8をCPU0と競合させない）**     | `common/mtkernel/config.h` の `_RA_ORDINAL` 分岐        |
| カーネル実体 | 同一 submodule を共有コンパイル | 同一                                           | `firmware/ra8p1/README.md:52-67`                          |

主張: **1個のダイ上で、同一のμT-Kernelソースから、コア・クロック・メモリ領域・
デバッグ端子の割当てが異なる2つの独立インスタンスを動かしている（AMP構成）。**
共有カーネル状態はゼロで、両者は IPC FIFO だけで結ばれる。
片方のカーネルがどうなっても他方のスケジューラは影響を受けない。

#### 4-7-f. 測れる主張に落とす（これが成果物）

| # | 主張                                           | 測り方                                                                               |
| - | ---------------------------------------------- | ------------------------------------------------------------------------------------ |
| 1 | CPU1 の 1 ms 周期は**CPU0 の負荷と独立** | CPU0 で推論＋USBバースト＋Wi-Fi を同時に回し、CPU1 の周期ジッタを比較                |
| 2 | 周期ハンドラ化で**ドリフトが消える**     | 4-7-a の前後で、1万周期あたりの累積誤差を実測（現状は約2倍に出る）                   |
| 3 | **制御タスクが死んでも車両は止まる**     | 4-7-b。`task_actuator` を無限ループへ落として実機確認                              |
| 4 | CPU0 内でも優先度で実時間性が保たれる          | `task_infer`（優先度11）負荷下で `task_command`（優先度6）のジッタを比較         |
| 5 | 2カーネルは互いのスケジューラに干渉しない      | CPU0 を`task_think_halt()` の無限ループへ落としても CPU1 が safe-stop 後に動作継続 |

> 1・3・5 はいずれも**ベアメタルでは示せない、または構造的に成立しない**主張であり、
> 「なぜCPU1もμT-Kernelなのか」への直接の回答になる。

---

## Phase 5: LAN UDP でのクラウド試験

計画書は「メインマイコン → サブマイコン → Wi-Fi → クラウド」を想定。
**ユーザー判断により LAN UDP 送信までで可**とする。

- 既に `wifi_telemetry.c` が STA + UDP JSON を `APP_UDP_DESTINATION_IPV4 "192.168.0.232"` :`5005` へ送出済み。
  **機能としては成立している**
- 残作業は ①Phase 3 の `0x22 POSE_TELEMETRY` と環境認識フィールドをJSONへ追加
  ②周期を 3-6 の設定へ ③`software/rover-monitor/main.py` の `FORWARD_DESTINATIONS` を
  有効化して学習データ収集とクラウド中継の分岐点にする
- MQTT/TLS/クラウドSDKは**範囲外**（差分として記録）

**併せて是正**:

- `firmware/esp32s3/src/app_config.h` に **Wi-Fi SSID とパスワードが平文でハードコードされている**。
  NVS または menuconfig 経由へ移す
- `wifi_telemetry_task` は 5120 B スタックに `char json[1400]`（`wifi_telemetry.c:26,370`）と
  `uint8_t rx_data[512]`（`:317`）を載せている。
  **JSONへ pose や音響フィールドを足す時はここが最も危ない**（FFT作業領域よりこちらが先に溢れる）

---

## 実装順序まとめ

| Phase       | 内容                                                                                                                           | 実機依存       | 目安STEP |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------ | -------------- | -------- |
| **0** | タイミング是正(0-A)・エンコーダ校正4分割(0-B)・DoA極性(0-C)・幾何実測(0-D)・デューティ/惰走(0-E)・age_ms生存性(0-F)・負債(0-G) | **必須** | 約350    |
| 1           | macOSビルド系確認、`uv`環境（TF追加）、TFLMベンダリング＋C++有効化                                                           | 不要           | 約250    |
| 2           | 音響識別AI（特徴量リング・32bin・**背景AE＋能動フレーム照合**・Code MRAM永続化・送受信規律）                               | 一部           | 約1400   |
| 3           | 状態送信レート是正＋累積カウント/時刻語＋オドメトリ＋**rover-monitor 3層可視化**＋**ToF ROIマルチゾーン(0-D-5)**   | 一部           | 約1050   |
| 4           | なめらか回避（古典→蒸留MLP）＋safety_arbiter＋S1〜S5＋**タスク分離の実測実証**＋**CPU1のμT-Kernel補強(4-7)**     | 一部           | 約1250   |
| 5           | UDP JSON 拡張＋資格情報の是正                                                                                                  | 不要           | 約150    |

合計約 **4450 STEP**。計画書の 4000〜5500 STEP 見積りの範囲内。

> 内訳の考え方: Phase 1 の TFLM 導入、Phase 2 の背景学習と現場照合、
> Phase 3 の可視化、Phase 4 の CPU1 μT-Kernel 補強（4-7、+250）。
> Phase 5 は LAN UDP までとしたため縮小。
> なお 4-7-a（周期ハンドラ化）は **0-A の修正実装そのもの**なので二重計上していない。
> Ethos-U を範囲外としたことで、ドライバ/Vela/メモリ配置の見積り困難な塊を抱え込んでいない。

## 検証方法

**Phase 0**

- 0-A: `actuator_service_update_1ms()` の実周期を実測＋**7定数の是正前後の実効値を対で記録**＋
  **CPU1指令タイムアウトが是正後も safe stop することを実機確認**
- 0-B: 4つの独立測定（極性符号／手回し10回転のカウント／実周期／ストップウォッチRPM）を別々に記録
- 0-C: 4方向DoA校正表
- 0-D: トレッド幅・ホイールベース・**ToF 3台の (x,y) オフセットと方位角**・ToF実効視野角。
  車輪径 108.00 mm は STL 由来で確定済み
- 0-E: 左右別の最小走行デューティ、**320‰ からの惰走距離**
- 0-F: **`task_sensor` を停止させて車両が止まること**
- すべて `docs/firmware/diagnostics/` に日付付きで残す（既存の慣行に合わせる）

**Phase 2**

1. ホスト参照実装とRA8P1実装の mel 係数・量子化の**ビット一致**
2. 背景モデルのMSEは候補生成だけに用い、対象識別には96次元要約のcosine距離だけを用いること
3. 既知音／未知音それぞれ20サンプルで、能動フレーム不足を「不定」として分離し、実録音の距離分布と誤判定を記録する
4. **レベルを変えて（距離・音量）同一音源が同一判定になること**（正規化の検証）
5. 電源を切って再投入し、背景デコーダ・5件の見本・しきい値が復元されること
6. **`task_infer` の実行時間を実測**し、`task_think` 周期内かつ `task_command` を遅らせないこと
7. 40パケットのバーストで `g_task_acoustic_link_sequence_drop_count` が増えず、
   `frame_index` の欠落が無いこと
8. 起動時 `esp_get_free_heap_size()` / `heap_caps_get_largest_free_block()` ログ、およびCPU0のサービス実行時間を記録

**Phase 3**

1. 状態フレーム周期を実測し、是正後に目標レートへ乗っていること
2. IPC でフレームを意図的に落として**累積カウント方式が変位を失わないこと**
3. **①ローバー中心ビューが、実際に置いた障害物の位置と方位を正しく表示すること**（メジャーで照合）
4. ②累積マップは既知の矩形経路（例 2 m × 2 m）で戻り点との差を実測し、
   **誤差を数値で明示したうえでラベル表示すること**（精度主張はしない）
5. 10 Hz / 20 Hz で `update_count` に重複が無いこと

**Phase 4**: S1〜S5 の各段で上表の「証明すること」を確認したうえで、最終的に

1. オフターゲット回帰試験がグリーン
2. `CPU0_SENSOR_HARD_STOP_DISTANCE_MM` へ意図的に接近させ、モデル出力に関わらず停止すること
3. **`task_sensor` 停止時にも停止すること**（0-F の回帰）
4. IMUコネクタを抜いてもToF健全なら減速走行を継続すること
5. veto 後の再発進が `emergency_stop` ラッチ往復を伴わないこと
6. **「ながら動作」の実証**: 音源追従＋回避＋推論の同時動作を撮影し、
   同時に **CPU1 1 ms 周期のジッタと `task_command` のジッタを推論負荷あり／なしで比較した数値**を残す
7. **CPU1 の μT-Kernel 補強（4-7-f）**:
   ①CPU0に推論＋USBバースト＋Wi-Fiを同時に掛けてもCPU1の周期ジッタが変わらないこと
   ②周期ハンドラ化の前後で1万周期あたりの累積誤差を実測（現状は約2倍に出る）
   ③**`task_actuator` を無限ループへ落として、`task_safety` が車両を止めること**
   ④CPU0を `task_think_halt()` へ落としてもCPU1がsafe-stop後に動作継続すること

**Phase 5**: rover-monitor に座標・障害物・検知イベントが時系列で見えること。
`app_config.h` に平文資格情報が残っていないことをgrepで確認。

---

## 付録: 確認済み諸元（すべて現在の実ファイル由来、推測値なし）

### RA8P1 (R7KA8P1KFLCAC / EK-RA8P1)

| 項目                  | 値                                                                                                                                                                                      | 出典                                                                           |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| CPU0                  | Cortex-M85**1 GHz**（PLL1P 1000 MHz、`BSP_CFG_CPUCLK_DIV` = /1）、Helium/MVE、hard-float、`-O2`                                                                               | `ra_gen/bsp_clock_cfg.h`、`.cproject`                                      |
| CPU1                  | Cortex-M33**250 MHz**（`BSP_CFG_CPUCLK1_DIV` = /4）                                                                                                                             | 同上                                                                           |
| FSP                   | **6.4.0**                                                                                                                                                                         | `ra/fsp/inc/fsp_version.h:43`                                                |
| NPU                   | **実在**。`R_NPU_Type` 4096 B @ `0x40140000`、`R_NPU_BASEP0..P3`（Ethos-U のレジスタ構成）、`ELC_EVENT_NPU_IRQ (0x067)`、`BSP_CFG_NPUCLK_DIV` = /2（**500 MHz**） | `R7KA8P1KF_core0.h:62866,64846`、`bsp_elc.h:103`、`bsp_peripheral.h:137` |
| NPUソフト層           | **存在しない**。`r_npu` なし、Ethosドライバなし、Vela成果物なし                                                                                                                 | `ls ra/fsp/src/`                                                             |
| CMSIS                 | `ra/arm/CMSIS_6/CMSIS/` は **Core のみ**（DSP も NN も無い）                                                                                                                    | 実測                                                                           |
| コードFlash / SRAM    | 1 MB / 約1.83 MB（両コア共有、CPU0 936 KiB + CPU1 936 KiB）                                                                                                                             | `ra_cfg.txt`、`firmware/ra8p1/README.md:65`                                |
| **データFlash** | **0 バイト（非搭載）** ← 永続化方式に直結                                                                                                                                        | `ra_cfg.txt`                                                                 |
| 同梱FSPドライバ       | CPU0:`bsp` `r_iic_master` `r_ioport` `r_ipc` `r_usb_basic` `r_usb_hcdc` / CPU1: `bsp` `r_gpt` `r_icu` `r_ioport` `r_ipc`                                          | `ls ra/fsp/src/`                                                             |

> FSPは選択モジュールだけを生成する。Code MRAM自己書換には`r_mram`、OSPIを使うには対応モジュールを **e² studio で追加→再生成**する必要がある。
> ただし P801/P803/P808 をサーボへ転用中のため **Octo-SPI は使用不可**。

### μT-Kernel の時間基準（最重要）

- `timer.c:107/119`: `event->time = lltoul(knl_current_time) + tmout + TIMER_PERIOD`
- `TIMER_PERIOD = CNF_TIMER_PERIOD`（`knldef.h:66`）
- プロジェクト側 `common/mtkernel/config.h:13` = **1** が、BSP側 `mtk3_bsp2/config/config.h:29` = **10** を
  include順で上書きする（**設計意図**。`firmware/ra8p1/README.md:52-59`）
- ⇒ **`tk_dly_tsk(N)` は N+1 ms 待つ**。誤差は一律 +1 ms なので
  **1 msループのみ2倍**、10 ms は10%、50/100 ms は1–2%

### 音響経路

| 項目                 | 値                                                                                                           |
| -------------------- | ------------------------------------------------------------------------------------------------------------ |
| XVF3800 → ESP32 I2S | 16 kHz /**2 ch** / 32 bit / Philips。**ビームフォーミング済みPCM**（生マイク4chは取得不可）      |
| 読み出し粒度         | `APP_AUDIO_BLOCK_FRAMES (256U)` = 2048 B = **16 ms/read**。**160サンプルホップと割り切れない** |
| ESP32の現行処理      | RMS + peak → dBFS スカラーのみ。**PCMは破棄**。左右2slotを1つのRMSに合算                              |
| ESP32 PSRAM          | **未設定**（`sdkconfig.defaults` に `CONFIG_SPIRAM*` なし）。内蔵SRAM 512 KB                       |
| TinyUSB              | `CONFIG_TINYUSB_CDC_TX_BUFSIZE=512`、`APP_USB_TX_TIMEOUT_MS`=20（`app_config.h:29`）                   |
| 既存タスクスタック   | 15360 B（`app_config.h:39-41`）。`wifi_telemetry_task` は 5120 B に `json[1400]` + `rx_data[512]`    |
| XVF3800 I2C          | DoA角度、VAD/speechフラグ、raw status 1バイトのみ                                                            |

### プロトコル `firmware/common/acoustic_protocol.h/.c`（両端で同一ソースをコンパイル）

- フレーム = ヘッダ14 B + ペイロード ≤ **96 B** + CRC-16/CCITT-FALSE 2 B（最大112 B）
- 実装済み: `0x01` HELLO(12) / `0x02` OBSERVATION(14) / `0x03` HEALTH(12) /
  `0x20` ROVER_TELEMETRY(**96 = 満杯、予備は `sensor_reserved` 1 B のみ**) / `0x21` ACTUATOR_TELEMETRY(24)
- **空き枠: `0x10` SET_CONFIG / `0x11` ACK / `0x7F` LOG**。`0x04`/`0x05`/`0x22` は未使用
- **分割・継続フラグは存在しない**。encoder `:102-103` と parser `:365-368` が対称に96超を拒否
- パーサは単一フレーム状態、FRAME_READY で `used`/`expected` をリセット（`:398-399`）→ 連続バーストに耐える
- 現行レート: 上り約 656 B/s、下り約 608 B/s。**律速はUSB帯域ではなく96 Bのペイロード上限**
- CDCテレメトリ周期 `CPU0_AUDIO_TELEMETRY_PERIOD_MS (250U)`、
  UDP側 `APP_UDP_TELEMETRY_PERIOD_MS (250U)` ＝ **両方4 Hz。上げるときは両方**

### センサ

| 項目                      | 値                                                                                                                                                                                                                                                              |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ToF                       | VL53L1X ×3（TCA9548A ch0/1/2）。**3台とも前方向きの平行ビーム**。光学中心は左(-90,+94)、中央(0,0)、右(+90,+94) mm。250 mm付近では隣接ビームが約7.5 mm重なるが、234 mmより近い領域では理論上隙間が開く（0-D-2）。後方・側方の測距なし                     |
| ToF FoV                   | ST ULD 既定ブロックのみ書き込み（`drivers/vl53l1x.c`、237行）、**ROI/timing budget を一切プログラムしていない** → 16×16 SPAD ≈ **約27°**（半角13.5°、ビーム幅 ≈ 0.48·d）。**ROIマルチゾーン化は未実装で、やるなら全部新規**（0-D-5） |
| ToF更新                   | 50 ms周期でToF3個＋IMUを逐次。`task_think` の消費は100 ms                                                                                                                                                                                                     |
| ToF値                     | 3:1 IIR平滑後、有効範囲 40–4000 mm、`range_status == 9` 必須                                                                                                                                                                                                 |
| **ToFの罠**         | 無効時は**last-goodではなく 0 mm** が published される                                                                                                                                                                                                    |
| **`age_ms` の罠** | `services/sensor_hub.c:335` が成功毎に0を代入 → 期限判定が**恒真**。ハング検出不能                                                                                                                                                                     |
| IMU                       | BMI270、ODR 100 Hz設定だが**ポーリング20 Hz**。±4 g / ±500 dps。**積分・バイアス除去・姿勢推定は皆無**                                                                                                                                            |
| IMU界面                   | `gyro_dps_x10` = **0.1 dps LSB**。センサ本来の 0.0153 dps より**6.5倍粗い**                                                                                                                                                                       |
| snapshot                  | `sensor_snapshot_t`（`services/sensor_hub.h:91-102`）に `update_count` と `sensor_diagnostics_t`（`failure_kind`/`failure_device`/`failure_stage`/`failure_channel`、ToF毎の `tof_range_status`/`tof_result`）がある                        |
| `valid_flags`           | `CPU0_SENSOR_VALID_ALL` はIMUを含む（`:35-36`）。回避ゲートは**完全一致**、音源ゲートは**マスク**                                                                                                                                               |

### 駆動・オドメトリ

| 項目             | 値                                                                                                                                                              |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 車輪             | ★**外径 108.00 mm / トレッド面幅 40 mm**（`tire.stl`）。**円周 339.29 mm**                                                                       |
| エンコーダ搭載輪 | ★**中央輪（非操舵）**。距離がそのまま車体中心線の走行距離になる                                                                                          |
| エンコーダ       | **6輪に対し2個**。各側3台は1つのBTS7960へ並列 → 個別制御不可                                                                                             |
| 方式             | GPIO 4本のエッジIRQ + 遷移表によるソフト4逓倍。`drivers/encoder.c` に `W` 累積カウンタ                                                                      |
| エンコーダ極性   | `WHEEL_ENCODER_LEFT_FORWARD_SIGN (-1)` / `WHEEL_ENCODER_RIGHT_FORWARD_SIGN (+1)`（`config/drive_config.h:11-12`）                                         |
| counts/rev       | `WHEEL_ENCODER_COUNTS_PER_REV (702U)`（`:9`）。2000 mm手押し10区間の共通推定701.85を丸めた実機校正値。**約0.4833 mm/count**                           |
| RPM算出          | `drivers/encoder.c` が `WHEEL_ENCODER_COUNTS_PER_REV * elapsed_ms` で除算。`elapsed_ms` は実経過時間を使用するよう是正済み                                |
| 状態送信         | `tasks/task_status.c:94-107` が**1反復1語**、`tk_dly_tsk(10)`=11 ms ⇒ **約187 ms/フレーム（5.3 Hz）**、エンコーダ語は約66 ms古い               |
| IPC ID           | 状態`0x81`〜`0x87`（`ipc_message.h:55-69`）。**`0x88`以降は空き**。語数は `ACTUATOR_IPC_STATUS_WORD_COUNT (7U)`（`:49`）                      |
| IPC FIFO         | FSP R_IPC、方向毎4段、32 bit語`[31:24] ID / [23:0] payload`。`SEQUENCE`(0x05) がcommitマーカ                                                                |
| 速度FB           | `DRIVE_SPEED_FEEDBACK_ENABLE (0U)` = 無効。開ループ `duty = rpm*1000/DRIVE_TARGET_RPM_MAX(300)`                                                             |
| デューティ倍率   | **800/800（対称）**（`config/drive_config.h:24-25`）→ 120 RPM 指令で**左右とも320‰**                                                            |
| デューティ範囲   | `DRIVE_DUTY_MIN_PERMILLE (0)`（**静止摩擦デッドバンドなし**）/ `DRIVE_DUTY_MAX_PERMILLE (700)`。クランプ順序のため**実効最大 560‰**            |
| 停止動作         | `drive_service_stop`（`:122-132`）→ `bts7960_stop()`（`drivers/bts7960.c:36-58`）が enableピンLOW＋4PWM 0＋タイマ停止。**速度0はランプせず即断** |
| ランプ実効       | `drive_service_ramp_value`（`:90-100`）は約2 msに1回＝**約1‰/ms**。PWM反映は `DRIVE_UPDATE_PERIOD_MS (5)` で間引き＝**実約10 ms粒度**        |
| 駆動極性         | `DRIVE_CHASSIS_FORWARD_SIGN (-1)` × `DRIVE_LEFT_MOUNT_SIGN (+1)` / `DRIVE_RIGHT_MOUNT_SIGN (-1)` を `#define` 時点で合成（`:15-19`）                 |

### 安全に関する既存の不変条件（AIが破ってはならない）

`docs/firmware/ARCHITECTURE.md` より:

- 「アクチュエータ制御経路は**CPU0からCPU1への一方向**である」
- 「UDPは外部診断専用で、PCやESP32S3からCPU1へ制御を迂回させない」
- 「将来Wi-Fi手動操作を追加する場合も、**CPU0を権限・安全・timeoutの境界として維持する**」

実コードで確認した挙動:

- CPU1 `actuator_service`: IPC指令が届かないと safe stop。名目 `1500 ms` だが**実測約3 s**（`:188-196` + 0-A）
- `task_think`: 観測時刻／sequence watchdog が **600 ms**（`CPU0_SOUND_OBSERVATION_TIMEOUT_MS`）超過なら
  controller へ渡さない
- `emergency_stop` は **CPU1側のラッチ**。解除に `emergency_stop==0` かつ `actuator_enable==0` の往復が必要
- **異常フラグは実行中に誰もクリアしない**
- CPU0は**CPU1の状態フレームが来るまで送信しない**（`tasks/task_command.c:245-247`）
- 既知の欠落: **ハードウェア非常停止入力なし、CPU間watchdogなし、各側3台の個別速度フィードバックなし**

### 閾値と速度の現状

```
CPU0_SENSOR_HARD_STOP_DISTANCE_MM  250    CPU0_SENSOR_FORWARD_RPM      55
CPU0_SENSOR_BLOCKED_DISTANCE_MM    350    CPU0_SENSOR_CAUTION_RPM      35
CPU0_SENSOR_SIDE_DISTANCE_MM       450    CPU0_SENSOR_TURN_OUTER_RPM   45
CPU0_SENSOR_CAUTION_DISTANCE_MM    650    CPU0_SENSOR_TURN_INNER_RPM   25
CPU0_SENSOR_CLEAR_DISTANCE_MM     1000

CPU0_SOUND_MOVE_LEFT/RIGHT_RPM     120  ← 音源追従の外輪
CPU0_SOUND_TURN_INNER_RPM           90  ← 旋回内輪
CPU0_SOUND_STEERING_MAX_DEG         45
CPU0_SOUND_OBSERVATION_TIMEOUT_MS  600
CPU0_SENSOR_STALE_TIMEOUT_MS       200  ← 0-F 是正時の基準値
```

ルールベース回避は最大 55 RPM 前提なのに、実際に動く音源追従は**外輪 120 RPM／内輪 90 RPM**。
ただし車輪径 108.00 mm が確定したので**制動距離は計算可能になった**（上記「制動距離の見通し」）。
残る未知は惰走距離のみで、0-E で1回測れば確定する。

### ビルド・ホスト環境

- RA8P1: VS Code タスク → **headless e² studio**。macOS は `.vscode/scripts/Invoke-RaBuild.sh` が
  `E2studio.app/Contents/MacOS/e2studio` / `arm-none-eabi-gcc` / GNU make を探索。
  ソース追加時は `Generate + Clean Build` が必須
- ESP32: PlatformIO / ESP-IDF 5.5.3。`idf_component.yml` の依存は `espressif/esp_tinyusb 2.2.1` のみ
  （**esp-dsp 未導入** → Phase 2 で追加）
- ホスト: `uv` 0.10.4。`software/rover-monitor` が既に `uv` 管理下

---

## 付録B: Codex で実装する場合の進め方とモデル配分

> 前置き: 私（Claude）は OpenAI の現行モデル名・価格を確実には把握していない（知識の期限あり）。
> よって**特定のモデル名で断定しない**。代わりに各フェーズが要求する能力を分類し、
> 「最上位の推論モデル／中位／安価」の3段で配分する。実際のモデル名と単価は
> その時点の Codex のラインナップへ読み替えて当てはめること。

### B-1. フェーズ別の要求能力とモデル配分

| Phase                           | コード量                       | 推論の難度                                       | 事故った時の危険度             | 推奨                                         |
| ------------------------------- | ------------------------------ | ------------------------------------------------ | ------------------------------ | -------------------------------------------- |
| **0-A** タイミング是正    | 小                             | **最高**（系全体に波及・安全挙動が変わる） | **最高**                 | **最上位＋人間レビュー必須**           |
| 0-B〜0-E 計測                   | ほぼ無し                       | 低                                               | 低                             | **人間（実機作業）**。エージェント不要 |
| **0-F** `age_ms` 生存性 | 小                             | 高                                               | **高**（safety veto）    | **最上位**                             |
| 0-G 負債整理                    | 中                             | 中                                               | 低                             | 中位                                         |
| **1** TFLM ベンダリング   | **最大**（ファイル大量） | 低                                               | 低                             | **安価**。ただし下記B-3の注意          |
| **2** 音響AI              | 大                             | 高                                               | 中                             | **設計は最上位／穴埋めは中位**         |
| **3** IPC拡張＋オドメトリ | 中                             | 中                                               | 中                             | 中位                                         |
| 3-2 可視化（JS）                | 中                             | 低                                               | 低                             | **安価**                               |
| **4** S1〜S5              | 中                             | **最高**                                   | **最高**                 | **最上位＋各段で人間ゲート**           |
| 4-1a/4-6 純関数＋シム           | 中                             | 中                                               | **低**（実機に触らない） | **安価〜中位**                         |
| **5** UDP/JSON            | 小                             | 低                                               | 低（資格情報除く）             | 安価                                         |

**要点**: 総コード量が最大の Phase 1 は推論難度が最低で、
推論難度が最高の Phase 0-A / 4 はコード量が小さい。
**「高いモデルを使う量」は全体の1〜2割に抑えられる**構造になっている。

### B-2. コストを下げる梃子（この計画に固有のもの）

1. ★**`software/control-sim/`（4-6）を最後ではなく最初に作る。**
   これが最大の梃子。純関数をホストGCCでビルドして UDP ログのトレースを流す回帰試験があれば、
   Phase 4 は「完璧に推論する」から「テストに対して反復する」に変わる。
   安価なモデルでも**テストが通るまで回せば**到達できる。
2. ★**純関数の境界が委譲の継ぎ目である。**
   `acoustic_identifier` / `odometry` / `safety_arbiter` / 古典プランナは
   すべて「入力構造体→出力構造体、I/Oもグローバルも持たない」設計にしてある。
   これらは**実機もRTOS知識も不要**でオフターゲット試験できるので、安価なモデルに出せる。
3. ★**先に契約を凍結する。** 2-2（特徴量仕様）・2-5（プロトコル表）・3-4（IPC語）の3つを
   先に確定して合意すれば、以降は界面が動かないので並列化・安価化が安全になる。
   逆にここが動くと全フェーズが作り直しになる。
4. **機械検証できる項目に寄せる。** Phase 2 検証①（mel係数のホスト実装とのビット一致）は
   完全に機械判定できる。こういう項目は安価なモデルの反復に向く。
5. **プラン全文（約1000行）を毎回渡さない。** 該当フェーズ節＋付録の必要行だけを渡す。
   特に付録の諸元表は、その回で触る行だけ抜いて渡す。
6. **Phase 1 のベンダリングは「移すだけ」に徹させる。** TFLM は
   `docs/firmware/CODING_RULES.md` の型・リンケージ統一対象から**除外**する方針なので、
   エージェントに整形・リネーム・lint適用を**させない**。差分が爆発する。

### B-3. 禁止事項

**冒頭の A-3 に移動した。** セッション開始時にそちらを読むこと。

### B-4. エージェントに委譲してはいけないもの

| 項目                        | 理由                                                                                                                                                          |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0-B〜0-E の実測             | 車輪を回す・定規で測る・スコープを当てる。**物理作業**                                                                                                  |
| **S1〜S5 の各ゲート** | 「250 mm で止まった」「`task_sensor` を止めても止まった」は**実機の目視・実測でしか確認できない**。エージェントの自己申告を通すと安全機構が空洞化する |
| 4-7-b の実証                | `task_actuator` を落として車両が止まるかは実機確認                                                                                                          |
| 0-A の完了判定              | CPU1指令タイムアウトが是正後も発火するか＝**今日より悪くなる唯一の失敗様態**。人間が見る                                                                |
| Wi-Fi 資格情報の取り扱い    | `app_config.h` の平文SSID/パスワードを**ログ・コミット・エージェントの文脈へ出さない**                                                                |

### B-5. 推奨する実行順（コスト効率順）

```
1. 契約凍結           2-2 / 2-5 / 3-4 を人間が確定        … 最上位で設計、量は少ない
2. control-sim 構築    4-6 を先行実装（実機不要・4-6-6 の順） … 中位。以降の反復単価を下げる
3. Phase 1            TFLM ベンダリング                    … 安価。量は最大
4. Phase 0-A / 0-F    タイミング・生存性（人間レビュー）    … 最上位。量は小
5. Phase 0-B〜0-E     実機計測                             … 人間のみ
6. Phase 2            純関数→背景AE/見本照合→タスク       … 設計は最上位、実装は中位
7. Phase 3            IPC語拡張→オドメトリ→可視化         … 中位、可視化は安価
8. Phase 4            S1→S2→S3→S4→S5（各段で人間ゲート） … 最上位＋人間
9. Phase 5            JSON拡張・資格情報是正               … 安価
```

> **現在地（2026-09-16）**: 1〜5 は完了または完了扱い。**6（Phase 2）の途中**である。
> 詳細と「次にやるべき最小単位」は冒頭の**★現在の進捗サマリ**を正とする。
> 8（Phase 4）は未着手だが、**4-0（デッドロック是正）と4-6-7（試験基盤のOS非依存化）は
> S1より前に単独で実施済み**なので、S1の着手条件は変わっていない。

> 2 を 8 より先に置くのが要点。回帰試験が無い状態で Phase 4 を最上位モデルに
> 何度も推論させるより、先にテストを作って安価に反復させる方が総額が下がる。

---

## 付録C: TRONコンテスト2026 評価観点への対応とDEMO設計

> 典拠は `docs/program-plan-2026_ja.pdf`（応募計画書・2026-02-28提出）**のみ**。
> 審査基準そのものは公開情報を持たないので**推測しない**。
> ここでは「**自分が応募書類で約束したこと**」を評価観点の代理として扱う。
> 約束していない機能を足すより、**約束した機能を測れる形で示す**方が確実に効く。

### C-1. 計画書が約束した機能と現在地

| # | 計画書の記述 | 本プランでの対応 | 現在地 |
| - | ------------ | ---------------- | ------ |
| 1 | **環境適応型・音源学習機能（学習モード）**。現場で落下音を鳴らしてエッジAIに学習させる | 2-3（背景AE＋見本照合）／2-6（MRAM）／2-7（ボタン＋LED） | ⚠ 送受信・UI・タスク・背景AE・識別・MRAM保存は実装済み。実機しきい値と復元確認が未完 |
| 2-1 | **聴覚AI**。移動中もマイク監視タスクが独立稼働し、突発音ごとに推論タスクが起床 | 2-1（特徴量リング）／`task_infer`（4-5） | ⚠ 背景AE・能動フレーム識別まで`task_infer`へ接続済み。実機計測と走行反映が未完 |
| 2-1 | **TDoA / GCC-PHAT で方向角** | **不採用**。XVF3800のDoAを使用（生マイク4chが物理的に取得不可） | ✅ 実装済み（DoA極性校正済み）。**計画書からの差分として説明必須** |
| 2-2 | **視覚/空間AI**。複数測距センサ→AI→滑らかなカーブ軌道 | 4-1-a（古典）→4-1-b（蒸留MLP） | ✅ 実装済み |
| 3 | **IoT位置情報マッピング**。オドメトリで相対座標、Wi-Fi経由でクラウド、スマホで確認 | Phase 3（3-5 オドメトリ／3-2 可視化）／Phase 5（UDP） | ⚠ UDP JSON送出のみ稼働。座標・マップは未実装 |
| 3 | **センチメートル精度のグリッド** | 3-1 が「精度」→「正しく分かること」へ**意図的に置換** | ⚠ **差分として説明必須**。誤差を数値で出してラベル表示する方針 |
| 付1 | 絶対距離推定に固執せず方向角＋前進補正 | `sound_follow_controller.c` の状態機械 | ✅ 実装済み |
| **付2** | **μT-Kernelの「ながら動作」とエッジAIの協調**（走りながら聞き続け、避け続ける） | **4-5**（S1〜S5）＋4-7 | ✅ 実装済み**`CPU0_AUTONOMY_MODE` のコンパイル時排他が残っている** |
| 付3 | 現場の環境に依存しないオンデバイス学習 | #1と同じ | ⚠ #1と同じ |

### C-2. ★最も費用対効果が高い順

計画書の**アピールポイント2（ながら動作）**が本コンテストの中核である。
`docs/firmware/ARCHITECTURE.md` の設計思想と一致し、**測れば必ず数値になる**（4-5、4-7-f）。
一方でNPU（Ethos-U）は「使えた」と言えて初めて点になるため範囲外としてある。

| 優先 | 作業 | なぜ効くか | 状態 |
| ---- | ---- | ---------- | ---- |
| **1** | ESP32の `0x04` トリガ＋送信（2-1／2-4） | **計画書の機能1・2-1・付3が同時に生き返る**。他のどの作業より波及が大きい | 実装済み・実機未確認 |
| **2** | `task_infer` の分離（優先度11）＋4-7-f の**ジッタ実測** | **アピールポイント2の唯一の証拠**。「推論中もCPU1の1 ms周期が乱れない」は数値で示せる | 分離は実装済み・ジッタ実測未実施 |
| **3** | 4-5 の S1〜S2（`safety_arbiter` ＋実行時合成） | コンパイル時排他が残る限り「ながら動作」は**構造的に成立しない** | 未実施 |
| **4** | 背景AE・能動フレーム識別（2-3/2-8） | オフラインCNNを要さず、現場の背景検出と個別見本照合を成立させる | 実装・ターゲットビルド済み。実機しきい値決定が未実施 |
| **5** | Phase 3 のオドメトリ＋可視化 | 計画書の機能3。**デモの「見せ場」**になる | 未着手 |

> **C-2で意図的に下げたもの**: 4-1-b（蒸留MLP）。4-1-a の古典プランナで
> 「滑らかなカーブ軌道」は成立するので、**MLPは加点の上積み**であって前提ではない。
> デモ成功率の観点では 4-1-a を固める方が先。

### C-3. ★DEMO設計 — 成功率を設計で作る

デモの失敗は「機能が無い」より**「その場で再現しない」**で起きる。
以下は本プランの既存の仕組みだけで作れる。

#### C-3-1. デモを壊す既知の様態と対策

| # | 失敗様態 | 根拠 | 対策 | 状態 |
| - | -------- | ---- | ---- | ---- |
| 1 | **回避が恒久停止する** | 4-0（実測で再現） | `RECOVER_CLEAR_MM` 是正済み。**実機ゲートが残** | ⚠ 4-0-5 |
| 2 | **学習が1サンプルも入らない** | ★鎖の断点 | `0x04` 送信の実装（C-2優先1） | ❌ |
| 3 | **学習結果が電源断で消える** | — | MRAM A/B＋CRC32＋世代で実装済み。**検証項目「電源再投入で復元」（検証方法Phase 2-4）が未実施** | ⚠ |
| 4 | **識別が環境で崩れる** | 2-2のレベル正規化 | 検証項目Phase 2-3（距離・音量を変えて同一判定）を**デモ会場で1回やる** | ❌ |
| 5 | **USBバーストの取りこぼし** | 2-4-1/2-4-4 | `g_task_acoustic_link_sequence_drop_count` と `frame_index` 欠落を**デモ前に読む** | 計測手段は有 |
| 6 | **CPU1指令タイムアウトで止まる** | 0-A | 実機確認済み | ✅ |
| 7 | **Wi-Fi が会場で繋がらない** | `app_config.h` 平文SSID | **デモはUSB CDC＋rover-monitorをローカルで完結**させ、Wi-Fiは「繋がれば見せる」扱いにする | 設計判断 |
| 8 | **ToF斜め入射で測距が伸び、側面が接触** | 0-D-2／`SENSOR_AUTONOMY.md` | 方向保持100°＋最低30°操舵で実装済み。**小さい障害物では遠回りになる**のでデモ用配置で調整 | ⚠ |

#### C-3-2. デモ会場で測る数値（そのまま発表資料になる）

4-5-f／4-7-f の主張はすべて**同じ走行から採れる**。

| 指標 | 取り方 | 主張 |
| ---- | ------ | ---- |
| CPU1の1 ms周期ジッタ（推論負荷あり／なし） | Live Watch ＋ GPIOトグルをスコープ | **別コア＋IPC境界で実時間性が守られる** |
| `task_command` の50 ms keep-aliveジッタ | 同上 | **同一コア内は優先度で守られる** |
| `task_infer` 実行時間 | `tk_get_otm()` 差分 | 周期内完了 |
| 60秒あたりの正味移動・訪問範囲・後退時間・再試行回数・**接触回数・恒久停止回数** | `SENSOR_AUTONOMY.md` の既存指標 | **回避の実用性**（4-0の効果もここに出る） |
| 期限超過カウンタ | 既存の診断語 | 0であること |

#### C-3-3. デモの構成（成功率順に段を分ける）

**必ず成功する段から順に見せ、後段が落ちても前段で主張が立つ**構成にする。

| 段 | 内容 | 前提 | 落ちたときの代替 |
| -- | ---- | ---- | ---------------- |
| D1 | **ながら動作の数値**（C-3-2のジッタ比較） | `task_infer` 分離 | 事前録画とログ |
| D2 | **障害物回避の連続走行60秒** | 4-0の実機ゲート | 障害物を減らす |
| D3 | **学習モード**（音を5回鳴らす→LED応答→MRAM保存→電源断→復元） | `0x04` 送信 | ホスト側 `acoustic-trainer` で同じ特徴量を見せる |
| D4 | **識別して音源へ向かう** | D3＋背景AE・能動フレーム識別＋実機しきい値 | D3までで「学習は成立」を示す |
| D5 | **rover-monitorで軌跡と検知位置** | Phase 3 | UDP JSONの生値を見せる |

> **原則**: D1〜D2 は**実機計測とcontrol-sim回帰で裏が取れる**ので、
> 本番で落ちる確率が最も低い。**ここを主軸に据える。**
> D3以降は Phase 2 の残作業に依存するので、**C-2の優先1・2を先に潰す**こと。

### C-4. 計画書との差分（発表時に明示すべきもの）

「約束と違う」を黙っていると評価が下がる。**理由を添えて明示する**方が強い。

| 差分 | 理由 | 記載場所 |
| ---- | ---- | -------- |
| TDoA / GCC-PHAT → XVF3800のDoA | XVF3800のI2Sはビームフォーミング済み2chで、**生マイク4chが物理的に取得できない** | 「確定した方針」節に既記 |
| Ethos-U（NPU）を使わない | シリコンは実在するが、ツールチェーン・メモリ配置の見積り困難。CPU実行（M85＋Helium）で成立する | 「Ethos-Uを将来課題とする根拠」節に既記 |
| クラウド → LAN UDPまで | ユーザー判断。MQTT/TLS/クラウドSDKは範囲外 | Phase 5 に既記 |
| 「センチメートル精度」→「誤差を数値で示す」 | 前向き3眼＋IMUオドメトリで精度を主張すると破綻する。**正しく分かることを目標に置換** | 3-1 に既記 |

---
