# 第10章: 実機キャリブレーション・テスト・トラブルシューティング

本章では、SEROVの実機を安全かつ確実に動作させるための「物理パラメータの実機校正記録」、e² studioにおける「Live Watchデバッグ必須変数」、および開発中によく遭遇する「トラブルシューティング集」を解説します。

---

## 10.1 実機キャリブレーション記録と定数の決定

組み込み制御において、カタログスペックの数値を鵜呑みにすると、計算誤差が蓄積して自律走行が破綻します。
SEROVプロジェクトでは、綿密な実機計測によって物理パラメータを固定しています。

### 1. エンコーダ分解能の実機校正（702 counts/rev）
JGA25-370モータのカタログ値は900 counts/revでしたが、実機において以下の厳密な手押し計測を実施しました。

```text
【測定条件】
  ・車体を直線で正確に 2000 mm 手押し（10回反復計測）
  ・車輪外径: 108.00 mm（外周長: 339.292 mm）
  ・左右累積エンコーダカウントの増分を記録
```

| 計測回 | 左車輪 カウント増分 | 右車輪 カウント増分 |
|:---:|---:|---:|
| 1 | 4,208 | 4,166 |
| 2 | 4,145 | 4,000 |
| 3 | 4,098 | 4,113 |
| 4 | 4,116 | 4,122 |
| 5 | 4,090 | 4,113 |
| 6 | 4,145 | 4,170 |
| 7 | 4,176 | 4,181 |
| 8 | 4,122 | 4,139 |
| 9 | 4,188 | 4,179 |
| 10 | 4,138 | 4,134 |
| **平均値** | **4,142.6** | **4,131.7** |

$$\text{counts\_per\_rev} = \frac{\text{カウント増分} \times 339.292\text{ mm}}{2000\text{ mm}}$$
- 左推定値: $702.77\text{ counts/rev}$
- 右推定値: $700.92\text{ counts/rev}$
- 左右平均値: **$701.85\text{ counts/rev}$**（左右差わずか $0.26\%$）

この計測結果に基づき、設定値 `WHEEL_ENCODER_COUNTS_PER_REV` をカタログ値の `900U` から **`702U`** に正式改定しました（`firmware/ra8p1/SoundExplorationRover_CPU1/src/config/drive_config.h`）。

---

### 2. 車体走行幾何と旋回半径計算
実機での旋回円測定から、旋回モデルパラメータを確定しています。

- **車輪半径 $r$**: $54\text{ mm}$（直径 $108\text{ mm}$）
- **実効トレッド幅 $T$**: **$260\text{ mm}$**（車体中心から左右車輪中心まで $130\text{ mm}$）
- **旋回半径 $R$ の計算式（差動二輪モデル）**:
  $$R = \frac{T}{2} \cdot \frac{v_R + v_L}{v_R - v_L}$$
  - $v_R = v_L$: 直進（$R = \infty$）
  - $v_R = -v_L$: 超信地旋回（その場旋回、$R = 0$）
  - 片輪停止（$v_L = 0$）: 車体中心の旋回半径 $R = 130\text{ mm}$

---

### 3. サーボ中央値（ニュートラル）の調整
DS3225MGサーボは個体ごとにホーンのスプライン噛み合わせ誤差（$\pm$数度）があります。
1. 車輪を床から浮かせ、CPU1へ $0^\circ$ 指令を入力。
2. ホーンを直進に最も近い歯位置に取り付け。
3. Live Watchで `g_servo_center_trim_us[0..3]` を $\pm 5 \sim 10\text{ }\mu\text{s}$ 単位で微調整し、直進位置へ合わせる。
4. 決定値を `src/config/servo_config.h` の `SERVO_CENTER_TRIM_US_FR/FL/RR/RL` へ書き込む。

---

## 10.2 Live Watch デバッグ必携シンボル集

実機動作中にe² studioの **Live Watch（変数リアルタイム監視機能）** に登録すべき最重要変数は以下の通りです。

### CPU0 側コンテキスト（思考・センサ・通信）

| 変数名 | 期待される正常値 | 監視の目的・トラブル検知 |
|---|---|---|
| `g_task_think_state` | `LISTEN` / `MOVE_STEP` 等 | 思考ステートマシンの現在状態。`FAULT` の場合は異常停止 |
| `g_task_think_fault_flags` | `0x00000000` | CPU0でラッチされた全障害ビットマップ |
| `g_task_sensor_update_count` | 連続増加（50msごと） | センサタスクの生存性。増加が止まると異常 |
| `g_task_think_sensor_fresh` | `1` (TRUE) | センサデータ鮮度。200ms以上途絶すると `0` になり停止 |
| `g_cpu0_sensor_valid_flags` | `0x0F` | センサ健全性（Bit0:L, 1:C, 2:R, 3:IMU）。1つでも欠落すると走行不許可 |
| `g_task_acoustic_link_usb_configured` | `1` | XIAO ESP32-S3とのUSB CDC接続完了フラグ |
| `g_task_acoustic_link_observation` | 随時更新 | 最新のDoA角度（0〜359°）、音響レベルdBFS、VAD |

### CPU1 側コンテキスト（駆動・アクチュエータ・安全）

| 変数名 | 期待される正常値 | 監視の目的・トラブル検知 |
|---|---|---|
| `g_task_actuator_update_count` | 連続増加（1msごと） | 1ms制御ループの生存確認 |
| `g_task_actuator_period_last_ms` | `1` (ms) | 制御ループの実周期。1以外の値が頻発すると負荷過大 |
| `g_actuator_service_applied_sequence` | 連続増加（50msごと） | CPU0から正常に確定指令が届いているかを確認 |
| `g_actuator_service_fault_flags` | `0x0000` | CPU1障害フラグ。**Bit 0（0x0001）は1500msタイムアウト** |
| `g_drive_left_duty_permille` | 走行時非ゼロ（前進負） | 左モータへの実適用PWM Duty（‰） |
| `g_drive_right_duty_permille` | 走行時非ゼロ（前進正） | 右モータへの実適用PWM Duty（‰） |
| `g_encoder_left_rpm_x10` | 走行時非ゼロ（前進正） | 左エンコーダ実測回転数（$0.1\text{ RPM}$ 単位） |
| `g_encoder_right_rpm_x10` | 走行時非ゼロ（前進正） | 右エンコーダ実測回転数（$0.1\text{ RPM}$ 単位） |

---

## 10.3 よくある障害シナリオと処方箋（Troubleshooting）

### 障害1: モータやサーボが全く動かない
- **原因①（DIPスイッチ）**: EK-RA8P1の `SW4-4` がOFFのままになっている。
  - **対策**: 電源を切り、`SW4-4` を **ON** にする（Arduinoコネクタ端子を有効化）。
- **原因②（電源供給）**: サーボやBTS7960の外部電源がOFF、またはGNDが共通化されていない。
  - **対策**: モータ用12V電源およびサーボ用5-6V外部電源をONにし、スターGND結線を確認。
- **原因③（指令タイムアウト）**: CPU1のLive Watchで `g_actuator_service_fault_flags` の Bit 0 が立っている。
  - **対策**: CPU0が実行中（Resume）であるか、CPU0の `task_command` が正常に動いているかを確認。

### 障害2: I2Cセンサが認識されない（`valid_flags != 0x0F`）
- **原因①（DIPスイッチ）**: EK-RA8P1の `SW4-5` がONになっている（I3C側が選択されている）。
  - **対策**: 電源を切り、`SW4-5` を **OFF** にする（IIC1を有効化）。
- **原因②（プルアップ抵抗）**: バス上のプルアップ抵抗が強すぎる（または弱すぎる）。
  - **対策**: EK-RA8P1基板上のオンボードプルアップ（P013, P109）が有効になっているか確認。外部モジュールのプルアップ抵抗が重複して並列になりすぎていないか点検。

### 障害3: USB CDCのリンクが成立しない（`usb_configured == 0`）
- **原因①（ホストアダプタ）**: J7コネクタに「OTGホストアダプタ（USB-Cオス - USB-Aメス）」を介さずにUSB-Cケーブルを直結している。
  - **対策**: EK-RA8P1のJ7はホストアダプタを装着してはじめてハードウェア的にHostモードを認識します。指定のアダプタを接続してください。
- **原因②（VBUS給電ピン）**: `PD07`（`USBHS_VBUSEN`）が出力Highになっておらず、XIAOへ5Vが給電されていない。
  - **対策**: CPU0のFSP設定で `USBHS_VBUSEN` が正しく割り当てられているか確認。

### 障害4: モータの回転方向がおかしい（旋回してしまう）
- **原因**: 左右モータの極性設定または配線が逆になっている。
  - **対策**: `src/config/drive_config.h` のマウント極性定数（`DRIVE_LEFT_MOUNT_SIGN` / `DRIVE_RIGHT_MOUNT_SIGN`）を反転（$+1 \leftrightarrow -1$）して再ビルド。

---

## 10.4 まとめ

- 実測に基づく校正（702 counts/rev, 260mm トレッド）により、理論値とのズレを排除した高精度制御を実現。
- Live Watchにより、デュアルコア間の通信状態・生存性・センサ健全性をリアルタイムに可視化・診断可能。
- ハードウェアスイッチ（SW4）や電源・GNDの確実な設定が、安定動作のための最重要前提条件。

以上で、全10章にわたるSEROV開発・設計教科書の本文を完結します。
これらを統合的に閲覧できるポータル（`index.html`）およびアーキテクチャ図（`interactive_system_architecture.html`）を活用し、ぜひSEROVの開発・拡張に挑戦してください！
