# 鳴子連続音・test_map2 の追従検証（2026-09-24）

## 観測

- ログ: `software/rover-monitor/logs/rover-monitor-20260924-093842.jsonl`
- 環境: `tmp/test_map2.drawio`。テレビ無音、鳴子を継続。壁側へ障害物を追加。
- 最初の `TARGET` は09:39:05.194、距離0.021、DoA 15°（車体座標では左15°）。09:39:08の前進指令は左87 / 右128 RPM・左45°で、09:39:11までにオドメトリ方位が約-80°へ変化した。
- 左へ回頭中の09:39:08.540にはDoAが61°（車体座標で左61°）へ飛んだ。固定音源なら相対方位はむしろ正面へ近づくはずで、追従制御がこの観測を鵜呑みにしたことが左への過回頭を助長した。
- 09:39:05以降の認識結果の変化点では、`TARGET` 61件、`NOT_TARGET` 61件。`TARGET` の大半は距離0.015〜0.04で強い一致だが、`NOT_TARGET` にも同じピークbin 29が多数ある。受理境界0.055を単純に上げる根拠にはならない。
- 約173秒の実演区間では、ログ採取点ベースで `LISTEN` が約75秒、側面ToFが300mm未満の状態が約125秒。長時間化は認識の見逃しだけでなく、過回頭から壁際の回避ループに入った影響が大きい。
- 学習取り込みは従来、推論完了**時点**の `VAD=1` と音量を要求していた。今回の `TARGET` 行337件中 `VAD=1` は53件のみ。音量トリガ済みの特徴量パッチと、その後のライブVADは時刻が一致しない。

## 今回の変更

1. 移動中のDoAを、最後に採用した方位とオドメトリ回頭量から予測する。観測が予測から35°を超えて外れた場合は、そのDoAで操舵せず予測方位を使う。静止中の初回DoAと、移動中に整合した新観測は引き続き採用する。
2. 新たな走行開始は従来どおり `TARGET`（距離上限0.055）が必要。同じピーク帯域の一時的な `NOT_TARGET`（距離0.15〜0.35）では直ちに一致保持を破棄せず、既存の期限・走行喪失タイマーで停止する。ピークが違う距離0.15以上、または距離0.35以上なら即時破棄する。
3. 学習見本の取り込みを特徴量イベントの有効性で判断し、推論完了時点のVAD・音量では鳴子の短い打音を除外しない。

## 実機再試験

1. CPU0へ修正ファームウェアを書き込む。ESP32S3へ前回の特徴量1秒再判定周期をまだ書いていなければ、そちらも更新する。
2. 保存済み見本を変えず、同じ配置・テレビ無音で5回走る。最初の鳴子から音源到達までの秒数、最初の30秒のToF・方位・`TARGET` 更新を記録する。目標は各回30秒以内だが、実機で確認するまで達成とは扱わない。
3. 依然として鳴子の見逃しが多ければ、Acoustic AI Labで現在の保存済みprofileをログに記録した上で、静止・静かな環境で5見本を再学習する。正面・左前・右前と距離差を含め、1見本ずつ取り込まれたことを確認する。5見本が自動で集まるため、鳴子をただ長時間鳴らすだけで5件を埋めない。
4. 再学習後は同じ5回走行に加え、鳴子なしで人声や鈴を鳴らし、誤発進が増えないことを確認する。受理境界0.055は変更しない。

Rover MonitorのJSONLには192次元の特徴量要約が含まれない。新しい見本と見逃しパッチの距離分布を厳密に比較するには、Acoustic AI Labの `summary_chunk` と `profile_chunk` を同時に採る。

## 再学習後の追試（11:29〜11:31）

- 最新ログは `software/rover-monitor/logs/rover-monitor-20260924-112427.jsonl`（515行、約133秒）。学習済み見本は5件・ピークbin 29。保存閾値は起動時0.103だが、実際の受理上限は0.055で、ログの `recognition.threshold` もすぐ0.055になっている。再学習前の10:07ログ（289/724行がTARGET）に対し、今回のログは276/515行がTARGET。ただし音を鳴らした区間・停止区間が異なるため、この比率だけで改善率は評価できない。
- 今回のTARGET距離は中央値0.026、最大0.046。NOT_TARGETは227行で、同じピークbin 29の行が70行、距離は0.064以上。5件再学習後の見本は明瞭に近づいているが、断続打音や短い有効フレームによるイベント全体の距離ぶれは残る。
- 11:31:05以降、後方音をDoA約150〜163°として複数回取得し、`SPIN_PREP`にも入っていた。しかし3眼ToFの通常回避が先に前進旋回・ピボットへ遷移し、`SPIN_STEP`は一行も出ていない。例えば11:31:06はToF `[567,691,1230]` mmで前方に余裕があるのに、`SENSOR_TURN_LEFT` が音源旋回を上書きした。これは識別器ではなく最終指令の優先順位の問題。
- 11:29:59〜11:30:22にはTARGETのまま `SENSOR_SAFE_STOP` が約23秒続いた。公開ログのToF有効フラグは15だが、思考タスク内部のセンサー生存性・更新回数は記録されていないため原因は未特定。この区間は今回の旋回優先修正だけでは改善したとみなせない。
- 一方11:31:27前後のToF `[198,436,896]` mmや11:31:39の `[839,490,375]` mmは近接物があり、無条件の旋回優先は危険。3眼すべて500mm以上かつIMU/センサー正常時だけ音源旋回を優先し、それ以外は従来の回避・後退に渡す。後方センサーがないため、この判定は背後の安全までは保証しない。
- 識別結果が一時的にNOT_TARGETとなっても音源一致の2.5秒保持中なら、後方旋回は最後の確定方位とIMUで継続する。未一致区間のライブDoAは採用しない。別音の強い不一致または保持期限切れでは停止する。
- 古い鈴試験ログでは、保存閾値0.103の時に鈴が鳴子として扱われた距離が概ね0.057〜0.103に分布した。新しい見本とは別モデルなので直接の誤検知率ではないが、現在の0.055上限を単純に緩めるのは精度維持に反する。音響AIの構造変更を評価するには、新しい5見本と鳴子・鈴・人声・無音の192次元要約を同じAcoustic AI Labセッションで採る必要がある。

この追試に対するホスト側検証は `software/control-sim` のTensorFlow不要テスト201件を通過。実機への書き込み・走行検証は未実施であり、後方音への旋回成功率や30秒以内到達は未確認。

## 11:47〜11:50実機ログ：走行中の見逃しと左壁接触

- ログ: `software/rover-monitor/logs/rover-monitor-20260924-114745.jsonl`。今回は後方旋回が `SPIN_STEP` 45行に現れ、前回の「旋回要求が常に回避に上書きされる」状態は解消した。ただし現在の作業ツリーでは旋回クリアランス調整値が100mmに変更され、11:49:18にはToF `[281,382,276]` mmでも旋回した。安全上、以後は既存の脱出距離380mmを下限にしている。100mmという調整値自体は保存した。
- 全619観測行の認識はTARGET 253、NOT_TARGET 293、判定不能71。ただし無音のLISTEN行が多く、この比率を鳴子検出率と呼べない。ピークbinが23〜31かつ有効フレーム60以上の比較可能な行では、走行中154行中109行（71%）、LISTEN中115行中80行（70%）がTARGETだった。走行音の混合は疑えるが、このログだけで走行音が主因とは断定できない。Rover Monitorには元の192次元要約やPCMがないため、モーターだけの音と鳴子同時の分離効果は評価できない。
- 11:50:01頃の停車時、音源が実際には右前という申告に対し、生DoAは約39〜41°で車体基準では左約40°を示した。11:50:17にも生DoA約89°、目標方位-86°と左を示した。左壁近傍の反射などが疑われるが、ログだけで原因は確定しない。最終の走行指令は左ToF約304〜340mmを避ける右操舵+16〜22°であり、制御器が左への走行を命じたわけではない。
- 11:50:05〜11:50:24、左ToFは約303〜313mm、前方は約1000mm、右は約1450〜1500mm。右旋回指令の左/右RPM約114/97に対し、実測右輪は約28RPMから0RPMへ落ち、左輪は約70RPMを維持した。車体の推定方位は約161°から124°へ左回りに変化した。従来のスタック判定は左右平均の並進速度（約170〜300mm/s）を見るため、片輪停止を見逃した。
- 今回、CPU1の左右実測RPMを回避制御へ渡し、片輪のみ20RPM以下で反対輪30RPM以上なら700ms、側面380mm以内で左右実測が2倍以上乖離した場合は400ms継続で、上限付き後退・再旋回を開始する。側面ToFだけでは後退せず、通過可能な障害物は前進回避を維持する。CPU1状態sequenceが更新されない古い実測は時間加算しない。
- 強い走行音を特徴量から引くには、少なくとも同じ路面・速度・モーター状態の「モーターのみ」「鳴子のみ」「同時」「人声/鈴」のAcoustic AI Labデータが必要。log-mel上の一律減算や受理閾値の緩和は、鳴子の短い打音まで失うか、鈴の誤検出を増やす可能性があるため今回は未実装。

ホスト側のTensorFlow不要テスト205件とASan/UBSan smoke、特徴量プロトコル試験は通過。これらの修正は実機未書込・未検証。次の実機試験では、左壁側で右輪速度が低下したとき `SENSOR_BACKUP` が発生すること、左右ToFが380mm未満なら `SPIN_STEP` が始まらないことを確認する。

## 12:16〜12:18 四方向・障害物なし試験

- ログ: `software/rover-monitor/logs/rover-monitor-20260924-121633.jsonl`。各方向で鳴子を鳴らし、一動作後に手で車体を初期位置へ戻した。以下の物理方向は実験者の申告による。

| 音源 | XVFのDoA | 従来のCPU0目標 | 実機結果 | 較正後の目標 |
|---|---:|---:|---|---:|
| 右前 | 約303° | 右+57° | 右前へ走行 | 右+57° |
| 左前 | 約58° | 左-58° | 左前へ走行 | 左-58° |
| 右後 | 約141° | 左-141° | 左へ旋回（誤り） | 右+141° |
| 左後 | 約237° | 右+123° | 右へ旋回（誤り） | 左-123° |

前二例は制御用DoA（`audio.doa_deg`）の符号反転で合い、後二例は同じ符号で合う。旋回RPMの左右は旧目標方位の符号に従っていたため、今回の後方誤旋回の直接原因は、後方にも前方の符号反転を適用していた車体座標変換である。CPU0の共通変換（追従と音源定位の両方が利用）を前後で分け、Rover Monitorの音響線とCPU0目標線も同じ右正へ統一した。モーター方向やIMU符号は変更していない。

これは四方向の実測点に基づく較正であり、真横近辺（制御用DoA 90°/270°）の遷移や反射音まで検証したものではない。旋回中に車体相対DoAが一周期で90°を超えて飛んだ場合は単発での逆旋回を抑え、近い方位が3回連続したら新しい基準として受け入れる。次回はまず障害物なしで右後・左後を各3回ずつ、続いて真横付近から少しずつ前後へ音源を動かし、DoAと実際の旋回方向が連続して妥当かを確認する。実機未書込・未再試験のため、後方旋回成功はまだ確認していない。

## 12:54〜12:57 壁際・真後ろ境界の旋回方向

- ログ: `software/rover-monitor/logs/rover-monitor-20260924-125402.jsonl`。12:56:44、音源が実際には右後方という申告に対し、制御用DoA 185°は正規化で左-175°となり、5点平均後の目標は左-177°。ToF `[790,945,994]` mmで左-70 / 右+70 RPMの左旋回を開始した。これは障害物回避ではなく、真後ろ±180°付近の左右の曖昧さが最初の左旋回を選んだもの。
- 左旋回中に左ToFが361mm、さらに234mmまで減り、12:56:47から回避ルール`PIVOT_RIGHT`へ移った。12:56:48には左138mm。ここで安全調停より音源左旋回を無条件に優先すると、左輪の接触リスクが高い。
- 別の区間12:57:22には制御用DoAが169°→276°→285°→110°→294°と大きく揺れ、旋回状態のまま右RPM指令が反対符号へ変わった。従来は累積DoAが20°遠ざかるだけで駆動方向を一度反転しており、反射・±180°境界・回避中の観測に対して過敏だった。

修正では、音源が真後ろ付近（車体相対角の絶対値160°以上）のときに限り、左右ToF差が150mm以上なら広い側へ旋回する。同じ実測ToFなら右側が204mm広いため、目標を右+177°として右旋回を開始する。明瞭な右後・左後（例: ±135°）の符号は変えない。旋回を始めた後は方向を保持し、DoAの逆進行だけで駆動方向を反転しない。IMUの逆向き・進行不足、旋回時間上限、安定したDoAの正面到達判定は維持する。

前方・左右ToFが380mm未満なら音源スピンの優先は許さず、回避または停止を続ける。これは音源優先を危険な接触より上位にしないため。障害物なしと左壁際で右後音源を再試験し、`SPIN_STEP`の左右RPM符号が旋回途中で反転せず、近接時に右側への安全な回避へ移ることを確認する。今回の修正はまだ実機未書込・未再試験。
