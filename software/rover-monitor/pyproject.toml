[project]
name = "rover-monitor"
version = "0.1.0"
description = "Real-time UDP telemetry monitor for Rover"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.6",
    "websockets>=13.0",
]