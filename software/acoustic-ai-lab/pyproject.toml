[project]
name = "acoustic-ai-lab"
version = "0.1.0"
description = "Direct USB logging and visualisation workbench for Sound Exploration Rover acoustic AI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "pyserial>=3.5",
    "uvicorn>=0.30.6",
    "websockets>=13.0",
]

[tool.uv]
package = false
